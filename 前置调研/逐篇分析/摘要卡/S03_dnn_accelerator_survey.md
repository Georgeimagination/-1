# S03｜DNN 加速器架构综述

## 摘要（100–200 字）

这篇 2020 年综述以 CNN、RNN 和 GAN 为主，直接说明训练比推理多出反向传播、梯度计算与前向激活复用，因此数据依赖更长；扩展到多加速器后，通信还可能主导训练时间和能耗。文章同时把数据流、片上复用、量化、稀疏调度与架构选择联系起来。它主要转述早期工作，不讨论 Transformer、预填充、逐词解码、KV cache 或现代 HBM，适合作历史机制证据，不能直接外推到 LLM。
