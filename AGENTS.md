# 训练与推理芯片调研协作说明

## 这个项目在做什么

这项调研用于学习和整理市面上的训练与推理芯片。具体对象、比较口径和完成标准见[调研计划](调研计划.md)，当前主清单包含 36 个 SKU（硬件配置明确的产品型号）或官方单芯片配置。

项目有两个主要入口：[前置调研](前置调研/README.md)整理训练与推理的负载特点和架构关注点；[芯片调研](芯片调研/README.md)按原始资料、资料卡、产品详解和比较分析组织具体产品研究。产品详解用于连续阅读，原资料卡用于事实追溯。

工作范围包括资料阅读、资料卡与产品详解整理，以及调研计划规定的架构比较。现有结构化 CSV、字段覆盖合同、正式表事务、哈希签字、回滚包、选择运行和 Windows 校验都是旧流程留下的历史材料，不再维护，也不能作为阅读资料、填写资料卡或认定芯片完成的先决条件。

## 新会话从哪里恢复

新会话按以下顺序读取：

1. [进度记录](进度记录.md)，确认当前正在处理哪款芯片以及接下来要做什么；
2. [待调研芯片清单](待调研芯片清单.md)，确认 36 个调研对象的完成状态；
3. [调研计划](调研计划.md)，了解资料优先级、引用规则和完成标准；
4. [芯片调研目录](芯片调研/README.md)，进入对应产品详解或资料；需要补充资料卡时使用[资料卡模板](芯片调研/资料卡/模板.md)。

旧项目管理、结构化数据、审计链和临时文件统一位于 `99_归档/`，只用于追溯，不作为恢复入口。不要因为旧审计文件提出了未完成的合同、迁移或校验任务，就暂停当前 SKU 调研。

## 一款芯片怎么做

主会话一次处理一款芯片。先读官方规格书、数据手册、架构白皮书、技术白皮书和原始论文，再看官方开发文档、产品页和发布说明。只有资料卡仍有缺口、来源冲突，或者需要官方资料没有的实测数据时，才查看独立微基准、拆解、专业媒体和其他资料。

阅读时直接填写资料卡。每个数字、日期、规格和实质性技术描述都要在正文中引用原文位置。PDF 使用 `[1, p. 23]` 或 `[1, Table 4]`，网页使用 `[2, 章节标题]`。资料卡末尾列出正文实际使用的资料，这些资料就是该芯片的最小参考资料。完全重复、只有宣传文字或无法确认原始出处的资料不进入最小参考资料。

芯片、裸片、单芯片封装、加速卡、服务器和集群的数据不能混用。精度、稀疏、峰值、实测、带宽方向和测试条件要写清楚。模型大小、batch、上下文长度、MoE 通信量和 KV Cache 迁移量属于负载或测试条件，不写成芯片属性。没有公开资料的项目写“未公开”“未找到”或“不适用”，不猜测，也不用相邻型号或系统数据填空。

资料卡写完后通读一次，确认引用可定位、对象边界正确、单位和条件清楚。满足[调研计划](调研计划.md)中的资料卡完成标准后，就把该芯片记为“已完成”，不再等待其他发布或入库流程。

## 子代理怎么使用

主会话负责按清单调度、合并阅读结果、完成资料卡定稿和更新进度。每款芯片开始时，先列出真正需要阅读的资料，再按照篇幅和复杂度分成一至三个阅读包。资料少时由一名子代理通读全部资料；资料多时由两至三名子代理分别阅读不同资料包。长篇白皮书、规格书或论文可以单独构成一个阅读包，若干较短的官方网页、开发文档或发布说明可以合并为一个阅读包。不固定为“一款芯片一个子代理”，也不机械地为每篇短资料单独建立子任务。

每名阅读代理直接提交可写入资料卡的摘录，包含数据或技术描述、对象和条件、精确引用位置以及对应参考资料。主会话把这些摘录合并成一张资料卡，并核对关键数字、引用位置、对象边界、单位和测试条件。同一份资料涉及多款芯片时只精读一次，后续芯片可以复用已经确认的摘录，但仍要在资料卡中保留原始引用。

子代理按资料包分工，不继续派生子代理，也不互相审核。只有合并时出现无法直接判断的对象边界或来源冲突，才安排一次独立复核。当前芯片定稿后再进入下一款，已经完成的内容以资料卡文件保存，不要求主会话在上下文中长期保留全部细节。

如果确实需要指定模型，不使用 Luna、`medium` 或 `ultra`。Terra 最低为 `high`、默认 `xhigh`；Sol 默认 `high`，复杂冲突或最终复核使用 `xhigh`。模型设置不能扩大任务范围。

## 文件怎么维护

人类使用的项目管理文档只有[调研计划](调研计划.md)、[进度记录](进度记录.md)和[待调研芯片清单](待调研芯片清单.md)。本文件只作为新会话的协作入口。

每完成一款芯片，只更新两处：在进度记录中写明当前数量和下一款芯片，在待调研芯片清单中修改状态并添加资料卡链接。只有调研方法本身发生变化时才修改调研计划。不要新建任务台账、里程碑、决策记录、字段覆盖表或另一套进度系统。

`前置调研/` 保留专题报告、逐篇分析及其原文。`芯片调研/` 下只按四类内容组织：`原始资料/` 保存论文、网页快照和来源清单，`资料卡/` 保存产品卡及共享架构资料，`产品详解/` 保存文章、架构图和统一阅读版，`比较分析/` 保存当前比较报告及配套图表和数据。

历史比较版本、核查记录、旧流程文件和临时文件统一放在 `99_归档/`。`补充资料-codex不用管/` 保留独立，不纳入整理和维护。不要删除旧资料，也不要改写已有归档内容。
