# 执行异常与降级记录

记录日期：2026-08-14

本记录区分访问边界、沙箱限制、工具或运行时故障，以及操作构造错误。这里只记录对语料固定和复现有意义的事件。

| 阶段 | 分类 | 现象 | 处置 | 对结果的影响 |
|---|---|---|---|---|
| 首轮公开文件下载 | 沙箱网络限制 | 默认沙箱中的 PowerShell 无法连接三个公开 PDF 网址。 | 按审批流程以受控网络权限重试；用户批准后，三份 PDF 均下载并完成 SHA-256 校验。 | 无。链接本身有效。 |
| S09 固定 | 远程服务访问限制 | SAGE 的原始 HTML 与 PDF 对自动下载均返回 HTTP 403；公开页面正文仍可由网页读取工具读取。 | 不绕过站点控制；保存 2026-08-14 的官方可见正文快照，并保留 DOI、许可和 403 状态。 | 缺少原始 HTML/PDF 字节副本；正文、表格、章节定位和 CC BY 4.0 信息仍可审读。 |
| S16 版本固定 | 远程服务访问限制 | TACO DOI 的 PDF 入口返回 HTTP 403。 | 不绕过访问控制；按预先约定回退到公开的 arXiv `2104.02188v1`，并记录期刊版边界。 | 使用 13 页预印本而非 23 页期刊终稿；最终引用不得混用页码或默认为期刊版。 |
| 规范化脚本初次生成 | 模型／操作构造错误 | PowerShell here-string 中多余地把单引号加倍，导致 Python `SyntaxError`。 | 重写同一脚本并先执行语法编译检查，再运行抽取。 | 无；错误脚本未产生研究结论。 |
| S09 快照首次写入 | 模型／操作构造错误 | 把整页正文嵌入单条 PowerShell 命令，触发 Windows 命令行长度错误 206。 | 改为受控分块写入，再校验文件长度和哈希。 | 无；最终快照完整写入。 |
| S05 PDF 渲染 | 工具包装运行时故障 | bundled `pdftoppm.cmd` 包装器报告找不到路径。 | 改用同一依赖包内的明确 `pdftoppm.exe` 路径；成功渲染 251 页。 | 无；13 张联系表已生成。 |
| PDF 文本抽取 | 非致命 PDF 解析警告 | pypdf 对 S05 的若干错误指针发出 `Ignoring wrong pointing object` 警告；一次重跑因 PowerShell 把标准错误流视为失败而显示退出码 1。 | 把警告单独保存到 `PDF抽取警告.log`，并核对 19 项抽取日志、S05 的 251 页文本和 251 页图像均已产出。 | 无已知缺页；视觉核对仍以 PDF 渲染为准，不能只依赖抽取文本。 |

本轮没有发生用户拒绝、自动审批拒绝或审批服务连接失败。
