# 正式数据未变更证明

检查日期：2026-08-14

专题执行前后分别对正式接口涉及的32张CSV表计算文件字节数和SHA-256。比较结果为：路径集合相同，32张表的字节数和哈希逐项相同，差异数为0。本专题没有修改正式事实库、最小参考资料库或`资料卡/模板.md`。

| 项目 | 结果 |
| --- | --- |
| 执行前清单 | `正式数据基线_执行前.csv`，32行，清单SHA-256 `0803BCC1F562CE2F2CCDABF4CB6B390F3F634F79D4AE9C9A26B2BE5DEA39EC59` |
| 执行后清单 | `正式数据基线_执行后.csv`，32行，清单SHA-256 `3B854B26C1F2AC7B4B747E59EFA9BF81717673AD1E6117FBAB4DC20DE1F70876` |
| 文件路径差异 | 0 |
| 文件字节数差异 | 0 |
| 文件SHA-256差异 | 0 |

两份清单自身的SHA-256可以不同，因为执行前清单由早期工具写入，执行后清单采用当前统一的UTF-8序列化；证明依据是清单内每个正式文件的路径、字节数与SHA-256逐项相同。
