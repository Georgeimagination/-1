# 当前状态

更新日期：2026 年 8 月 18 日  
当前阶段：目录迁移已验收；逐芯片工作暂停  
总控：主代理  
总体状态：等待下一步；两类调研已经分目录保存，数据清洗与调研恢复链已通过内容级验收

## 当前结论

M1 三类试填、M2 高风险对象范围门、第一波 29 张七厂商架构卡、第二波工作包和第三波组合事务均已完成相应验证。第三波原子事务已经正式执行：向 20 张正式 CSV 写入 1,057 个已签字主键，并新增 5 张资料卡、8 份官方 HTML 快照和 2 份官方固定 PDF；事务未回滚，结构化数据完整性复核为 `accept`。

用户随后把研究目标收紧为训练与推理芯片本体。后续不再把模组、加速卡、云实例、服务器、机架或 Pod 作为主线交付对象。工作顺序为：先确定芯片名单，再逐芯片完成数据工程，最后为每款芯片生成一份可独立阅读的综合资料卡。

用户于 2026 年 8 月 17 日确认 49 个芯片对象，包括 38 个主样本和 11 个历史锚点，对应 45 个共享硅设计组。`清单/训练与推理芯片名单.md` 已成为唯一维护入口。原 77 行候选全集、计数验收、阶段交接和独立复核已移入 `审计/归档/芯片名单冻结_2026-08-17/`，不再并行维护。

2026 年 8 月 17 日已经按冻结名单顺序启动首个单芯片工作包 `R1-CHIP-NVIDIA-GA100`。当前只处理 `NVIDIA GA100 die`：先核对正式库中的 GA100/Ampere 对象、关系、事实和来源能否复用，再补字段与证据缺口；完成独立复核、最小来源重跑和综合资料卡验收后，才进入下一款芯片。

用户于 2026 年 8 月 17 日要求暂停 GA100，并于 8 月 18 日决定把前置架构关注点专题与 49 款芯片主线分到两个顶层目录。迁移已经通过 `accept_with_runtime_caveat` 验收：原始资料、32 张正式表、来源筛选记录、最小来源选择运行、既有资料卡和审计备份的内容基线均未变化。前置专题现位于 `../01_前置调研-训练推理架构关注点/` 的兄弟目录，当前目录继续作为正式主线运行根。

跨平台恢复检查确认 32 张正式表、153 个来源入口、79 个本地路径与哈希、11 次选择运行和 106 个成员都能从新主线根解析。资料收集脚本已经改为默认写预览目录，只有显式 `--apply` 才写正式资料池；三个 PowerShell 校验器都能从脚本位置推导本目录。完整验收见 `审计/目录迁移验收_2026-08-18.md`。

主线仍不开展跨芯片定量比较或性能排名。工艺、容量、带宽、功耗、频率、单芯片总峰值和系统配置只写入相应实现对象，不能回填成无条件的架构代际事实；已完成的19份固定语料专题保持隔离，不改变这条规则。

2026 年 8 月 14 日按用户明确指令完成[训练与推理芯片架构差异专题](../../01_前置调研-训练推理架构关注点/README.md)：19名不同代理逐篇审读固定语料，主会话交付总结报告、0.1版模板、证据矩阵、三票核验和H100/Inferentia2只读试填。17条核心结论全票确认，C11、C13、C18限定保留；32张正式表执行前后哈希相同。该专题没有改变M2正式工作队列。

## 当前基线

PDF 清单仍登记 111 份、313,921,821 字节和 2,210 页；磁盘实有 113 份、315,363,681 字节和约 2,214 页。差异来自第三波新增但尚未登记的 MI455X 与 MI350P 两份官方 brochure。旧清单的 111 份文件仍全部通过路径、大小、哈希和页数核对；资料池校验目前因这两份未登记 PDF 有 4 项关键失败，另保留 1 条既有 Google TPU 解析警告。

正式库现有 78 个对象、27 条关系、798 条事实、832 条逐来源断言和 1,059 条字段要求。来源侧有 92 个来源家族、93 个内容版本和 153 个访问入口。正式库有 11 次最小来源选择运行和 106 个成员；10 次运行状态为 `reviewed`、1 次为 `superseded`，106 个成员的行级复核状态均为 `reviewed`。

事实字段现有 120 个，注册表有 324 个列定义。字段主体合同已分为事实主体合同和字段要求目标合同，正式校验器默认执行硬门；当前硬门通过 125,619 项检查，32 表聚合值为 `5f62191bfc737535802cd6e0524b1fdf7a742ca36300db11b5a9233d4540052a`。

现有 78 个正式对象仍临时分为 10 个 `chip_primary`、29 个 `architecture_evidence` 和 39 个 `out_of_scope_nonchip`，范围校验通过。这个 10/29/39 只是正式库存分类，不能替代 49 个冻结芯片。下一阶段先逐芯片盘点正式库可复用内容，再为缺失对象和事实建立工作包；不从归档候选全集批量回写。

## 已正式合并的工作包

| 工作包 | 正式结果 |
|---|---|
| `M2-GHC-ARCH` | 4 张架构卡、35 条事实、35 条断言、80 条字段要求、7 个最小来源成员和 46 条实现对象待办。 |
| `M2-GA-ARCH` | 14 张架构卡、141 条事实、141 条断言、72 条字段要求、19 个最小来源成员和 65 条实现对象待办。 |
| `M2-NA-ARCH` | 11 张架构卡、185 条事实、185 条断言、369 条字段要求、17 个最小来源成员和 9 条实现对象待办。 |
| `M2-W2-AMD-HELIOS-SYSTEM` | 1 张系统观察卡、18 条事实、22 条断言、17 条字段要求和 3 个最小来源成员。 |
| `M2-W2-TRN2-CORRECTION` | 修正 M1 Trainium2 事实链并新增 3 张实例卡；删除 11 条错误事实链，重建 41 条实例事实映射。 |
| `M2-W2-AWS-PACKAGE-FACTS` | 4 张封装实现卡、57 条事实、69 条断言、71 条字段要求、A01 至 A08 八源最小集和 11 份同日官方快照。 |
| `M2-W3-BUNDLE-FORMAL-TRANSACTION` | 第三波组合事务正式写入 1,057 个签字主键和 15 个载荷；合并后校验 125,619 项，未回滚。MI455X 模组和 MI350P 卡随后退出芯片主线，Ascend 950 裸片与封装保留。 |

Helios 包正式新增 102 行结构化记录。其中 97 行依据独立清单把 `review_status` 从 `draft` 改为 `reviewed`，与 3 AI exaflops 有关的 5 行仍为 `needs_resolution`。验收见 `审计/M2_Helios_参考机架包正式合并验收.md`。

Hopper 和 Trainium2 的架构卡继续通过正式关系复用 M1 事实，没有创建第二套原子事实。MLUarch05 保留低披露边界。TPU 8t/8i、Trainium4、Rubin CPX、Ascend 960/970 和 MI430X 已进入冻结名单；设计期或路线图数据必须显式标为 `projected` 或 `pending_verification`。

字段主体合同不是产品工作包，但已经作为正式数据模型迁移完成。`allowed_subject_kinds` 约束事实主体，`allowed_requirement_target_kinds` 约束字段要求目标；二者由当前校验器分别检查。

## 第二波状态

AWS 四代物理对象事实包已经在字段迁移后的基线上重新签字并正式合并，合并后独立复核为 `accept / closed`。486 行签字包含 477 项授权写入和 9 项不写守卫；事务替换 20 张正式表、写入 11 份快照和 4 张资料卡，没有使用回滚。五组官方资料分歧继续保持 `needs_resolution`，没有在合并中静默选值。验收见 `审计/M2_AWS四代物理对象事实包正式合并验收.md`。

Trainium2 三实例轻量包曾被独立复核拒绝。复核发现 S10 不能支撑 10 条 BF16、FP16、TF32 或 trn2u 历史稀疏事实，S06 也不能支撑 trn2.3xlarge 的芯片数量；网络带宽还混淆了实例注入、系统聚合与单链路口径。纠错包随后完成全部删除和字段修复，由未参与修复的代理给出 `accept` 并签署 17 行生命周期清单。正式库现只保留两条 S10 FP8 历史断言，错误的 UltraServer 3.2 Tbps 主体链已删除；三张实例卡和修正后的 M1 Trainium2 卡均已进入正式目录。验收见 `审计/M2_Trainium2_纠错包正式合并验收.md`。

Google TPU 的云端单器件、VM 或机器类型、slice/Pod 三层模型已经通过。v5e、v5p、v6e 三个云端单器件对象和三条架构关系已预留，正式范围映射现有 63 行。来源修正版经独立复核后有限接收 1 个 family、1 个 source、6 个 endpoint 和五份 renderer 提取，当时正式校验为 93,378 项；G15 原始响应体、正式 screening/coverage/selection、器件事实和资料卡仍未接收。

## 第三波与范围收缩

MI455X、MI350P 与 Ascend 950 组合事务已经完成正式写入和合并后完整性核查。真实载荷是 5 张资料卡、8 份官方 HTML 快照和 2 份官方固定 PDF，不是早先概述中的“10 份网页快照”。正式事务本身通过，但用户随后明确只关心训练与推理芯片本体，因此 MI455X 模组和 MI350P PCIe 卡退出主线；Ascend 950 die、950PR 和 950DT 仍属于芯片主线。

既有非芯片数据不物理删除，保留作历史证据和审计材料；从现在起不再继续补齐这些对象，也不把它们计入主线完成度或最小来源分母。当前正式卡片文件共有 45 张，但不能解读为 45 款芯片已经形成综合卡。

## 未入选对象的处理

路线图观察、边界待决和排除对象已经随 77 行候选全集归档，不再作为活动名单维护。以后若某个对象满足正式纳入门槛，直接修改 `清单/训练与推理芯片名单.md`，并在决策记录中说明新增依据；不恢复并行观察表。

## 生命周期与合并纪律

四张核心表此前有 1,924 行按显式主键清单完成 `draft`→`reviewed` 迁移；事实语义状态、来源筛选结论和其他列均未改变。Helios 包另有 97 行按独立清单完成同类迁移。Trainium2 纠错包后置迁移另改动 25 个主键中的 27 个状态单元格，只涉及 2 次选择运行的 `status / review_status` 和 23 个成员的 `review_status`。`reviewed` 只表示行级复核完成，不等于把 `provisional`、`needs_resolution` 或 `pending_verification` 批准为最终结论。

后续芯片工作包继续使用 staging、独立复核、总控验收和顺序合并。子包不能并发写正式 CSV；正式库发生有意合并后，仍在运行的复核必须重新建立哈希基线，不能把并发变化误判成自身副作用。

## 已知风险

`scripts/collect_references.py` 仍使用旧 macOS 绝对路径，不能在当前 Windows 工作区直接重跑。资料池的平台分布不均；动态产品页、云配置和生产爬坡状态需要保存截止日快照。寒武纪 CNToolkit 与 CNNL 页面返回远端 HTTP 401，这是远端服务限制，不是沙箱或审批问题。

第三波 5 张正式卡仍有 7 处旧阶段文字。MI455X 与 MI350P 已退出新主线，不再专门补卡；Ascend 950 三张卡将在逐芯片阶段按“一芯片一文档”重生成。资料池的两份 AMD brochure 仍待登记或转入历史证据口径，不能为了让校验通过而直接删除。

字段主体合同的 132 行审计已经闭环：12 行扩合同、12 行迁主体、108 行保留；4 条 Trainium2 要求已迁到芯片级精度路径。正式 `fields.csv` 新增 `allowed_requirement_target_kinds`，校验器加入同目标事实闭环和架构关系投影例外。迁移有 166 行精确独立签字、10 文件备份和三种正式校验结果；见 `审计/字段主体合同正式迁移验收.md`。仍需防止旧 staging 用 12 列表头覆盖当前 13 列 `fields.csv`。

`M2-GA-ARCH` 的 `.validation-temp-R1` 清理曾被自动审批复核的连接或用量上限拦截；这不是用户拒绝，也不是沙箱拒绝。目录包含指向正式 `论文/` 的 Windows 目录联接，只能按核对过的精确路径处理，不能递归跟随。四份 AMD ISA PDF 的固定下载也曾因自动复核用量耗尽被拒，正式库保留 AMD 官方在线入口。

GHC 合并备份仍有两份内容相同的哈希清单；删除其中一份的动作曾被自动审批用量上限拦截。正式恢复依据使用 `backup-hashes-v2.csv`，重复清单不影响验收。

## 下一停点

任务 `R1-DIRECTORY-MIGRATION` 已完成，`R1-CHIP-NVIDIA-GA100` 仍按用户要求暂停。当前没有正在写入正式数据的芯片工作包。

恢复主线前，先在 Windows 环境补跑 `Test-SourcePool.ps1`、`Test-ChipScope.ps1` 和 `Validate-ResearchData.ps1`；当前 macOS 环境没有 PowerShell 运行时，这项限制已经记入迁移验收。补跑通过后，再由用户决定先对齐前置专题的 0.1 分析模板与正式 0.2 资料卡模板，还是直接恢复 GA100 复用审计。

## 恢复工作时先读

1. `AGENTS.md`
2. `清单/训练与推理芯片名单.md`
3. `进度/决策记录.md` 的 DEC-030 至 DEC-034
4. `研究计划.md`
5. `进度/当前状态.md` 与 `进度/任务台账.csv`
6. `审计/训练推理芯片主线对象范围.csv`、`审计/训练推理芯片主线选择运行范围.csv` 和 `数据/objects.csv`
7. `资料卡/模板.md`
