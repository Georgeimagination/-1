# 当前状态

更新日期：2026 年 8 月 13 日  
当前阶段：M2 裸片、封装、SKU、云配置与系统资料卡  
总控：主代理  
总体状态：进行中

## 当前结论

M1 三类试填、M2 高风险对象范围门和第一波七厂商架构卡均已通过。`M2-GHC-ARCH`、`M2-GA-ARCH`、`M2-NA-ARCH` 分别完成修正、独立复核和正式合并，验收记录见：

- `审计/M2_GHC_架构包正式合并验收.md`
- `审计/M2_GA_架构包正式合并验收.md`
- `审计/M2_NA_架构包正式合并验收.md`

第一波共有 29 张架构卡；连同 M1 三张试填卡，`资料卡/型号索引.md` 现列出 32 张已验收卡片。接下来只把工艺、容量、带宽、功耗、频率、单芯片总峰值和系统配置写入相应实现对象；这些数据不能回填为无条件的架构代际事实。M2 仍不分析训练与推理架构差异。

## 当前基线

资料池现有 111 份内容去重的 PDF，共 313,921,821 字节、2,210 页；网页清单 914 行，按大小写敏感字符串统计为 893 个入口，忽略大小写为 892 个入口。PDF 覆盖 NVIDIA 65 份、Google TPU 25 份、华为 15 份、Groq LPU 2 份和 AMD Instinct 4 份；AWS 与寒武纪仍没有本地 PDF。

正式库现有 75 个对象和 24 条关系。事实层有 614 条事实、600 条逐来源断言和 786 条字段要求。新增四个 AWS 单器件对象目前只完成身份预留，尚未产生实现事实。来源筛选有 62 个 `selected`、10 个 `redundant_covered`、2 个 `pending`、3 个 `out_of_scope`、1 个 `rejected_unreliable`、2 个 `inaccessible` 和 1 个 `lead_only`。已经保存 4 次反向移除运行、62 个成员。

事实字段现有 120 个。`byte/s` 的读写带宽与 `byte/cycle` 的每周期读写传输量已经分开；XCD、CU 或通道作用域写入条件集。最新正式数据校验通过 91,827 项检查。资料池校验核对 111/111 份 PDF 的路径、大小、SHA-256 和页数并通过，保留一条既有 Google TPU PDF 解析警告。

## 第一波架构包

| 工作包 | 状态 | 正式结果 |
|---|---|---|
| `M2-GHC-ARCH` | 已正式合并 | 4 张卡、35 条事实、35 条断言、80 条字段要求、7 个最小来源成员和 46 条实现对象待办。 |
| `M2-GA-ARCH` | 已正式合并 | 14 张卡、141 条事实、141 条断言、72 条字段要求、19 个最小来源成员和 65 条实现对象待办。 |
| `M2-NA-ARCH` | 已正式合并 | 11 张卡、185 条事实、185 条断言、369 条字段要求、17 个最小来源成员和 9 条实现对象待办。 |

Hopper 和 Trainium2 的 M1 事实继续复用，没有创建第二套原子事实。MLUarch05 保留低披露边界。TPU 8t、TPU 8i、Trainium4、Rubin 和 CDNA 6 仍是观察对象，不按完整量产架构解释。

## 已冻结的延期项

A800 数据中心候选、H800 和 H20 要先拆成固定物理 SKU。LP30、MI440X、MI430X 和 MI500 Series 的对象层级还未确定；H20 BFX 存在官方架构归类冲突；Trainium4 未来系统没有独立产品身份。寒武纪 BANG v5.0、MLU570 和 MLU370-M8 仍受 CNToolkit 正文不可访问限制。它们只有在取得范围验收文件列明的解除证据后才能进入正式抽取。

华为 Atlas 950 保持一个 SuperPoD 对象，64、1024、8192 NPU 只作为带条件和日期的配置。CloudMatrix384 是硬件超节点，其上的云服务另建候选。TPU 8t/8i 不制造尚未出现的 accelerator type 或 machine type。

## 下一停点

第二波排队建议已经过独立红队。首组执行槽调整为 AWS 物理芯片或封装、AMD Helios 参考机架、Trainium2 三个云实例；三项当前都是 `accept_with_fixes`，必须先关闭各自的身份或动态来源门。Google TPU 配置族原方案因把配置容器与单设备实现事实混层而被 `reject`，五个建议对象 ID 不预留，十行待办保持原位，先重做对象模型与同日来源冻结。裁决见 `审计/子代理交接/m2_wave2_independent_review.md`，执行边界见 `DEC-023`。所有包继续采用 staging、独立复核和总控顺序合并，不并发写正式 CSV。
AWS package 的独立身份门已经通过，四个 `package / needs_resolution` 单器件对象与四条架构关系已正式预留；`package` 仍只是保守事实容器，物理构造没有公开。11 个 HTML 抓取件通过哈希复算，但实际获取日为 2026-08-13，事实抽取前须另建同日 `web_snapshot` endpoint，并登记 Trainium3 的 144 GB/GiB 分歧。Helios 已固定两份对象匹配的 AMD 页面，并在本地正文找到“72 个 MI455X”、参考设计而非在售产品、预期 2026 年下半年规模部署等直接陈述，正在收口轻量卡与结构化片段。Trn2 三实例包已排队。Google 对象模型已通过独立复核，裁决为 `accept_with_fixes`：云端单器件、VM 或机器类型、slice/Pod 三层成立，原五个 slice/configuration family 建议 ID 撤回。v5e、v5p、v6e 三张云端器件卡尚未开工；须先拆分 `GV5P-03` 的 95/96 GiB 处置项，补 G15 与 tpu-machines 来源，固定动态 HTML，并确定配置行的结构化落点。

## 已知风险

`scripts/collect_references.py` 仍使用旧 macOS 绝对路径，不能在当前 Windows 工作区直接重跑。资料池的平台分布不均；动态产品页、云配置和生产爬坡状态需要保存截止日快照。寒武纪 CNToolkit 与 CNNL 页面返回远端 HTTP 401，这是远端服务限制，不是沙箱或审批问题。

`M2-GA-ARCH` 的 `.validation-temp-R1` 清理被自动审批复核的连接或用量上限拦截；这不是用户拒绝，也不是沙箱拒绝。目录包含指向正式 `论文/` 的 Windows 目录联接，后续只能按核对过的精确路径处理，不能递归跟随。四份 AMD ISA PDF 的固定下载也曾因 auto-review 用量耗尽被拒，正式库保留 AMD 官方在线入口。

GHC 合并备份仍有两份内容相同的哈希清单；删除其中一份的动作被自动审批用量上限拦截。正式恢复依据使用 `backup-hashes-v2.csv`，重复清单不影响验收。

行级复核状态生命周期迁移已经完成。1,924 行显式 PK 清单由未参与生成的代理独立复算并签字 `accept`；总控按绑定哈希备份四张正式表后，恰好把 1,924 个 `review_status` 从 `draft` 改为 `reviewed`。事实 `resolution_state`、断言 `extraction_status`、字段要求与来源筛选结论均为 0 变化，其他列也没有变化；正式校验通过 91,827 项检查。验收见 `审计/正式库四表生命周期迁移验收.md`。`reviewed` 只表示行级复核完成，不等于把 `provisional`、`needs_resolution` 或 `pending_verification` 批准为最终结论。

## 恢复工作时先读

1. `AGENTS.md`
2. `研究计划.md`
3. `进度/当前状态.md`
4. `进度/任务台账.csv`
5. `审计/M2_对象范围验收.md`
6. 三份 M2 架构包正式合并验收与 `审计/M2_第一波收尾一致性复核.md`
7. 三份 M2 批次计划
8. `审计/M2-GA-ARCH_实现对象待办.csv`、`审计/M2-NA-ARCH_实现对象待办.csv` 和 `审计/M2-GHC-ARCH_实现对象待办.csv`