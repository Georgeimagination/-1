# 项目协作说明

## 项目目标与范围

本目录要建立两项可持续维护的成果：

1. NVIDIA GPU、Google TPU、AWS Trainium/Inferentia、Groq LPU、寒武纪、华为昇腾和 AMD Instinct 的型号资料卡；
2. 与资料卡逐条对应、经过内容去重的最小参考资料库。

现阶段只整理产品身份、架构规格、原子事实、证据出处、矛盾和缺口，不分析训练导向与推理导向芯片的架构差异，也不做跨厂商排名。厂商定位和部署场景可以作为原文事实记录，不能提前写成跨产品结论。

相邻目录 `调研-训练推理芯片调研` 与 `调研-训练推理芯片调研-codex-v3` 是历史初步调研，只作为资料线索和旧记录读取。新的资料卡、数据、审计记录、下载文件和报告全部写入本目录；历史报告不能替代原始来源支撑关键规格。

## 当前状态

- 两个历史调研目录最初汇总出 102 份内容去重的 PDF；M1 补入 3 份 NVIDIA H100 固定资料，M2 GHC 架构包补入 2 份 Groq 固定资料，M2 NVIDIA/AMD 架构包再补入 4 份 AMD 白皮书。当前资料池共 111 份 PDF、313,921,821 字节、2,210 页，另有网页与在线资料清单及 11 条待补论文记录。
- 本地 PDF 现有覆盖为 NVIDIA 65 份、Google TPU 25 份、华为昇腾与达芬奇 15 份、Groq LPU 2 份、AMD Instinct 4 份；AWS 和寒武纪仍无本地 PDF。Trainium2 有 15 个官方网页快照，NVIDIA Groq 3、Blackwell Ultra、Rubin、AMD CDNA、AMD Helios 与寒武纪产品身份页有截止日快照，MLU590 有 WAIC 快照和固定 MLU-OPS 提交。
- `清单/网页与在线资料.csv` 有 914 行。区分大小写的原始字符串为 893 个，PowerShell 默认忽略大小写时为 892 个；差异来自同一 DOI 的 `MM/mm` 路径。后续分别记录原始入口、规范化入口和来源家族，不能全局把网页路径转成小写。
- `资料卡/字段字典.md` 和 `资料卡/模板.md` 已升为 0.2 版；32 张正式表、字段与枚举注册表和校验器通过 M1 验收。M2 高风险对象身份核对已通过；第一波三个架构工作包均已独立复核并正式合并，共有 29 张七厂商架构卡。
- `进度/` 已建立当前状态、任务台账、里程碑和决策记录；它们是跨会话恢复工作的正式状态来源。第二波已预留 AWS 四个单器件对象及其架构关系；Helios 参考机架观察卡和 Trainium2 三实例纠错包均已正式合并；AWS 四代物理对象事实包经首轮独立复核后完成修正并冻结，正在最终独立复核。Google TPU 的云端器件、机器类型和 slice/Pod 三层模型已通过独立复核；v5e、v5p、v6e 三个云端器件和架构关系已经预留，来源冻结门仍须修正。
- 资料池离线审计、七厂商候选型号普查、173 行对象候选规范化、可移植只读校验器、0.1 数据模型审查、三张试填卡和 32 张正式表均已由主代理复核通过；M2 三份批次计划覆盖七厂商，56 行高风险对象候选已完成身份核对、独立复核和总控验收。
- 本目录没有可用的 Git 版本记录，也没有自动化测试框架。

## 已完成内容

- 从两个历史调研目录收集 PDF 和在线来源线索。
- 按 SHA-256 对 PDF 做内容去重，并记录文件路径、页数、大小和解析状态。
- 建立 `论文PDF清单.csv`、`网页与在线资料.csv`、`待补论文清单.csv` 和 `汇总统计.json`。
- 编写 `参考资料总览.md`，说明当前资料来源、数量和已知边界。
- 编写研究计划、README 和协作说明。
- 建立资料卡字段字典、资料卡模板、七个厂商目录和 `资料卡/型号索引.md`；索引当前列出 29 张架构卡、3 张 M1 试填卡、3 张第二波 Trainium2 实例卡和 1 张系统观察卡。
- 建立当前状态、任务台账、里程碑、决策记录和子代理交接机制。
- 完成资料池离线基线审计；补取 H100 三份、Groq 两份和 AMD 四份固定 PDF 后有意更新为 111 份，并重新确认文件数、大小、哈希、页数及 URL 两种去重口径。
- 完成七条产品线候选型号普查，校验 64 个唯一官方入口，并对 2025 至 2026 年的高风险动态状态做总控抽查。
- 把候选型号拆为 173 个一行一对象的候选记录，七厂商、对象层级、范围状态、64 个来源键和关系提示均通过总控复算；范围冻结前使用 CAND 临时标识。
- 建立 `scripts/validation/Test-SourcePool.ps1` 只读校验器；总控实跑确认其复现全部资料池基线，退出码为 0，只有一条已知 PDF 解析警告。
- 完成 0.1 数据模型审查，确定统一对象层、唯一规范事实层、逐来源断言、七目标外键和来源家族/访问入口分层。
- 建立 32 张正式空表、323 个注册列、118 个 M1 事实字段、71 组受控枚举和数据校验器；M2 独立复核补入两个 `byte/cycle` 存储传输字段后，当前事实字段为 120 个。总控已重跑正式校验器。
- M1 已通过；当时有 14 个对象和 13 条关系。M2 首轮范围验收形成 71 个对象和 20 条关系；第二波 AWS 补裁决预留四个 `package` 对象及四条架构关系，Google 补裁决再预留三个 `cloud_accelerator` 对象及三条架构关系，正式库现有 78 个对象和 27 条关系。第一波三个架构包和 Helios 系统观察包合并后，Trainium2 纠错包又删除 11 条错误事实链并重建相关证据。正式库现有 621 条事实、611 条断言和 792 条字段要求；最新校验通过 93,251 项检查。未通过身份门的候选继续使用 `CAND` 标识。
- 完成 H100 试填前来源预审，固定 H100 SXM 对象边界、发布期规格版本关系和独立微基准去重候选；随后从 NVIDIA 官方入口取得 v1.04 白皮书固定 PDF并更新资料池基线。
- H100、Trainium2 和 MLU590 三张试填卡已完成结构化合并、两轮独立复核与总控验收。MLU590 供货状态继续待验证，未找到一手定值的规格不采用第三方数字补齐。
- Trainium2 三实例独立复核随后发现 M1 事实链中有 10 条 S10 来源不支持的 BF16、FP16、TF32 或 trn2u 历史稀疏事实，以及 5 条把实例或系统聚合网络带宽写成单链路速率的字段错误。纠错包已经通过新的独立复核并正式合并；十条无证据 S10 规格和错误 UltraServer 3.2 Tbps 主体链已从正式库删除。
- M2 首轮对象范围验收完成 56 行高风险候选映射；第二波 AWS 物理对象补裁决新增 4 行，`审计/M2_对象范围映射.csv` 现有 60 行。四个 AWS 单器件对象按 `package / needs_resolution` 预留，并分别连接已验收架构；验收记录为 `审计/M2_第二波_AWS物理对象范围补充验收.md`。第一波 GHC、GA、NA 三个架构包已通过独立复核与总控验收；对应验收文件为 `审计/M2_GHC_架构包正式合并验收.md`、`审计/M2_GA_架构包正式合并验收.md` 和 `审计/M2_NA_架构包正式合并验收.md`。第一波收尾清理了正式对象与来源选择运行中的旧阶段说明，并把恢复点记入 `审计/M2_第一波收尾一致性复核.md`。第二波 Helios 系统观察包也已通过独立复核和总控验收，正式卡及合并边界见 `审计/M2_Helios_参考机架包正式合并验收.md`。
- 四张核心表的行级生命周期迁移已完成。1,924 行显式主键清单经未参与生成者独立签字后执行，恰好改变 1,924 个 `review_status`，语义列和其他列变化均为 0；验收见 `审计/正式库四表生命周期迁移验收.md`。后续仍须把 `reviewed` 与 `accepted`、`needs_resolution`、`pending_verification` 分开解释。

## 待完成工作

后续按 `研究计划.md`、`审计/M2_对象范围验收.md`、三份 M2 批次计划和各架构包实现对象待办推进。第一波架构包已经关闭；当前进入裸片、封装、SKU、云配置和系统工作包，架构事实只通过正式关系复用，不复制到每个实现对象。

资料库建成前，不进入训练与推理架构差异分析。缺少公开资料的字段保留为空并标明原因，不从相邻型号、系统聚合值或媒体推测中补齐。

## 已知问题

- `scripts/collect_references.py` 仍使用原作者机器上的 macOS 绝对路径，不能在当前 Windows 工作区直接重跑；修复前只能把现有清单视为一次性汇总结果。
- H100、MLU590、第一波 29 张架构卡和 Helios 系统观察卡已完成相应事实覆盖与反向移除。Trainium2 三实例纠错包已完成正式合并；AWS 四代物理对象修正版已冻结并进入最终独立复核，其余裸片、封装、SKU、云配置和系统对象仍须逐包抽取、独立复核和总控验收。
- 111 份本地 PDF 的平台分布仍不均，AWS 和寒武纪没有本地 PDF；AMD 目前只有 4 份架构白皮书，产品与系统资料仍主要依赖官方网页。
- 动态产品页、新闻稿、媒体报道和第三方分析较多，部分内容可能只是复述固定版数据手册或白皮书。
- AWS 第二波 11 份 HTML 实际获取于 2026-08-13，不能挂到旧的 2026-08-12 endpoint；事实抽取前须另建同日 `web_snapshot`，并保留 Trainium3 的 144 GB 与 144 GiB 单位分歧。
- 字段主体合同只读预审发现 132 行正式事实或字段要求的实际主体类型不在 `fields.csv.allowed_subject_kinds` 中，涉及 26 个字段、28 组组合；其中 8 行是事实，124 行是字段要求。当前 validator 尚未检查这项合同。明细见 `审计/字段主体合同全库预审.csv`，不得据此批量扩合同或迁主体。
- 芯片、封装、卡、服务器、机架和集群参数在旧材料中偶有混用，后续必须按对象层级重建。
- 部分厂商不公开向量吞吐、片上带宽、累加器物理位宽、互联有效载荷或持续性能。未公开和未找到可靠来源要分别记录。

## 目录与主要文件职责

当前文件：

- `README.md`：项目入口、范围、当前状态和阅读顺序。
- `AGENTS.md`：长期协作约定和项目记忆。
- `研究计划.md`：资料卡、事实数据和最小参考资料库的实施方案与验收标准。
- `参考资料总览.md`：已有资料汇总结果。
- `论文/`：当前本地 PDF 原始资料池。原文件按只读来源处理。
- `清单/`：已有 PDF、网页和待补资料清单。
- `进度/`：当前状态、任务台账、里程碑和决策记录。
- `资料卡/`：字段字典、模板、型号索引和分厂商资料卡。
- `数据/`：正式对象、事实、字段要求和派生指标表；已有 18 张表与字段、枚举注册表，当前有 78 个对象和 27 条关系。
- `最小参考资料库/`：来源家族、内容版本、访问入口、逐来源断言、筛选、覆盖、冲突、检索和反向移除记录；已有 14 张表。
- `审计/`：资料池、型号范围、一致性、交付检查和子代理交接。
- `scripts/collect_references.py`：历史收集脚本，当前路径配置不可直接运行。
- `scripts/validation/Test-SourcePool.ps1`：参数化的资料池只读基线检查；在合法更新清单后，需要同步审计并更新基线常量。
- `scripts/validation/Validate-ResearchData.ps1`：根据字段和枚举注册表检查 32 张表的结构、引用、本地 endpoint 哈希、证据状态与来源数量、最小集成员和派生指标；派生指标的三项检查状态使用正式枚举值 `passed`。

执行目录已经建立；尚未创建或冻结的数据文件，以 `研究计划.md` 中的“成果与目录”及后续数据模型决策为准。执行阶段不得在 `论文/`、缓存、临时目录或下载中间目录内创建项目级 `AGENTS.md`。

## 长期任务状态

长期任务不能把聊天记录当作唯一记忆。恢复工作时依次阅读 `AGENTS.md`、`研究计划.md`、`进度/当前状态.md`、`进度/任务台账.csv`，再读取相关里程碑、决策记录和子代理交接。

主代理在任务开始时把台账状态改为 `in_progress`；只有产物存在、验证完成且交接通过验收后，才改为 `completed`。字段或范围冻结、试填卡验收、厂商批次完成、最小来源集生成和最终交付都属于必须写入项目记录的任务节点。每个节点至少更新当前状态和任务台账；改变范围、口径、数据模型或工作方法时，同时更新决策记录；跨越阶段门时更新里程碑。

子代理不得直接修改 `进度/`。子任务先在 `审计/子代理交接/` 写明任务状态、输入、已完成事项、验证、未解决问题、建议下一步和全部写入文件，主代理复核后统一更新全局状态。

## 证据与数据约定

每条关键规格拆成可独立核验的原子事实，至少包含产品对象、字段、原值、单位、条件、对象层级、来源标识和原文定位。资料卡中的关键字段必须能回到页码、表格、章节、网页标题或其他稳定定位。

来源强弱按它对具体事实的支持能力判断。固定版数据手册、架构白皮书、官方开发文档和原始论文优先；独立微基准、拆解和可复现实测用于补充或验证。媒体资料若只是复述更强来源，不进入最小参考资料集；如果提供独有实测、直接采访、产品状态或矛盾证据，可以保留。

原始资料不因筛选而删除。被判为重复、过时、低相关或只作线索的来源保留筛选记录，并注明被哪些来源覆盖或为何不进入正式参考资料。

最小来源集必须保存可复现的反向移除记录。已复核的 selection run 要列出范围、算法版本、成员、入选角色和不可替代理由；事实集合变化后重新运行，不能沿用旧结论。直接事实的 `evidence_state` 按不同 `source_id` 数量计算，同一来源的镜像、不同 endpoint 或重复断言不增加来源数。

同一指标不得混用以下口径：

- 芯片、裸片、封装、模组、加速卡、服务器、机架和集群；
- 稠密和稀疏、理论峰值和持续实测；
- 输入精度、乘积精度、累加精度和输出精度；
- 单向、双向、线路聚合、有效载荷和系统总带宽；
- GB 与 GiB、十进制与二进制单位。

未知数值不得写成零。使用 `not_public`、`not_found`、`not_applicable`、`conflicting_unresolved` 或 `pending_verification` 等正式状态。只有一手资料明确说明未公开时才用 `not_public`；完成计划检索仍无可靠结果时用 `not_found`。根据公开数据计算的结果必须保留原始输入、公式、单位换算和适用范围。

## 多代理协作约定

主代理负责字段字典、全局标识、共享数据表、来源筛选规则、合并和最终验收。子代理按厂商或独立审查任务分工，只写各自的厂商目录或交接文件，不并发修改全局 CSV、索引、README、AGENTS 或研究计划。

每个子代理交付时必须列出：任务状态和输入，处理的型号与对象层级，读取的来源，已完成事项与验证，全部写入文件，尚未核实的字段，来源矛盾，被筛除资料及理由，以及建议的下一步。未经主代理复核的资料卡标为草稿，不能进入完成清单；子代理自行报告完成也不能直接改变全局台账状态。

同一厂商的高风险资料卡应由不同于初稿作者的代理复核。主代理在合并前检查对象层级、精度、单位、来源定位和事实状态；跨厂商结论仍留到后续独立阶段。

M2 子代理只写自己的 `审计/子代理交接/m2_staging/<work_package>/`。卡片草稿和结构化片段在总控验收前不进入正式厂商目录或全局 CSV；对象 ID、共享来源 ID 和关系由总控预留并顺序合并。

## 运行与检查

本目录目前以 Markdown、CSV 和本地 PDF 为主。修改后至少执行以下只读检查：

~~~powershell
Get-ChildItem -LiteralPath 'D:\Workspace\2026projects\调研-训练推理芯片调研-资料汇总' -Force
Get-ChildItem -LiteralPath 'D:\Workspace\2026projects\调研-训练推理芯片调研-资料汇总' -Recurse -File
rg -n --glob '*.md' '\\\[|\\\]|\\\(|\\\)' 'D:\Workspace\2026projects\调研-训练推理芯片调研-资料汇总'
~~~

修改 `论文/` 或四个清单前后，先运行：

~~~powershell
& 'C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe' -NoProfile -ExecutionPolicy Bypass -File '.\scripts\validation\Test-SourcePool.ps1' -RootPath '.'
~~~

该脚本默认只读。基线发生有意变化时，先审计差异，再更新脚本中的预期数量和哈希。结构化数据修改后运行：

~~~powershell
& 'C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe' -NoProfile -ExecutionPolicy Bypass -File '.\scripts\validation\Validate-ResearchData.ps1' -RootPath '.'
~~~

新增 CSV 后检查表头、唯一标识、外键、空值状态和 UTF-8 编码。新增或调整计算字段后逐项复算，不以抽样代替关键数字核对。新增本地资料后记录 SHA-256、页数、来源 URL、获取日期和合法公开入口。

中文研究文档交付前运行：

~~~powershell
& 'C:\Users\Huashuo\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' 'C:\Users\Huashuo\.codex\skills\report-humanizer\scripts\scan_report_ai_tells.py' <file.md>
~~~

机器扫描后还要人工检查标题、首段、表格引导、转场和结尾。第一次出现的缩写和专有名词给出简洁解释；Markdown 行内公式使用 `$...$`，独立公式使用单独成行的 `$$`。

## 文件修改规则

- 当前项目根目录固定为 `D:\Workspace\2026projects\调研-训练推理芯片调研-资料汇总`。
- 使用 Windows PowerShell 和显式绝对路径；含中文或空格的路径使用 `-LiteralPath`。
- 写入文本时显式使用 UTF-8，并在每次修改后立即验证文件存在、内容和编码。
- 不改动两个历史调研目录，除非用户以后明确扩大写入范围。
- 每次修改非生成文件后检查 `README.md` 和本文件。项目目标、结构、进度、运行方式、证据规则或已知问题变化时同步更新。
- 不在文档中记录密码、令牌或其他敏感信息。

