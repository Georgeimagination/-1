# 当前状态

更新日期：2026 年 8 月 13 日  
当前阶段：M2 裸片、封装、SKU、云配置与系统资料卡  
总控：主代理  
总体状态：进行中

## 当前结论

M1 三类试填、M2 高风险对象范围门和第一波 29 张七厂商架构卡均已通过。第二波首个正式成果是 AMD Helios 72-MI455X 参考机架观察卡；它已经过初稿、修正、独立复核和总控验收。连同 M1 三张试填卡和新验收的三张 Trainium2 实例卡，资料卡索引现有 36 张已验收卡片。

当前仍不分析训练导向与推理导向芯片的架构差异。工艺、容量、带宽、功耗、频率、单芯片总峰值和系统配置只写入相应实现对象，不能回填成无条件的架构代际事实。

## 当前基线

资料池现有 111 份内容去重的 PDF，共 313,921,821 字节、2,210 页；网页清单 914 行，按大小写敏感字符串统计为 893 个入口，忽略大小写为 892 个入口。PDF 覆盖 NVIDIA 65 份、Google TPU 25 份、华为昇腾 15 份、Groq LPU 2 份和 AMD Instinct 4 份；AWS 与寒武纪仍没有本地 PDF。AMD Helios 和 MI400 两个正式页面已经保存 2026-08-13 HTML 快照。

正式库现有 78 个对象、27 条关系、621 条事实、611 条逐来源断言和 792 条字段要求。来源筛选有 68 个 `selected`、10 个 `redundant_covered`、2 个 `pending`、3 个 `out_of_scope`、2 个 `rejected_unreliable`、2 个 `inaccessible` 和 1 个 `lead_only`。正式库有 7 次最小来源选择运行、88 个成员；Trainium2 纠错新增的 2 次运行和 23 个成员已按独立签字的显式主键清单完成复核状态迁移。现在 6 次运行为 `reviewed`、1 次为 `superseded`，88 个成员均为 `reviewed`。

事实字段现有 120 个。`byte/s` 的读写带宽与 `byte/cycle` 的每周期读写传输量分开记录；加速器复合芯粒（XCD）、计算单元（CU）或通道作用域写入条件集。最新正式数据校验通过 93,251 项检查。资料池校验核对 111/111 份 PDF 的路径、大小、SHA-256 和页数并通过，保留一条既有 Google TPU PDF 解析警告。

## 已正式合并的工作包

| 工作包 | 正式结果 |
|---|---|
| `M2-GHC-ARCH` | 4 张架构卡、35 条事实、35 条断言、80 条字段要求、7 个最小来源成员和 46 条实现对象待办。 |
| `M2-GA-ARCH` | 14 张架构卡、141 条事实、141 条断言、72 条字段要求、19 个最小来源成员和 65 条实现对象待办。 |
| `M2-NA-ARCH` | 11 张架构卡、185 条事实、185 条断言、369 条字段要求、17 个最小来源成员和 9 条实现对象待办。 |
| `M2-W2-AMD-HELIOS-SYSTEM` | 1 张系统观察卡、18 条事实、22 条断言、17 条字段要求和 3 个最小来源成员。 |
| `M2-W2-TRN2-CORRECTION` | 修正 M1 Trainium2 事实链并新增 3 张实例卡；删除 11 条错误事实链，重建 41 条实例事实映射。 |

Helios 包正式新增 102 行结构化记录。其中 97 行依据独立清单把 `review_status` 从 `draft` 改为 `reviewed`，与 3 AI exaflops 有关的 5 行仍为 `needs_resolution`。验收见 `审计/M2_Helios_参考机架包正式合并验收.md`。

Hopper 和 Trainium2 的架构卡继续通过正式关系复用 M1 事实，没有创建第二套原子事实。MLUarch05 保留低披露边界。TPU 8t、TPU 8i、Trainium4、Rubin 和 CDNA 6 仍是观察对象，不能按完整量产架构解释。

## 正在进行的工作

AWS 四代物理对象事实包在首轮独立复核后完成 B01 至 B09 修正，修正版已经冻结并交给另一位代理做最终复核。当前候选有 57 条事实、69 条逐来源断言和 71 条字段要求，最小来源集从九源收敛到八源；包内相对、绝对两种路径均通过 9,289 项检查，在当前正式基线上的临时合并通过 101,048 项检查。正式库尚未写入；最终复核还要明确 `FIELD-PHY-CLOCK` 是否允许 component 主体，并逐主键核对 457 行生命周期清单。

Trainium2 三实例轻量包曾被独立复核拒绝。复核发现 S10 不能支撑 10 条 BF16、FP16、TF32 或 trn2u 历史稀疏事实，S06 也不能支撑 trn2.3xlarge 的芯片数量；网络带宽还混淆了实例注入、系统聚合与单链路口径。纠错包随后完成全部删除和字段修复，由未参与修复的代理给出 `accept` 并签署 17 行生命周期清单。正式库现只保留两条 S10 FP8 历史断言，错误的 UltraServer 3.2 Tbps 主体链已删除；三张实例卡和修正后的 M1 Trainium2 卡均已进入正式目录。验收见 `审计/M2_Trainium2_纠错包正式合并验收.md`。

Google TPU 的云端单器件、VM 或机器类型、slice/Pod 三层模型已经通过。v5e、v5p、v6e 三个云端单器件对象和三条架构关系也已补裁决并预留；正式范围映射现有 63 行。来源准备包的独立复核为 `accept_with_caveat`：G15 固定件缺少承担证据的前 148 行，`tpu-machines` 又被误标为器件范围之外。新的修正版只处理来源门，事实和资料卡尚未启动。

## 已冻结的延期项

A800 数据中心候选、H800 和 H20 要先拆成固定物理 SKU。LP30、MI440X、MI430X 和 MI500 Series 的对象层级还未确定；H20 BFX 存在官方架构归类冲突；Trainium4 未来系统没有独立产品身份。寒武纪 BANG v5.0、MLU570 和 MLU370-M8 仍受 CNToolkit 正文不可访问限制。只有取得范围验收文件列明的解除证据后，这些候选才能进入正式抽取。

华为 Atlas 950 保持一个 SuperPoD 对象，64、1024、8192 NPU 只作为带条件和日期的配置。CloudMatrix384 是硬件超节点，其上的云服务另建候选。TPU 8t/8i 不制造尚未出现的 accelerator type 或 machine type。

## 生命周期与合并纪律

四张核心表此前有 1,924 行按显式主键清单完成 `draft`→`reviewed` 迁移；事实语义状态、来源筛选结论和其他列均未改变。Helios 包另有 97 行按独立清单完成同类迁移。Trainium2 纠错包后置迁移另改动 25 个主键中的 27 个状态单元格，只涉及 2 次选择运行的 `status / review_status` 和 23 个成员的 `review_status`。`reviewed` 只表示行级复核完成，不等于把 `provisional`、`needs_resolution` 或 `pending_verification` 批准为最终结论。

所有第二波工作包继续使用 staging、独立复核、总控验收和顺序合并。子包不能并发写正式 CSV；正式库发生有意合并后，仍在运行的复核必须重新建立哈希基线，不能把并发变化误判成自身副作用。

## 已知风险

`scripts/collect_references.py` 仍使用旧 macOS 绝对路径，不能在当前 Windows 工作区直接重跑。资料池的平台分布不均；动态产品页、云配置和生产爬坡状态需要保存截止日快照。寒武纪 CNToolkit 与 CNNL 页面返回远端 HTTP 401，这是远端服务限制，不是沙箱或审批问题。

字段主体合同预审另发现 132 行不一致：8 条事实和 124 条字段要求的实际主体类型未列入对应字段的 `allowed_subject_kinds`，涉及 26 个字段、28 组组合。当前校验器没有检查这项合同。明细已写入 `审计/字段主体合同全库预审.csv`；修复前按字段语义逐组裁决，不能批量扩合同或搬移主体。

`M2-GA-ARCH` 的 `.validation-temp-R1` 清理曾被自动审批复核的连接或用量上限拦截；这不是用户拒绝，也不是沙箱拒绝。目录包含指向正式 `论文/` 的 Windows 目录联接，只能按核对过的精确路径处理，不能递归跟随。四份 AMD ISA PDF 的固定下载也曾因自动复核用量耗尽被拒，正式库保留 AMD 官方在线入口。

GHC 合并备份仍有两份内容相同的哈希清单；删除其中一份的动作曾被自动审批用量上限拦截。正式恢复依据使用 `backup-hashes-v2.csv`，重复清单不影响验收。

## 下一停点

先完成 AWS 物理对象事实修正版的最终独立复核；同时修正 Google TPU 云端单器件来源门。AWS 包只有在字段合同、八源最小集、生命周期清单和临时正式合并全部通过后才可写入；Google 包须关闭 G15 部分提取、`tpu-machines` 作用域和反向移除问题，渲染文本不得冒充上游原始网页快照。

## 恢复工作时先读

1. `AGENTS.md`
2. `研究计划.md`
3. `进度/当前状态.md`
4. `进度/任务台账.csv`
5. `审计/M2_对象范围验收.md` 与 `审计/M2_第二波_AWS物理对象范围补充验收.md`
6. 三份第一波架构包验收、`审计/M2_第一波收尾一致性复核.md` 与 `审计/M2_Helios_参考机架包正式合并验收.md`
7. `审计/子代理交接/m2_review_aws_package_facts.md`、`审计/子代理交接/m2_review_trn2_instance.md` 和对应 correction staging
8. 三份实现对象待办和 Google TPU 三层模型独立复核
