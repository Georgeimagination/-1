# 字段主体合同独立审计进度

截至 2026 年 8 月 13 日，本子任务已完成。预审清单的 132 个主键全部裁决：`facts.csv` 8 行、`field-requirements.csv` 124 行，覆盖 26 个字段和 28 组“字段、实际主体、登记主体”组合。动作分布为 `expand_contract` 16 行、`retarget_row` 8 行、`no_change_with_rationale` 108 行。正式表、资料卡、现有暂存目录（staging）、根 README、AGENTS、研究计划和进度目录均未修改。

`FIELD-PHY-CLOCK` 已分层裁决。四条 Trainium2 组件时钟事实的断言均回到 S02 的 Compute Engine Specifications：Tensor、Vector、Scalar 和 GpSimd 的来源主语就是计算引擎，数值分别为 2.4、0.96、1.2 和 1.2 GHz。事实合同从 `object` 局部扩为 `object;component` 有直接依据，也能承接 AWS（Amazon Web Services）Trainium3 Tensor Engine 的 2.4 GHz 候选。三条 H100 `not_found` 要求是在问 BF16、FP16 和 INT32 非 Tensor precision path 是否绑定明确峰值频率；它们是路径级缺口，不构成允许路径级时钟事实的证据。因此，要求合同写 `object;component;precision_path`，事实合同不加入 `precision_path`。

124 条要求的正式状态为：`not_applicable` 60 条、`not_found` 53 条、`value_available` 8 条、`inaccessible_evidence` 2 条、`conflicting_unresolved` 1 条。60 条不适用项由 object→component 28、object→precision_path 16、object→link 15、object→topology 1 组成。缺失和不适用描述的是检索或适用范围，不能据此倒推出未来事实主体。

三个 CSV 已由 `Build-Adjudication.ps1` 生成并复算。当前结果为 132 个全局唯一主键恰好一次、28 组全覆盖、26 个字段候选、8 条 row overlay；候选应用后的事实合同与要求合同不匹配均为 0。正式校验器只读运行仍通过 93,251 项检查，但源码没有检查 `allowed_subject_kinds`，所以不能据此宣称全库闭合。完整裁决和过渡方案见同目录 `README.md` 与 `validator-audit-design.md`。