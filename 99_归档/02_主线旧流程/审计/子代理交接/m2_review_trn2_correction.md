# Trainium2 修复包独立复核

## 当前状态

复核完成。裁决为 `accept`；在冻结包哈希仍匹配且严格保留本文生命周期边界的前提下，允许总控合并。复核对象为 `审计/子代理交接/m2_staging/M2-W2-TRN2-CORRECTION/`；本报告不修改修复包或正式库。

## 输入与边界

已完整读取根目录 `AGENTS.md`、`研究计划.md`、字段字典与模板、DEC-014、DEC-015、DEC-020、DEC-025、DEC-027、原驳回报告 `m2_review_trn2_instance.md`，以及修复包的 README、handoff、196 行操作清单、17 行生命周期清单、24 份覆盖表、五张卡、S07 说明、来源筛选说明、应用脚本、回滚方案和现有验证记录。正式库只用于只读基线核对；脚本复现将在隔离副本中执行。

## 审查清单

| 编号 | 审查事项 | 当前状态 |
|---|---|---|
| R1 | S10 仅保留芯片 FP8 与 `trn2.48xlarge` FP8，十条污染链无残留 | 通过静态模拟与原文核对 |
| R2 | `trn2.3xlarge` 芯片数来源由 S06 改为 S07 | 通过静态模拟与原文核对 |
| R3 | 三实例注入带宽、UltraServer 聚合带宽及错误 3.2 链删除 | 通过静态模拟与原文核对 |
| R4 | 41 条卡片事实映射、来源分布、39 条可采事实的四源最小集、S15 审计边界 | 通过；选择运行和成员按包内 `draft` 状态合并 |
| R5 | S07 来源、入口、日期、路径、nonce 等价和哈希 | 通过 |
| R6 | 188 个表操作、196 行操作清单、8 个文件及五张卡的一致性；主键基线与追加碰撞 | 通过 |
| R7 | 17 行生命周期迁移只改行级复核状态 | 通过；已另附独立签字 |
| R8 | 相对根与绝对根两种调用、隔离副本、正式表哈希、正式校验器 | 通过 |
| R9 | 临时目录和 junction 清理 | 通过 |
| R10 | 关键 `locator` 与 `quoted_context` 的逐源语义支持 | S06、S07、S09、S10、S15 关键原文已核 |

## 已完成事项与验证

### 修复后状态的静态模拟

本代理不运行包内脚本、不写正式库，而是在内存中按 `delete → update → append` 顺序重放 24 份覆盖表。模拟结果为 621 条事实、611 条断言、792 条字段要求、141 个条件集、15 个冲突组和 31 个冲突成员，与修复作者记录一致。

S10 在模拟后的断言表中恰好剩两行：芯片 FP8 稀疏 5.2 PFLOPS 与 `trn2.48xlarge` FP8 稀疏 83.2 PFLOPS。十条不受 S10 直接支持的 BF16、FP16、TF32 或 `trn2u.48xlarge` 历史稀疏事实，以及它们的断言、字段要求、冲突组、冲突成员和条件集均无主键或文本残留。错误的 `FACT-AWS-TRN2-ULTRA64-EFA-3P2` 完整链也无残留。

`FACT-AWS-TRN2-3XL-CHIP-QTY` 的唯一断言已由 S06 换为 S07，原值仍为 1。四条网络事实分别规范化为 25,000,000,000、400,000,000,000、400,000,000,000 和 1,600,000,000,000 byte/s；前三条使用 `FIELD-INT-INJECTION-BW`，UltraServer 12.8 Tbps 使用 `FIELD-INT-AGGREGATE-BW`。

### 操作清单与主键

`operation-manifest.csv` 有连续且唯一的 1 至 196 序号：78 个删除、45 个更新、65 个追加、6 个新建文件和 2 个替换文件。其中 188 个表操作与 24 份覆盖表逐行同构，覆盖表表头与正式表一致；每个删除和更新主键在基线中恰好命中一次，所有追加主键均不碰撞。当前未发现机械合并错误。

### 卡片事实与最小来源集

`card-fact-map.csv` 有 41 个唯一映射和 41 个唯一事实，三张实例卡分别为 4、20、17 条；来源分布为 S06 32 条、S07 5 条、S09 1 条、S10 1 条、S15 2 条。S15 两行仍是 `rejected_conflict_evidence`、`needs_resolution`，只保存 512 GiB 和 8,192 GiB 的不可靠声明及冲突审计，不计入正向覆盖分母。余下 39 条可采事实中，S06、S07、S09、S10 各有唯一贡献；删除任一来源都会失去至少一条事实，所以四源集合在该明确范围内满足反向移除最小性。

新建的实例选择运行、重跑的 M1 选择运行及 23 个成员目前全部是 `draft`。本复核确认其范围和反向移除计算，但不把它们表述为已经正式晋级的 `reviewed` 运行。本次 `accept` 明确允许这些行按草稿状态合并。

### S07 与逐源语义

两份 S07 快照均为 487,377 bytes、3,241 行。2026-08-12 原始哈希为 `4c315ad5c342d10e1e0a6a8a5ab59b87be94e9fc19911f6ac122964317aa32cf`，2026-08-13 原始哈希为 `94ceea74234b30d6568127badd6a4df68a46b861d1d989c21463fea3b0690edb`。逐行比较仅第 4、70 行两处 nonce 不同；分别把 CSP 中 `nonce-...` 的值和 JSON 的 `"nonce":"..."` 值替换为 `<normalized>` 后，两份全文相同，归一化 SHA-256 均为 `3070baef5d94db45396bd210d4f16f1bf8f8aa1b2586d1215530f82e2acf473e`。因此保留 S07 的来源版本标签和内容指纹，只更新核验日期并新增 2026-08-13 endpoint，符合版本规则。新 endpoint 的日期、正式目标路径、字节数和原始哈希互相一致；其 `review_status=draft` 没有冒充生命周期晋级。

关键定位已回看固定原文，而非只依赖校验器。S06 的规格表直接支持两种 48xlarge 实例的当前计算、容量、内存带宽、实例内互联与拓扑映射；S07 的 Product details 表分别把 3.2 Tbps 列在 `trn2.48xlarge` 与 `trn2u.48xlarge` 两个实例行，并支持 1/16/16 个芯片、96 GB/1.5 TB 内存和 0.2/3.2/3.2 Tbps 实例网络；同页正文另明确 UltraServer 为 12.8 Tbps，不能把实例数值再挂到 UltraServer 或解释为单链路；S09 支持 2024-12-03 正式可用状态；S10 只直接支持芯片 FP8 与 16 芯片 Trn2 实例 FP8 两条历史稀疏数值；S15 的 512 GiB 与 8,192 GiB 仅作为待决质量审计。个别旧断言的 `quoted_context` 为空，但其 locator 能稳定落到上述固定标题、表行或正文；本包新增或改写的 S07 关键断言均同时提供表行/列定位和简短原文上下文。

### 包指纹、隔离复现与正式库保护

冻结包当前有 49 个文件。独立聚合口径是：以包根为基准取得使用 `/` 的相对路径，按 `StringComparer.Ordinal` 排序；每行依次写相对路径、TAB、文件字节数、TAB、小写文件 SHA-256，使用 LF 换行并保留末尾 LF；对整个 UTF-8 无 BOM 文本再算 SHA-256。复算结果为 `1dc2d1923146667b6798805585211981cd86ed4196d7fa80f7d490b7b950204f`，首行是 `README.md<TAB>3032<TAB>d870e8...11e6`，末行是 `validation/validation-report.md<TAB>3209<TAB>86efe7...e488`。

应用脚本只对两个隔离副本执行。第一次从项目根使用隔离目标的相对 `TargetRoot`，第二次从项目外目录使用脚本和隔离目标的绝对路径；两次都成功，32 张结果表的哈希逐表相同。两个副本均得到 621 条事实、611 条断言、792 条字段要求，官方校验器均通过 93,131 项检查。应用前正式库的 32 张表与包内基线逐表一致，正式校验器新鲜通过 93,799 项检查；隔离复现完成后再次复核，正式 32 表仍无哈希变化。

两个隔离目录创建时和应用后都没有 reparse point（重解析点，包括 junction）。复现结束后按已验证的绝对路径删除，再查没有临时目录或 junction 残留。

### 生命周期分层

17 行生命周期清单包括 8 条事实、8 条字段要求和 1 条新增断言。八条事实从假冲突中解脱后，`resolution_state=provisional`、`evidence_state=single_source` 是修复本身的语义变化；八条要求恢复为 `value_available` 也是修复本身的语义变化。把这些语义修复先视为已落地的中间态后，生命周期动作本身只把 16 个既有行的 `review_status` 从 `needs_resolution` 改为 `reviewed`，并把新 S07 断言的行级状态定为 `reviewed`；没有再改事实值、来源或语义状态。相应的独立签字已经另行落盘。

### 总控合并后的复查

上述独立裁决和生命周期签字落盘时，正式 32 表仍与修复包基线一致。总控收到 `accept` 后约于 2026-08-13 05:06 执行正式合并，并先建立 `审计/合并备份/M2-W2-TRN2-CORRECTION-20260813/`；这次正式库写入不是本复核代理执行的。

合并完成后，本代理又做了一轮只读复查。备份中的 15 张受影响表逐表匹配包内基线哈希；再以内存方式对备份重放 overlay，与当前正式表按行序和单元格比较，共检查 49,469 个单元格，差异为 0。8 个复制目标也全部与包内源文件哈希相同。17 个生命周期主键均达到签字状态；两个修正后的选择运行和 23 个成员仍为 `draft`，新 S07 endpoint 也仍为 `draft`。正式校验器新鲜通过 93,131 项检查。

因此，报告前文“正式 32 表没有变化”只描述隔离复现结束、总控尚未合并时的保护检查。当前正式库已经发生经过授权的 15 表和 8 文件变更，并与已接受包一致。

### 工具异常和成稿检查

复核时有若干辅助只读命令因 PowerShell 语法构造或本机 .NET 方法版本不兼容而失败。这些均属模型/操作命令错误，不是沙箱拒绝、审批失败或远端服务错误；失败命令没有修改包或正式库，改正后相关检查均已重跑通过。

中文报告和生命周期签字均已运行 `report-humanizer` 机器扫描，未发现可机器识别的套话。随后按 `shuorenhua` 做人工保真检查，保留了所有 ID、路径、日期、数值、单位、哈希、枚举状态和责任边界；没有把 `draft` 改写成 `reviewed`，也没有改动引用原文。

## 边界与执行条件

本包的两条新选择运行、23 个选择成员和新 S07 endpoint 仍是 `draft`。`accept` 只表示修复内容、四源反向移除计算和合并机械性已经通过独立复核，不表示这些行已经完成正式生命周期晋级。总控应按包内原值合并；后续若要把选择运行或成员改为 `reviewed`，必须另列显式主键清单并再次签字。

## 最终裁决

裁决：`accept`。

该裁决绑定 49 文件冻结包聚合 SHA-256 `1dc2d1923146667b6798805585211981cd86ed4196d7fa80f7d490b7b950204f`。只要总控合并前复算仍相同，即允许按 196 行操作清单合并，并按独立生命周期签字应用其中 17 行状态结果。若包内容或聚合哈希变化，本裁决自动失效。

独立生命周期签字为 `审计/子代理交接/m2_review_trn2_correction_lifecycle_signoff.md`，当前 SHA-256 为 `197222789c4e11d42ab1d7db6b72e22ac03a19a39237a0735a41a4a6e70a7daa`。

项目级 `README.md` 和 `AGENTS.md` 已检查。本代理受“只写复核报告与生命周期签字”的边界约束，没有修改它们；总控已执行正式合并，应由总控把本次验收和当前计数同步到项目状态文档。

## 本代理写入文件

- `审计/子代理交接/m2_review_trn2_correction.md`
- `审计/子代理交接/m2_review_trn2_correction_lifecycle_signoff.md`