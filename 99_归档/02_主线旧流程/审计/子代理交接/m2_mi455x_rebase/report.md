# MI455X 模组包新基线重签报告

> 裁决：`accept`  
> 日期：2026-08-13  
> 对象：`M2-W3-AMD-MI455X-MODULE`  
> 新精确签字：`审计/子代理交接/m2_mi455x_rebase/m2_mi455x_rebase_signoff.csv`  
> 写入边界：本目录；正式 32 表、正式资料卡、论文和网页快照均未写入

本文中的 PK 是 primary key，即结构化表的主键；CSV 是逗号分隔值文件；SHA-256 是 256 位安全散列算法。AWS 指 Amazon Web Services。HTML 是超文本标记语言页面，PDF 是便携式文档格式，endpoint 指来源内容的具体访问入口或本地副本。`gate` 是项目校验器的硬门模式。

## 裁决

MI455X 模组包在 AWS 四代物理对象事实包正式合并后的基线上仍可接收。原暂存包的对象边界、39 条直接事实、53 条断言、44 条字段要求、四来源最小集和 3 个目标文件没有发生实质变化。新签字只重新绑定当前正式基线、当前临时合并校验数和本次独立复核者，未改事实值、条件、来源定位、要求状态、卡片正文或目标路径。

本次 `accept` 允许总控使用新签字准备正式事务，不表示本任务已经正式合并。正式写入仍由总控顺序执行，并须在执行前再次核对新签字、冻结包、当前基线和三个目标文件门。

## 当前基线与写集交集

当前正式库在 `gate` 模式通过 32 张表和 106,160 项检查，注册表为 324 列、488 个枚举值。按事务使用的 `relative_path|sha256`、UTF-8 和 LF 连接算法，32 表聚合值为 `f1520907d54e406e7d82f591e3799f07f569218aff0c321ac3b836b6c649da10`。当前基线清单 JSON 的 SHA-256 为 `7ff0be6c7c72f7aba85fe584a14ef9df11f72e17c84a40cb541e1c6b2933e4b1`，大小 8,965 字节。

旧 MI455X 基线与当前基线逐文件比较后，有 20 张表变化、12 张表不变。变化来自已经完成的正式工作，不能只凭整表哈希判断 MI455X 冲突。因此本次把 AWS 授权写集和 MI455X 精确写集按 `target_path|pk_value` 重新求交：AWS 有 477 项授权写入，MI455X 有 242 个结构化主键，交集为 0。当前正式表中也没有任何一个 MI455X 待写主键。`pk-conflict-audit.csv` 的 242 行全部为 `no_conflict`；其中 240 个主键位于整表哈希已变化的表，2 个位于未变化的表。

三个非结构化目标也逐一复核。正式 MI455X 资料卡、固定 HTML 快照和固定 PDF 目前都不存在；三份暂存来源的大小与哈希仍和旧签字一致；它们与 AWS 文件目标的交集为 0。`target-file-guard-audit.csv` 的三行全部为 `guard_pass`。

## 精确签字的变化

新签字有 245 行、25 列，其中 242 行绑定结构化主键，另外 3 行绑定资料卡、HTML 和 PDF。文件大小为 209,683 字节，SHA-256 为 `196863031761221d825309387c563553187b0ee23630002aeaeed57bbbd6084c`。

旧、新签字共审计 6,125 个单元格。4,900 个单元格未变，1,225 个发生预期变化，没有意外变化。变化分成两类：

| 变化类型 | 单元格数 | 涉及列 | 原因 |
|---|---:|---|---|
| 基线重绑 | 980 | `formal_baseline_manifest_sha256`、`formal_baseline_manifest_bytes`、`formal_baseline_aggregate_sha256`、`temporary_merge_validator_checks` | 245 行分别绑定当前基线清单、`f152...` 聚合值和 111,040 项临时合并检查 |
| 独立复核者 | 245 | `reviewer` | 复核者改为 `m2_mi455x_rebase` |

逐单元格结果保存在 `old-to-rebase-signoff-cell-audit.csv`，其中 1,225 项变化另存于 `old-to-rebase-signoff-cell-diff.csv`。`rebase-signoff-validation.json` 已核对 245 个唯一签字行、242 个唯一结构化目标键、3 个文件动作、0 个 PK 冲突、0 个 AWS 交集和 0 个意外单元格变化，结果为 `passed`。

## 新基线临时合并

临时合并从不存在的 `.temporary-merge` 目录开始。脚本复制当前正式 32 表，把新签字中的 242 行按主键追加并执行签字规定的生命周期变化，再复制 3 个目标文件。为让正式校验器核对既有本地入口，临时镜像还复制了 69 份已经登记的本地 endpoint 文件；这些文件没有写入正式目录。

临时镜像在 `gate` 模式通过 111,040 项检查，注册表仍为 324 列、488 个枚举值。临时合并后的 32 表聚合值为 `0d94c60e136fab58941ad8d1f6b5b55a4d2013a370c9a1dc938bd8005fa57bca`。正式库在运行前后均为 `f1520907d54e406e7d82f591e3799f07f569218aff0c321ac3b836b6c649da10`，临时目录已删除。新鲜通过证据是 `temporary-merge-fresh-pass.json` 和 `temporary-validator-output.txt`。

## 冻结范围

原暂存包的 42 项冻结成员继续逐文件匹配，原冻结清单 SHA-256 为 `56ca298d6c9b327c3b5ef2babf30a0f5dc109b519381083e5717452a01b1331b`，成员聚合值为 `dfed75716edaff380b35f4b356f4a631a8ce17413a8e339ac2b77448cb94b0a0`。

新的 `rebase-freeze-manifest.csv` 冻结旧终审、旧精确签字、AWS 写集证据、42 个原暂存成员和本次重签证据，共 61 行。清单 SHA-256 为 `d3156e2f08b72eca66c3146a423e60264f10aa6eaa3229b684789c4b87640802`；按 `relative_path|sha256|bytes` 排序并用 UTF-8、LF 连接后的成员聚合值为 `e8fc9691f1da8c6ab0fe270782b7aefbb79a49f7d3e9e33fedb12d3eb103499e`。独立复读结果为路径重复 0、清单主键重复 0、文件缺失或哈希和大小不匹配 0。

## 执行错误与任务边界

执行中出现的失败均归为模型操作或脚本实现错误。影响临时合并的两次失败分别是 Windows PowerShell 5 对无 BOM UTF-8 脚本中的中文路径误解码，以及泛型 `List[object]` 与数组直接相加时报 `Argument types do not match`。两次失败都没有写正式库，临时目录均被清理；修正后从不存在的临时根完成了上述 111,040 项新鲜通过。失败和修正记录在 `temporary-merge-attempt-history.csv` 与 `operator-error-log.csv`。

冻结清单生成时，第一次重复路径统计把 `-gt 1` 误写为 `-gt1`。清单文件随后被独立重新读取，61 行的路径、主键、哈希和大小全部通过，因此该操作错误没有改变冻结结果。本任务没有沙箱拒绝、用户拒绝、自动审批拒绝、审批连接失败或远端服务错误。

正式库当前仍是 AWS 合并后的 `f152...` 基线。总控若继续正式事务，应先核对新签字 SHA-256 `196863...6084c`、61 行冻结清单、当前基线聚合值和三个目标不存在条件；任一项变化都应停止沿用本次签字并重新复核。