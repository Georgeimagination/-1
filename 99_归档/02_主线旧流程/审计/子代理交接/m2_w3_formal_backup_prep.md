# M2 第三波组合正式事务外置备份与授权标记准备

## 裁决

状态为 `blocked_auto_review_trusted_user_context_unavailable`，`ready_for_formal_execution_backup=false`。

总控转述用户已经明确授权以下范围：向 20 张正式 CSV 写入 1,057 个已签字主键，新增 5 张资料卡和 10 份官方网页快照，共 15 个文件；发生故障时使用 24 项外置备份按原子事务合同逆序回滚。本子任务只负责生成授权标记，不运行正式事务。

我把这段精确范围写入外置授权语境记录，并更新了 7 项授权输入清单。随后申请创建执行器要求的六字段 `M2W3-BUNDLE-FORMAL-TRANSACTION-AUTHORIZATION.txt`。自动审批系统拒绝了该动作：它能确认命令只写标记，但认为标记会解除 20 表、1,057 行和 15 文件事务的执行门；本子代理的受信任用户内容里没有逐字授权，只有总控的任务转述。按审批要求，我没有重试、换文件名或采用其他方式绕过。

授权标记仍不存在，正式事务没有运行。正式 32 表、资料卡、PDF 和网页快照的写入次数均为 0。

## 外置目录与冻结输入

BackupRoot 是正式项目根之外的专用备份目录：

`D:\Workspace\2026projects\m2w3-bundle-formal-backup-20260813-f152`

该目录与正式项目根不同，既不是正式根的子目录，也不是正式根的父目录。

组合准备包 `manifest.csv` 的 SHA-256 为 `efdb763a24c0e97fd483735bba5d68dd01f0768553f963988436efa933ebbaba`；75 个成员逐项匹配。按 `relative_path|sha256|bytes`、UTF-8 和 LF 复算的包聚合值为 `958e1b5106b6c42ae116fe643b3b8ec63a190cc20951654380d86c45a2ad332f`。

独立复核签字的 SHA-256 为 `2291a5c77c3b4fef6a94488a4f3b76cbf1df092230a279292a7246d03efa6e5c`。它绑定令牌摘要 `7c2902c416f246f9f13e0462599d87f12805e6387de21810df20c140153a7a3a`、f152 基线和冻结候选。独立复核清单的 SHA-256 为 `6c1340ef2a8ba746d5c54d2c34638c3563e89a8c35a0ac905924cf98db8c30fa`。

## 外置备份与授权输入

正式备份清单有 24 个唯一条目：20 张待改表、2 张只读保护表、1 份 15 目标不存在清单和 1 份候选 20 表后哈希清单。24 个文件的哈希和字节数全部匹配，差异数为 0。备份清单 SHA-256 为：

`cff6755cbdd5b24ee4cda2c025ee65ab8ccfd041f47cf5b4c1fb8ab079fd255e`

7 项授权输入是准备包清单、准备包聚合记录、上游绑定、独立复核清单、独立复核聚合记录、`accept / true` 签字和用户授权语境记录。用户授权语境已改为总控转述的精确 20 表、1,057 行、5 张资料卡、10 份官方网页快照和原子回滚范围，其 SHA-256 为 `db36de1aaad8a98ff62d662e4d9fe0c535ac9f0f1138766bb1ce5ceaca8383b2`。更新后的 7 项授权输入全部匹配，清单 SHA-256 为：

`31e138af1b9058c07b54240f379582eb4f88d1b985e2b7851e2e90e0cf243541`

执行器要求的授权标记按冻结合同本应精确连接六项：正式令牌、独立签字 SHA-256、上游绑定 SHA-256、f152 聚合、准备包清单 SHA-256 和 24 项备份清单 SHA-256。由于自动审批拒绝，该文件没有生成，也没有 marker SHA-256 可报告。

## 正式写前状态

拒绝发生后重新只读核验：正式 32 表聚合仍为 `f1520907d54e406e7d82f591e3799f07f569218aff0c321ac3b836b6c649da10`，正式校验器通过 106,160 项检查，15 个正式目标存在数为 0。24 项备份和 7 项授权输入的哈希、字节数差异均为 0，授权标记不存在。

这次失败属于自动审批拒绝，不是用户拒绝、审批连接失败、沙箱拒绝、远程服务错误、工具故障或模型能力不足。总控若处在能向审批系统提供用户逐字授权的受信任上下文中，可以按同一六字段合同创建标记；本子代理不能把转述当作审批器所需的受信任用户内容。

## 执行记录中的错误

此前准备阶段有三次命令构造错误：只读检索使用了不适合 Windows 的通配路径，PowerShell 诊断字符串把冒号紧接在变量名后，首次创建外置目录时误用了 `New-Item -LiteralPath`。三次都在目标操作前停止；更正后已用显式路径重新执行并验证。它们与本次自动审批拒绝无关。

## 项目文档检查

根 `README.md` 和 `AGENTS.md` 已只读检查。本任务只更新交接报告和正式根之外的授权输入，正式数据状态、项目范围、目录职责与运行约定没有改变，因此没有修改这两份根文档。