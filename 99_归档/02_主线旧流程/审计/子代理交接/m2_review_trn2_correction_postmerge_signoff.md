# Trainium2 修复包合并后状态签字

签字日期：2026-08-13  
独立复核者：`m2_review_trn2_correction`  
裁决：`accept`  
迁移清单：`审计/子代理交接/m2_review_trn2_correction_postmerge_migration.csv`  
迁移清单 SHA-256：`c852696600480a9d8afc55f7a474ef6058897245eedca8b0358d5d6d9a5e10a0`

## 选择运行与成员

我授权总控执行清单中的 25 行迁移。清单恰好包括两个选择运行和它们的 23 个成员，主键没有重复；生成清单时，正式表中的目标行全部仍为 `draft`。

两个运行是 `SELRUN-M1-PILOTS-TRN2-CORRECTED-20260813` 和 `SELRUN-M2W2-TRN2INST-CORRECTED-20260813`。前者有 19 个成员，后者有 4 个成员。总控可以把两个运行的 `status` 和 `review_status` 同时从 `draft` 改为 `reviewed`，并把 23 个成员的 `review_status` 从 `draft` 改为 `reviewed`。

这项语义许可建立在已经完成的反向移除复核上。三实例运行的正向分母为 39 条可采纳事实，四个成员的不可替代缺口分别是 S06 32 条、S07 5 条、S09 1 条、S10 1 条；S15 的两条不可靠容量声明只保留审计链。M1 重跑仍有 19 个来源：H100 和 MLU590 成员与原运行相同，Trainium2 修复后 S06、S07、S10 分别有 49、9、2 条正式支持事实，其余 Trainium2 来源也都保有直接事实或状态、冲突职责。23 个成员均存在，均有 `selected` 筛选记录、匹配的角色记录、非空不可替代理由和至少一条当前正式事实贡献。

执行时不得改动两个运行的 `scope_kind`、`scope_id`、`cutoff_date`、`algorithm_version`、`created_date`、`reviewer` 或 `notes`；也不得改动成员集合、`selection_run_id`、`source_id`、`selected_role`、`mandatory_reason` 或 `notes`。生成清单时，`selection-runs.csv` 的 SHA-256 为 `784b6bf8c6dc218da115aea8cd48793790aac7a0688257da51e2fb4d951024d4`，`selection-members.csv` 的 SHA-256 为 `2886365f1cded00393df3b1923d8805e41d1a3e425ab7464d7652dd6670ff2d1`。任一基线哈希或目标旧状态不符，本许可失效。

## 三张实例卡的行政元数据

三张正式实例卡已经包含在前一份独立复核的 `accept` 范围内，内容、事实映射和来源边界均已验收。我授权总控只做下表列出的三处行政元数据替换。每张卡恰好各命中一次旧标题标记、旧卡片状态和空缺复核人；其余字节必须保持原样。

| 正式文件 | 操作前 SHA-256 | 允许的三处替换 | 预期 SHA-256 |
|---|---|---|---|
| `资料卡/AWS/实例/AWS_EC2_trn2.3xlarge_实例卡.md` | `b9a792a44953000a9a1c392a0af983d3b50373d2bf95f98d3c238c47e55590d4` | 标题删除“（修复草稿）”；`卡片状态：draft` 改为 `卡片状态：已验收`；`复核人：尚未指定` 的值改为 `m2_review_trn2_correction`，并以行内代码格式呈现 | `a06e3f20d2abf4d1e05cf032c9aca21e04678aeea7961acfe6a7895afafbe5d9` |
| `资料卡/AWS/实例/AWS_EC2_trn2.48xlarge_实例卡.md` | `763dd81ff801eedbf567dd181b2f0282d36302711e7c279e1995de0c212c4a62` | 同上 | `3ffd4e7a690d3369cc3217d00c6b0d54d51e9793ecb90b8d93f29e4bdebee302` |
| `资料卡/AWS/实例/AWS_EC2_trn2u.48xlarge_实例卡.md` | `992040c5c337201a498ce4f84aad6a4a5e79303309805f642f38677522174d1a` | 同上 | `49e1e162b14c9335543bf03591e38f32d496efaa8071b1239ab22122b643ac23` |

“其余字节不改”也包括三张卡结尾原有的合并前说明。该句属于已经验收的历史过程记录，本次授权没有扩大到正文改写。三次替换后，三张卡的预期字节数依次为 5,053、6,649、6,229 bytes。若当前哈希、命中次数、替换后字节数或预期哈希不符，应停止操作。

## 执行后只读复查

总控在本签字生成后执行了迁移和三张卡的行政替换，并把操作前文件备份到 `审计/合并备份/M2-W2-TRN2-POSTMERGE-20260813/`。本代理没有修改正式库，随后用备份与当前正式文件作了独立逐项比较。

两张选择表共有 27 个单元格变化：两个运行各有 `status`、`review_status` 两项变化，共 4 项；23 个成员各有一项 `review_status` 变化，共 23 项。清单中的 25 个主键全部命中，没有缺失、额外主键或越界列变化。迁移后，`selection-runs.csv` 的 SHA-256 为 `1c0b49aff25115cb358ad1d6dab32ed9de3b0814fa11342beb9b435ff48f9ba2`，`selection-members.csv` 的 SHA-256 为 `b921192ac0cec96a5ebf1fd8c8d0ecc4951577804c2b1bea2976b1e4117bd620`。

三张卡分别从备份文本应用本签字允许的三次替换后，与当前正式文件逐字节相同；每张卡的三个旧标记在备份中都恰好命中一次。迁移后字节数为 5,053、6,649、6,229，SHA-256 与上表预期值完全一致。因此，卡片正文和其他元数据没有随行政替换发生变化。

新鲜运行正式 32 表校验器，结果为 `PASS`，共执行 93,179 项检查；注册表统计为 323 列、488 个枚举值。至此，本签字授权的两项操作均已按限定边界执行并通过独立复查。

本签字不授权新 S07 endpoint、来源筛选、来源角色、事实、断言、字段要求或其他卡片发生状态变化，也不改写选择运行的范围、成员、角色和不可替代理由。

## 写入边界

本代理只写本签字和 25 行迁移清单，没有修改正式库。项目级 `README.md` 和 `AGENTS.md` 已检查；本次迁移现已执行并复查，但全局项目状态的同步属于总控职责，本代理按写入边界不作修改。
