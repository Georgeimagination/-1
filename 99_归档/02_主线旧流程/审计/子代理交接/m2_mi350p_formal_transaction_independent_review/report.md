# MI350P 正式事务独立复核

结论：accept / ready_for_formal_execution=true

本复核只检查冻结的 MI350P 正式事务准备包，签字范围固定为 formal_execution_of_exact_274_row_signoff_via_transaction_v1。它授权总控在全部绑定仍一致、正式备份合同满足时调用事务执行器；本复核没有执行正式事务，也没有修改准备包、暂存包、正式 32 表、正式资料卡或来源文件。

## 冻结范围

准备包 manifest.csv 的 SHA-256 为 c21612350bae17288e365d26f95b5f669426ba99fd33cefce746644986cb3c98。67 个清单成员的哈希和字节数全部匹配，包聚合为 e7138b42baee6b2d138cb5d8f31593c4f6c62319fa16688e866b840d2e419949。

MI350P 数据签字共有 274 行，其中 270 行是分布在 19 张表中的主键写入，4 行是文件载荷。3 条无写保护与正式库一致，4 个文件目标在复核结束时仍不存在。数据复核报告、数据签字、暂存冻结清单、候选逐表后哈希和三份事务脚本均与准备包记录的 SHA-256 一致。

## 独立验证

静态事务合同通过 8,811 项检查；事务预检通过 11,290 项检查；隔离源根的正式基线校验器通过 106,160 项检查，32 表聚合保持为 f1520907d54e406e7d82f591e3799f07f569218aff0c321ac3b836b6c649da10。对真实正式根的默认拒绝也重新通过，执行器在创建候选、备份或目标之前停止。

三场事务演练依次执行，没有并行：

| 场景 | 结果 | 事务日志 | 核对结果 |
|---|---|---:|---|
| 正向提交 | PASS | 48 行 | 替换 19 张表并复制 4 个载荷；候选和提交后均通过 111,390 项检查，聚合为 28923f7aa9dea488152f699e6879319d1fb96222fe72745754cf8ffd7b46518e |
| 替换 3 张表后故障 | FAIL_ROLLED_BACK | 14 行 | 3 张表按提交顺序的逆序恢复，聚合回到 f152...da10 |
| 复制 2 个载荷后故障 | FAIL_ROLLED_BACK | 86 行 | 2 个载荷和 19 张表分别逆序撤销，聚合回到 f152...da10 |

六个负向门全部通过，覆盖默认拒绝、错误令牌、待签复核、错误镜像标记、目标预先存在和错误合并前哈希。每个负向门都保持 f152...da10，没有留下正式目标。

## 签字与边界

签字文件为 formal-execution-signoff.csv，只有一行，复核者为 m2_mi350p_formal_transaction_independent_review，结论为 accept，ready_for_formal_execution=true。签字绑定准备包 manifest 与 aggregate、三份事务脚本、MI350P 数据复核报告和 274 行数据签字、暂存冻结清单、正式基线、候选逐表后哈希、候选 aggregate、111,390 项候选校验计数和授权令牌哈希。

这份签字不替代正式备份，也不允许手工改表。正式执行前仍须建立并核验 23 项备份 manifest 和授权标记；任何绑定变化都会使本签字失效。

## 运行错误记录

复核期间有 12 条命令构造、编码或临时路径处理错误，均记录在 operator-error-log.csv。其中两次演练因 Windows PowerShell 5.1 的长路径处理在候选写表前停止，事务报告回滚成功；一次盘符映射因工具进程隔离不可见，脚本没有启动。随后使用同一独立复核目录的短盘符映射，在单一工具进程内完成三场未改脚本演练并解除映射。上述错误均未影响正式库，也没有发生用户拒绝、自动审批拒绝、审批连接失败、沙箱拒绝或远程服务错误。

复核结束时，独立源根和正式根均为 f152...da10，4 个文件目标均不存在，事务临时目录、锁和盘符映射均为 0。项目根 README.md 与 AGENTS.md 已按只读方式检查；本复核没有改变项目目标、结构、运行方式、证据规则或全局进度，因此无需修改。