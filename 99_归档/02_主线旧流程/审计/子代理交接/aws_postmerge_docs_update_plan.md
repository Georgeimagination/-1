# AWS 正式合并后的文档更新清单

审计日期：2026 年 8 月 13 日  
审计方式：只读  
结论：AWS（Amazon Web Services）四代物理对象事实包已经正式合并，合并后独立复核为 `accept / closed`。项目说明、进度记录和型号索引仍停留在“重新签字、演练、尚未写入”的旧状态，须由总控统一更新。

## 已核对的正式结果

正式库当前有 78 个对象、27 条对象关系、678 条事实、680 条逐来源断言、863 条字段要求。`card-completeness.csv` 有 360 行，覆盖 40 个唯一资料卡对象；面向读者的验收卡构成为 29 张架构卡、3 张 M1 试填卡、3 张 Trainium2 实例卡、1 张 Helios 系统观察卡和本次 4 张 AWS 封装实现卡，共 40 张。

来源侧有 83 个来源家族、84 个内容版本、138 个访问入口、88 条来源筛选记录、98 条入选角色记录。筛选状态为 69 个 `selected`、9 个 `redundant_covered`、2 个 `pending`、3 个 `out_of_scope`、2 个 `rejected_unreliable`、2 个 `inaccessible` 和 1 个 `lead_only`。正式库有 8 次最小来源选择运行，其中 7 次为 `reviewed`、1 次为 `superseded`；8 次运行的 `review_status` 均为 `reviewed`，96 个选择成员也全部为 `reviewed`。

字段注册表仍为 324 列、120 个事实字段和 488 个枚举值。正式校验器在 `subject_contract_mode=gate` 下通过 106,160 项检查；32 表聚合值为 `f1520907d54e406e7d82f591e3799f07f569218aff0c321ac3b836b6c649da10`。资料池仍为 111 份 PDF、313,921,821 字节、2,210 页、914 行网页清单和 11 条待补记录，检查通过，只有一条既有 Google TPU PDF 解析警告。

AWS 包相对合并前基线新增 57 条事实、69 条断言、71 条字段要求、27 个组件、6 个存储层、3 条链路、20 条精度路径、29 个条件集和 36 行九域完整度记录。来源审计新增 5 个冲突组及 10 个冲突成员、20 条检索日志、50 条检索结果、1 条覆盖关系、14 条来源角色、1 次选择运行及 8 个成员、11 个访问入口；来源家族和内容版本数量没有增加。`SCREEN-M2-GA-A01` 从 `redundant_covered` 更新为 `selected`，所以筛选总行数不变。

正式事务使用 486 行签字：477 项授权写入和 9 项不写守卫。20 张 CSV 被替换，15 个新文件写入；独立复核确认 0 个签字差异、0 个 payload 差异、0 个守卫差异、0 个越权变化和 0 个临时残留。合并前 32 表聚合值为 `d10ad305a820f0fc1db7f2bf7030ca149ea7b556ceacd0a32d593f08a7cc01ff`，合并后为上述 `f152...a10`。事务没有使用回滚。

## 四张新卡和固定快照

应在型号索引中新增以下正式卡：

| 正式对象 | 正式路径 | 卡中正式事实数 |
|---|---|---:|
| `OBJ-AWS-INFERENTIA1-CHIP` | `资料卡/AWS/封装/AWS_Inferentia1_芯片实现资料卡.md` | 8 |
| `OBJ-AWS-TRAINIUM1-CHIP` | `资料卡/AWS/封装/AWS_Trainium1_芯片实现资料卡.md` | 11 |
| `OBJ-AWS-INFERENTIA2-CHIP` | `资料卡/AWS/封装/AWS_Inferentia2_芯片实现资料卡.md` | 11 |
| `OBJ-AWS-TRAINIUM3-CHIP` | `资料卡/AWS/封装/AWS_Trainium3_芯片实现资料卡.md` | 27 |

11 个 `web_snapshot` 访问入口已经写入 `最小参考资料库/快照/AWS/PackageImplementations/2026-08-13/`，状态均为 `accessible / reviewed`。A01 和 A07 各保留一份 `latest` 审计快照和一份版本化首选快照；其余七份快照均为首选入口。它们复用 `SRC-M2-GA-A01` 至 `A08`、`A10`，没有新建来源家族或来源版本。包级最小来源集是 A01 至 A08 八源；A10 的五条 package 事实均由 A07 覆盖，因而不进入本次 package 选择运行，但 A10 仍因架构事实而保持全局 `selected`。

## 总控编辑清单

### `README.md`

- “当前阶段”首段删除“旧签字失效、正在重新签字和演练、四卡尚未写入”的旧叙述，改为：字段合同迁移后的 AWS 重签、事务演练、正式合并及合并后独立复核均已完成，裁决为 `accept / closed`。
- “已有资料”补充 AWS PackageImplementations 的 11 份 2026-08-13 官方 HTML 快照；“AWS 没有本地 PDF”仍然成立，不应删掉。
- 把正式统计改为 678 facts、680 assertions、863 requirements、40 张验收卡、8 次选择运行（7 reviewed、1 superseded）和 96 个全部 reviewed 的成员。删除“AWS 只完成身份预留、23 条待办尚未形成正式事实”。
- 把校验结果改为 106,160 项，补记当前 32 表聚合值 `f152...a10`；字段数和注册列数不变。
- “阅读顺序”加入 `审计/M2_AWS四代物理对象事实包正式合并验收.md`。

### `AGENTS.md`

- “当前状态”中把 AWS 重签与演练改为正式合并和合并后 `accept / closed`；Google 的待办边界保持不变。
- “已有资料”或快照说明中加入 11 份 PackageImplementations 快照，不要把它们算作 PDF。
- “已完成内容”的资料卡索引统计改为 29 张架构卡、3 张 M1 卡、3 张 Trainium2 实例卡、1 张系统观察卡和 4 张 AWS 封装卡。
- 正式库统计改为 678/680/863，校验改为 106,160；对象与关系仍是 78/27。
- 在已完成工作包中补记 AWS 包的 57/69/71、八源最小集、11 快照、4 卡及验收路径。
- “已知问题”删除“必须完成重签与严格演练才能合并”。保留并改写为：五组官方资料分歧仍在正式库中以 `needs_resolution` 并列保存；其中包括 Trainium1、Inferentia2 的 820 GiB/s 与 820 GB/s，以及 Trainium3 的 144 GiB 与 144 GB、4.9 TB/s 与 4.7 TB/s、16 与 20 个 CC Core。
- 把“正式合并仍须保留 Trainium3 分歧”改为“正式合并已经保留这些分歧”。

### `研究计划.md`

- 顶部“当前状态”改为 AWS 包已正式合并并经合并后独立复核关闭。
- 第 11 节末尾的长状态段落更新为 678/680/863、40 卡、8 次运行/96 成员、106,160 项检查和 `f152...a10`；把 AWS 的“须重签、演练、正式合并”改为完成态。
- 保留计划中的方法、字段、对象层级和验收规则；本次没有改变研究范围或数据模型。

### `进度/当前状态.md`

- “当前结论”把 36 张改为 40 张，并点明新增四张 AWS 封装实现卡。
- “当前基线”更新正式统计、筛选状态、选择运行、成员、校验数和 32 表聚合值；来源家族 83、来源 84、入口 138 可在同段补齐。
- “已正式合并的工作包”新增 `M2-W2-AWS-PACKAGE-FACTS` 一行：4 卡、57 facts、69 assertions、71 requirements、八源选择、11 快照。
- “正在进行的工作”不再保留 AWS 未合并段落；可改成已关闭结果和五组未决冲突，或把该段移入“已正式合并”。Google G15 等后续事项继续保留。
- “生命周期与合并纪律”可补记 AWS 的 486 行签字、477 项写入、9 项守卫及无回滚结果。
- “下一停点”删除 AWS 重签/演练，改为后续已排队实现包和 Google G15 原始响应体；若总控按当前队列推进，可写 MI455X 模组、MI350P 卡与 Ascend 950 物理对象包的复核/接收。
- “恢复工作时先读”加入 AWS 正式合并验收和合并后独立复核。

### `进度/任务台账.csv`

只改 `task_id=M2-W2-AWS-PACKAGE-FACTS` 这一行：

- `status=completed`，`completed_date=2026-08-13`；
- `output_paths` 追加 `审计/子代理交接/aws_post_field_rebase`、`aws_post_field_rebase_independent_review`、`aws_formal_transaction_prep`、`aws_formal_transaction_independent_review`、`aws_formal_postmerge_review.md`、`aws_formal_postmerge_signoff.csv`、四张正式卡和 `审计/M2_AWS四代物理对象事实包正式合并验收.md`；
- `verification` 改为：合并后独立 `accept / closed`；486 行签字，477 项写入、9 项守卫；20 表与 15 文件均匹配；正式库 678/680/863，40 卡；验证器 106,160 项通过，32 表聚合值 `f152...a10`，资料池通过；
- `blocker` 清空；
- `next_step` 改为保留五组 `needs_resolution` 冲突，并按队列推进后续实现对象包。

### `进度/里程碑.md`

把 M2 节点中 AWS 的旧行改成完成态：迁移后重新签字、事务脚本及独立复核、外置备份、正式提交、合并后独立复核均已完成；新增 57/69/71、四卡、八源、11 快照；该节点正式库为 678/680/863、40 卡，验证器通过 106,160 项，裁决 `accept / closed`。不要改写 Trainium2、Google 或字段迁移节点的历史“该节点验收时”数字。

### `资料卡/型号索引.md`

- 首段把“第二波 3 张实例卡和 1 张系统观察卡”改为“第二波 3 张实例卡、1 张系统观察卡和 4 张 AWS 封装实现卡”。
- 汇总表将 AWS 的“第二波实现或系统卡”由 3 改为 7、AWS 合计由 10 改为 14；合计行由 4/36 改为 8/40。其他厂商数字不变。
- 在 AWS 表中加入上述四个对象和卡片链接，状态写“已验收；物理封装细节仍为 `needs_resolution`”或同义的克制表述。

### `进度/决策记录.md`

在 `DEC-028` 后新增一个简短决定，建议使用：

## DEC-029：AWS 四代实现以保守 package 对象承接直接规格

- 日期：2026 年 8 月 13 日
- 状态：生效；事实包已正式合并并通过合并后独立复核
- 决定：Inferentia1、Trainium1、Inferentia2 和 Trainium3 继续使用 `package` 作为单器件实现事实容器。只接收官方资料直接支持的芯片级峰值、存储、执行资源、工艺和互联数量；裸片数、芯粒构成、封装方式、中介层、面积和晶体管数保留 `not_found`。五组官方分歧并列保存为 `needs_resolution`，不换算后静默选值。
- 原因：现有一手资料能证明单器件实现规格，却不足以证明更细的物理封装结构；部分版本还对单位、带宽和 CC Core 数量给出不一致值。
- 影响：四张实现卡、57 条事实、69 条断言、71 条字段要求和八源最小集进入正式库。11 个 2026-08-13 快照只增加访问入口，不重复计算独立来源；A01/A07 的 latest 入口只作审计。验收见 `审计/M2_AWS四代物理对象事实包正式合并验收.md`。

## 正式验收文件建议内容

新建 `审计/M2_AWS四代物理对象事实包正式合并验收.md`，内容按以下顺序写：

1. 写明裁决 `accept`、状态 `closed`、验收日期和对象边界。说明 `package` 是保守的单器件容器，不等于已证明单裸片或已知封装结构。
2. 列出四个对象、四张卡及每卡正式事实数 8/11/11/27；列出包级 57 facts、69 assertions、71 requirements、27 components、6 memory levels、3 links、20 precision paths、29 conditions、36 completeness rows。
3. 说明来源侧没有新增 family/source，只新增 11 个 2026-08-13 快照入口；A01 至 A08 构成八源最小集，A10 的 package 事实由 A07 覆盖；A01/A07 的 latest 与版本化正文等价，不增加证据来源数。
4. 列出五组未决冲突及 20 个 `not_found` 物理字段。明确这些状态已经过复核，但没有被解释成已解决。
5. 记录事务边界：486 行签字、477 项授权写入、9 项守卫、20 表、15 文件、合并前后聚合值、候选 20 表 posthash 清单 SHA-256 `4188a42de1eafc383a592814330790ebb939d2613756781a3c941611e11857a7`、外置备份 manifest SHA-256 `dd94cfbe618272a46242cfa842b936512ecb6f5ccd6c66e710829c538f7a4f8f`，以及未启用回滚。
6. 记录合并后独立复核：签字外改动、payload、守卫和残留均为 0；引用 `审计/子代理交接/aws_formal_postmerge_review.md` 和 `aws_formal_postmerge_signoff.csv`。
7. 写最终基线 78/27/678/680/863、40 卡、83 families、84 sources、138 endpoints、8 runs、96 members、106,160 项校验和 `f152...a10`；资料池检查保持 111 PDF 和一条既有警告。
8. 结尾只保留真实后续：五组冲突继续跟踪，后续若出现能证明物理封装结构或解释版本差异的一手资料，再新建修订事实和重跑本对象的最小来源选择。

## 审计中的异常

两条只读统计命令最初把 `foreach` 语句结果直接接到管道，PowerShell 报“空管道元素”；这是命令构造的模型/操作者错误。改为先把结果赋给变量再格式化后，统计完成。没有发生沙箱拒绝、审批失败、远端服务错误或文件写入副作用。

## README 与 AGENTS 检查

`README.md` 和 `AGENTS.md` 已只读检查，均需由总控更新，因为正式数据、资料卡数量、进度状态、校验数和 AWS 已知问题都发生了变化。本审计按任务边界没有修改它们。