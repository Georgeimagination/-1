# 候选校验项计数说明

MI350P 最终签字中的 `temporary_merge_validator_checks` 为 111,343，正式事务候选镜像通过 111,390 项检查。前者对应 staging（暂存区）临时合并，后者对应完成生命周期状态提升后的受控事务候选，因此两道门都要保留。

暂存区临时合并直接追加草稿行，没有执行 270 条 `review_status: draft -> reviewed`；选择运行的语义 `status` 也仍为 `draft`。受控事务重放后，40 条非派生事实进入 `reviewed`。正式校验器会逐条检查这些事实是否至少有一条已复核断言，由此新增 40 项。

`SELRUN-M2W3-AMD-MI350P-20260813` 同时从 `draft` 改为 `reviewed`。校验器先检查一次成员非空，再对 3 个成员分别检查不可替代理由和 `selected` 筛选状态，共新增 7 项。总增量为 47 项：

$$
111{,}343 + 40 + 1 + 3 \times 2 = 111{,}390
$$

候选镜像和正向事务演练都在 `gate` 模式通过 111,390 项检查，32 表聚合为 `28923f7aa9dea488152f699e6879319d1fb96222fe72745754cf8ffd7b46518e`。事务预检继续要求 274 行上游签字逐行绑定 111,343；执行器把 111,390 作为状态提升后的候选与提交后检查数。二者检查的阶段不同，不能互相替代。
