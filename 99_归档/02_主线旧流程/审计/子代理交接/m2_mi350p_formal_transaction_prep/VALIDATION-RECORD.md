# MI350P 正式事务准备验证记录

结论：`ready_for_independent_review`

这份记录说明准备包已经具备独立复核条件，但没有授权正式执行。manifest 是逐文件哈希清单；aggregate 是按约定把多条文件哈希再次汇总得到的聚合哈希。准备者没有签署 `accept`，正式执行器继续处于硬拒绝状态。

## 正式基线与输入绑定

冻结前重跑正式校验器，32 表通过 106,160 项检查。事务预检通过 11,290 项检查，正式 32 表 aggregate 保持为 `f1520907d54e406e7d82f591e3799f07f569218aff0c321ac3b836b6c649da10`。四个 MI350P 正式文件目标均不存在，3 条无写保护与冻结签字一致。

准备包逐项绑定以下输入：

| 输入 | SHA-256 或聚合值 |
|---|---|
| MI350P 最终独立复核报告 | `470d381d66e5cb7ccd7b3fc9aa88ede12ed1a995a6a1af6e03844630d9b43461` |
| 274 行精确签字 | `74ac69c299f5ce817c03236ef97df4e13f08f2d6724bb1c0178376661be5dad1` |
| 50 行 staging（暂存区）冻结清单 | `1cb279b56f4fbfc688ed2f798646a93baed02ffbd34a647ffecf4cb6672f0e01` |
| staging 冻结内容 aggregate | `152df43e8b64c2b78c54a41c6cf9bd7acfa4d3315fcf33d6128399b22f2f8825` |
| 签字所用正式基线清单 | `68773358da2fe590aaaecdcccdaf0e68d61176df35d4b759dad14b4ae1174e5c` |

274 行签字包含 270 个结构化主键和 4 个文件载荷。270 个主键分布在 19 张表中；`authorized-write-set.csv`、逐表合并前哈希、表头、来源行哈希和目标行哈希均已冻结。静态合同共通过 8,811 项检查。

## 拒绝门

执行器对当前正式根默认拒绝，且拒绝发生在结果目录、候选、备份或目标文件创建之前。六个负向门均达到预期：

| 场景 | 预期停止点 | 结果 |
|---|---|---|
| 不提供正式授权参数 | 默认拒绝门 | 通过 |
| 提供错误授权令牌 | 令牌核验门 | 通过 |
| 独立复核仍为 `pending / false` | 独立签字门 | 通过 |
| 隔离镜像标记错误 | 镜像身份门 | 通过 |
| 四个目标之一预先存在 | 目标不存在门 | 通过 |
| 合并前逐表哈希错误 | 基线哈希门 | 通过 |

六次负向检查的正式 aggregate 前后均为 `f152...da10`，四个正式目标始终不存在，正式写入次数为 0。详细记录见 `negative-gates.csv`、`negative-gates-summary.json` 和相应日志。

## 隔离镜像演练

| 场景 | 事务日志行数 | 已提交表 / 已回滚表 | 已提交载荷 / 已回滚载荷 | 场景结束状态 |
|---|---:|---:|---:|---|
| 正向 | 48 | 19 / 0 | 4 / 0 | 候选 aggregate 为 `28923...18e` |
| 替换 3 张表后中断 | 14 | 3 / 3 | 0 / 0 | 逆序回滚到 `f152...da10` |
| 复制 2 个载荷后中断 | 86 | 19 / 19 | 2 / 2 | 逆序回滚到 `f152...da10` |

正向候选和提交后镜像均通过 111,390 项检查。两种中断场景分别命中预定故障点，CSV 逆序回滚顺序均已核验；第二种中断同时核验了载荷逆序删除。每场演练都保存事务日志、结果摘要和执行输出。三场结束后，正式 32 表没有变化，四个正式目标没有出现，事务临时文件、临时目录和锁均为零。

111,390 与上游签字中的 111,343 分属不同状态阶段，47 项差值已经逐项解释，见 [candidate-validator-count-analysis.md](candidate-validator-count-analysis.md)。

## 冻结检查

7 个 PowerShell 脚本都完成语法解析，错误数为 0。`new-file-targets.csv` 列出的四个正式路径逐一核对为不存在；正式根没有 `.mi350p-formal-transaction-new` 或 `.mi350p-formal-transaction-restore` 文件，准备包没有遗留事务锁或 `.tmp-mi350p-formal-*` 目录。

项目根 `README.md` 与 `AGENTS.md` 已按只读方式检查，本任务未修改这两个文件。全部已知命令构造和计数假设错误记录在 `operator-error-log.csv`：共 10 条，均归类为模型或操作者错误，均未影响正式库；本任务没有用户拒绝、自动审批拒绝、审批连接失败、沙箱拒绝或远程服务错误。

## 下一道门

不同代理必须以 `m2_mi350p_formal_transaction_independent_review` 身份复算本包，并将签字范围固定为 `formal_execution_of_exact_274_row_signoff_via_transaction_v1`。在其独立签字达到 `accept / true` 且绑定冻结后的 `manifest.csv` 和包 aggregate 之前，不能向正式根传入 `-AllowFormalRoot`，也不能创建正式备份或执行正式写入。
