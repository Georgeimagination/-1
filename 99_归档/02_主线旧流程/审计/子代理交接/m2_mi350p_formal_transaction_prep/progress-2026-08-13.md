# 工作节点

整体状态：`ready_for_independent_review`

## 节点 0：范围与输入确认

状态：`completed`

已读取项目协作规则、MI350P 最终独立复核报告与 274 行精确签字，并核对相应 SHA-256。当前交接目录是本任务唯一写入范围；项目根 README、AGENTS、正式 32 表、正式资料卡和四个正式文件目标均按只读处理。

## 节点 1：静态事务合同

状态：`completed`

8,811 项静态检查通过。274 行签字拆为 270 个结构化主键和 4 个文件载荷；19 张写表、3 条无写保护、4 个不存在目标、逐表基线，以及备份和逆序回滚清单已经冻结。

## 节点 2：执行器与负向门

状态：`completed`

正式模式默认拒绝。六个负向门分别覆盖无授权参数、错误令牌、独立复核仍待签、错误镜像标记、目标预先存在和合并前哈希错误；全部在正式写入前停止。只有不同代理完成独立复核后，执行器才允许对正式根做最小重新绑定。

## 节点 3：隔离镜像演练

状态：`completed`

正向、替换 3 张表后中断、复制 2 个载荷后中断三种场景均通过。每个场景都保存事务日志和结果摘要。两种中断场景完成逆序回滚；三种场景结束后，临时文件与锁为零，正式 32 表仍为 `f152...da10`，四个正式目标仍不存在。

## 节点 4：冻结与交接

状态：`completed`

最终预检通过 11,290 项检查，正式校验器通过 106,160 项检查，7 个 PowerShell 脚本的语法解析错误为 0。冻结清单、验证记录和执行回滚合同已备齐。准备者未签 `accept`，本包冻结为 `ready_for_independent_review`。
