# AWS 正式合并事务准备进度

更新时间：2026-08-13

状态：`ready_for_independent_review`

正式执行：`not_authorized`

本交接目录已经冻结 20 张待写 CSV、9 个不写守卫、15 个新文件目标和候选 20 表 posthash。一次性事务脚本绑定 486 行签字、两份既有 AWS 复核哈希、当前 32 表基线、三份脚本、package manifest/aggregate、外部事务复核签字、授权 token、外部 25 项备份 manifest 和授权 marker。

正式模式在任何结果、候选、锁或临时文件写入前，先验证事务独立复核签字、完整外部备份、15 个目标不存在和冻结 posthash。通过后，formal 的 candidate、临时备份和结果仍位于正式根之外。提交临界点再次检查 32 表 aggregate 与 9 个守卫；回滚按 journal 的实际提交顺序逆序执行，payload 删除受当前 SHA-256 门保护。

当前冻结版本已通过语法解析、17 项静态合同检查、正式根默认拒绝、4,324 项只读预检、97,920 项正式数据校验和 32/32 表哈希核对。正式 15 个 payload 目标为 0，事务 temp/lock 为 0。正式 CSV、资料卡和快照没有被本任务修改，也没有创建正式备份目录。

较早版本的三场景镜像证据继续保留，但收口修改改变了脚本哈希。独立复核者需要对冻结 manifest 对应的当前脚本重新跑正向、两个故障注入和 formal 前置门负向场景。复核通过后，应在冻结包外生成新的单行事务签字；本状态不允许准备代理自行执行正式合并。