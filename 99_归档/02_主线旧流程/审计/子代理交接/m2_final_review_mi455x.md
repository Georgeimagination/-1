# M2-W3 AMD MI455X 模组包最终独立复核

> 复核结论：`accept`  
> 正式合并授权：有条件授权，且只限精确签字清单  
> 复核日期：2026-08-13  
> 被复核包：`审计/子代理交接/m2_staging/M2-W3-AMD-MI455X-MODULE/`  
> 精确签字：`审计/子代理交接/m2_final_review_mi455x_signoff.csv`  
> 写入边界：本报告和精确签字；复核期间未修改暂存包、正式 32 表、正式资料卡、正式来源文件或项目根文档

本文中的 CSV 是逗号分隔值文件，SHA-256 是 256 位安全散列算法。HBM 是 High Bandwidth Memory（高带宽内存），XCD 和 IOD 分别指加速计算裸片和输入输出裸片。`endpoint` 指同一来源内容的具体访问入口或本地固定副本。

## 裁决

修复后的 MI455X 模组包可以进入总控正式合并。上一版独立复核提出的状态、派生存算比、稠密口径、网页定位和置信理由问题均已关闭；包内 39 条事实全部是单个 `OBJ-AMD-MI455X` 模组的直接事实，没有把 Helios 机架值或 CDNA 5 架构机制复制到产品层。资料卡、结构化行和四来源最小集能够互相对应。

本授权绑定当前正式基线和冻结包，不能泛化为“按文件整体追加”。总控只能执行精确签字中的 242 个结构化主键和 3 个文件复制动作。冻结清单、正式基线、任一来源文件或目标存在状态发生变化时，授权立即失效，需要重新核对。

## 冻结包与来源

`validation/freeze-manifest.csv` 的 SHA-256 为 `56ca298d6c9b327c3b5ef2babf30a0f5dc109b519381083e5717452a01b1331b`。清单有 42 行，42 个成员的文件大小和 SHA-256 全部匹配；按 `relative_path|sha256|bytes` 排序连接后的聚合 SHA-256 为 `dfed75716edaff380b35f4b356f4a631a8ce17413a8e339ac2b77448cb94b0a0`。

四个固定来源均重新核对了文件大小和哈希：

- MI455X 产品页固定 HTML：217,833 字节，`2fa27618862c42b0b9e6eed2e5c88c360e6eac2650d9fc97bae08c6338315b47`；
- MI455X brochure 固定 PDF：659,374 字节，`6555fef1399a35f472e325dc61936d293e129735ec1e191d5fd0f3b6fa0c4208`；
- 既有 MI400 固定页：318,594 字节，`aa5603704c4d77d1e912a5921e289ccabaa45111bab209c2ce08975aedc58a7e`；
- 既有 CDNA 5 白皮书：4,152,251 字节，`2381d60185f79989d3d5e4260c86f72504fcab256b40bb85fe4a6dd782afb3ef`。

修复后的反向移除结果成立。产品页保留精确对象身份、截止日 `announced` 状态语境、WGP、L2、晶体管和具名互联端点；brochure 保留精确峰值表、XCD/IOD 数量、CPU 到 GPU 带宽和可靠性信息；MI400 固定页保留单模组 3D 混合键合和 CoWoS-L 封装表述；CDNA 5 白皮书只解释已经存在的架构关系及架构级语义边界。删除任一来源都会丢失上述独有贡献，因此四个成员都进入最小来源集。

## 修复项复核

截止日状态链已经恢复。`FACT-M2W3-AMD-MI455X-STATUS` 的规范值为 `announced`，对应字段要求是 `value_available`，产品页断言使用固定页面标题和 `Launch Date 7/23/2026`。这项归一化只表示产品已公开宣布，不证明出货、量产爬坡或一般可用；实际可用日期仍为独立的 `not_found` 缺口。旧状态检索日志和四条结果均已删除。

四条产品级存算比已整链删除。事实、要求、条件集、派生指标和派生输入中均未留下相关主键；`derived-metrics.csv` 与 `derived-inputs.csv` 只有表头。资料没有把性能表基准列标为 dense，也没有给出 MI455X 产品级累加语义，因此不计算该派生指标是正确处理。

三个矩阵 precision path 的备注都改为 base-column 与厂商标注 structured-sparsity 分列，九个条件指纹中没有 `dense` 外推。七条产品页断言的定位与固定 HTML 中的实际标签一致，包括 `Lithography`、`Stacks of Memory`、`GPU Form Factor`、`Cooling`、`Dedicated Memory Size` 以及两条双向带宽标签。15 条被点名事实的 `confidence_reason` 均只引用该事实已登记的断言来源，证据状态仍按不同 `source_id` 的数量计算。

## 结构化闭合

独立复算得到 39 条直接事实、53 条断言、44 条字段要求、9 个条件集、8 条无结果检索日志和 32 条检索结果；派生事实、派生指标和派生输入均为 0。另有 5 个组件、2 个存储层级、3 条链路、12 条精度路径、9 行完整度、2 个新增来源、4 个新增 endpoint、1 次选择运行和 4 个选择成员。19 组预期计数全部匹配，结构化行的 `review_status` 当前均为 `draft`。

八个 `not_found` 要求各有一条检索日志和四个选定来源的逐一核对结果。资料卡证据映射覆盖 39/39 条事实、8/8 个非值可用要求和既有 `OREL-AMD-MI455X-IMPLEMENTS-CDNA5` 关系，失败为 0。卡片没有引用已删除的派生主键。

## 校验与正式库静态性

正式基线以 `gate` 模式通过 32 张表和 97,920 项检查。把 24 份暂存 CSV 叠加到独立临时镜像后，官方校验器新鲜通过 102,752 项检查，注册表为 324 列、488 个枚举值。临时镜像随后删除。

复核开始和临时验证完成后，正式 32 表都与包内 `formal-32-csv-baseline.json` 匹配 32/32，变化数为 0；按同一口径计算的聚合 SHA-256 为 `6aa1bd6373b9d07b383a25c1355609cc6076ee8e491048d68d9c4ba445c0d165`。这也是正式执行前必须再次满足的硬门。

临时镜像的第一次追加尝试因延迟读取保留文件句柄而失败；镜像随后按 32 表基线完整重置，再用一次性读取重做，102,752 项检查通过。另有几次 PowerShell 命令拼接错误，均属于复核者操作错误，没有写入正式库；后续成功复算覆盖了相应检查。没有沙箱拒绝、审批失败或远端服务错误影响本裁决。

## 精确授权

`m2_final_review_mi455x_signoff.csv` 有 245 行，SHA-256 为 `b57c0d283301df0793ad61b07f43d03c10c3bb34bd4334d48aa5976f744185fb`，文件大小 211,156 字节。其中 242 行逐一绑定非空暂存行的表名、主键、来源文件哈希、规范化行哈希和正式基线；另有 3 行绑定资料卡、固定 HTML 和固定 PDF。

总控可在全部硬门匹配时执行以下动作：

1. 追加签字中的 242 个结构化主键，并只把这些行的 `review_status` 从 `draft` 改为 `reviewed`；
2. 把 `SELRUN-M2W3-AMD-MI455X-20260813` 的语义 `status` 从 `draft` 改为 `reviewed`，四个 selection member 的语义内容保持冻结值；
3. 把两个本地 endpoint 的 `local_path` 改为签字指定的正式 HTML 和 PDF 路径，其他列保持冻结值；
4. 逐字节复制资料卡、固定 HTML 和固定 PDF 到签字指定且执行前必须不存在的正式目标；
5. 合并后以 `gate` 模式重跑正式校验器，并复核 39 条事实、53 条断言、44 条要求、选择运行与四个选择成员的生命周期。

签字没有授权改写事实值、条件、来源定位、证据状态、要求状态或卡片正文，也没有授权覆盖已存在的目标文件。空暂存表不产生正式写入。执行前若正式 32 表聚合不再是 `6aa1bd6373b9d07b383a25c1355609cc6076ee8e491048d68d9c4ba445c0d165`，总控不得沿用本授权。

## 保留缺口

实际可用日期、单模组功耗、程序员可见累加、物理累加器、MoE/Top-K 专用实现、HBM 总接口宽度、die 面积和版本化 runtime 仍为 `not_found`。这些缺口不能用 launch date、液冷形态、邻近 SKU、Helios 聚合值或第三方数字补齐。`accept` 表示当前公开证据已经按项目规则闭合，不表示这些字段已有公开定值。