# AWS（Amazon Web Services）四代物理对象事实包正式合并后独立复核

复核日期：2026-08-13  
裁决：`accept`  
状态：`closed`

正式合并与签字结果逐项一致，可以验收。复核只读取正式库、冻结事务包、独立事务签字和正式事务外置备份；除本报告及其签字清单外，没有修改正式数据、资料卡、网页快照、根目录文档、外置备份或事务结果。

## 合并结果

事务结果为 `PASS`，共 2,117 项检查；477 项授权写入、9 项不写守卫、20 张 CSV（逗号分隔表格）替换和 15 个新文件均已完成。候选和提交后的 32 表聚合值同为 `f1520907d54e406e7d82f591e3799f07f569218aff0c321ac3b836b6c649da10`，候选 20 表写后哈希清单的 SHA-256（文件内容哈希算法）为 `4188a42de1eafc383a592814330790ebb939d2613756781a3c941611e11857a7`。事务没有启用回滚。

我用外置备份重建了合并前 32 表：聚合值为 `d10ad305a820f0fc1db7f2bf7030ca149ea7b556ceacd0a32d593f08a7cc01ff`。合并后恰好 20 张签字表发生变化，其余 12 张表均未变化。20 张正式表的哈希和字节数全部等于冻结候选清单，事务结果中保存的清单也与冻结文件逐字节相同。

486 行 AWS 签字含 457 行生命周期写入、5 行精确语义收尾、15 个文件写入和 9 个不写守卫。457 个主键全部存在且只出现一次；438 行为新增，19 行为既有行更新，没有删除或签字外主键变化。逐单元格重放冻结来源行、`review_status` 授权和 5 项语义收尾后，与正式库 0 处不符。生命周期状态为 408 个 `reviewed` 和 49 个 `needs_resolution`；AWS 来源选择运行及 8 个成员均为 `reviewed`。

9 个守卫覆盖 `fields.csv`、`objects.csv` 和 `object-relations.csv`。守卫行、完整行 JSON（结构化文本格式）、三个文件哈希都与合并前备份一致。11 份网页快照和 4 张资料卡全部存在，哈希、字节数及来源文件一致；两个目标目录中没有签字外文件。四张卡状态均为 `accepted`，所列正式事实分别为 8、11、11、27 条，与新增 57 条正式事实相符。20 个签字访问入口（endpoint）均为单行记录；11 个本地快照路径可解析且哈希相符，9 个原有在线入口保持原状态。

## 运行与清理

提交事务日志（journal）有 72 行连续记录，包含 20 对 CSV 提交事件、15 对新文件提交事件和最终完成事件；每个写前、写后哈希都与备份或冻结候选一致，没有回滚事件。外置备份清单（manifest）恰好 25 项，逐项哈希和字节数通过；授权标记（marker）与独立执行签字、冻结包清单、AWS 签字和合并前聚合值精确绑定。正式根、外置备份和外置临时目录中没有残留事务临时目录、锁、替换临时文件或恢复临时文件。

正式数据校验器已重新运行，`subject_contract_mode=gate` 下 106,160 项检查全部通过，注册表为 324 列、488 个枚举值。当前正式库有 78 个对象、27 条对象关系、678 条事实、680 条断言、863 条字段要求、40 张资料卡、8 个来源选择运行和 96 个选择成员。

资料池只读检查也重新通过：111 份 PDF（便携式文档格式）、313,921,821 字节、2,210 页、914 行网页清单和 11 条待补记录均未变化。仍有 Google 张量处理器（TPU）固定 PDF 的 1 条已知解析警告，没有新增警告。

## 裁决边界

本次 `accept` 只确认 AWS 四代物理对象事实包按冻结的 486 行签字完整写入，且没有发现越界改动。外置备份和事务结果应继续保留，供总控形成正式验收记录和更新项目状态。AWS `package` 对象仍是保守容器；49 个 `needs_resolution` 状态、Trainium3 的十进制 144 GB/二进制 144 GiB 及其他公开资料分歧没有在本次合并中被改写。

项目级 `README.md` 和 `AGENTS.md` 已只读检查，无需由本复核修改。正式状态、统计和进度应由总控在接受本签字后统一更新。