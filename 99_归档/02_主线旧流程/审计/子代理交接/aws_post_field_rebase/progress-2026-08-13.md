# AWS 字段迁移后重签进度

记录时间：2026-08-13 08:32（Asia/Hong_Kong）

## 已完成

已独立复算字段主体合同迁移后的正式 32 表聚合值，结果为 `d10ad305a820f0fc1db7f2bf7030ca149ea7b556ceacd0a32d593f08a7cc01ff`，与总控提供值一致。当前 `数据/fields.csv` 有 13 列；`FIELD-PHY-CLOCK.allowed_subject_kinds` 已是 `object;component`，新增的 `allowed_requirement_target_kinds` 为 `object;component;precision_path`。AWS 冻结包内的 `structured/fields.csv` 仍是历史 12 列表头。

已完整读取任务指定的八组输入，共登记 150 个文件。旧 Google 后重签版有 486 行、478 个写授权和 8 个不写守卫。重叠裁决已固定：`AWS-SIGN-0458` 不再是 `exact_cell_write`，改为 `already_satisfied` 整行 `no_write_guard`。新签字仍为 486 行，精确统计为 477 个写授权、9 个不写守卫、0 个 `exact_cell_write`。新签字 SHA-256 为 `b9fa74876dfa4769df99277359f5c0be7dff268a353964abf1d392eb3708e4e3`。

该裁决保护两个边界：AWS 包不得重复写已经满足的 `allowed_subject_kinds`，也不得用冻结包的 12 列 `fields.csv` 覆盖当前 13 列表。逐单元格差异和裁决记录已写入 `prior-to-new-cell-diff.csv` 与 `field-contract-overlap-adjudication.csv`。

## 正在进行

正在改造严格隔离合并器和演练器，使它们接受 477/9 边界，验证 `FIELD-PHY-CLOCK` 整行不变，并显式检查 13 列合同。随后将执行三类测试：旧签字在新基线上拒绝；预置目标在任何 CSV 写入前拒绝；新签字在隔离副本正向通过当前 gate 校验器。

## 当前阻断

没有数据或授权阻断。生成脚本开发时出现过两次本地实现错误：一处把只有表头的空 CSV 当作数据行读取，另一处使用了 PowerShell 的歧义 `Where-Object` 参数组合。两处都属于脚本构造错误；修正后重签生成已经通过。正式 32 表和其他输入未发生写入。
## 演练完成

严格演练已通过。演练器通过 372 项检查，隔离合并器通过 25,467 项检查；正向执行 477 个写授权和 9 个不写守卫，默认 gate 校验器通过 106,160 项检查。旧签字在 SHA 门退出 1，预置资料卡目标在任何 CSV 写入前退出 1。正式 32 表前后哈希全部不变，临时目录已清理。当前裁决为 `accept_pending_independent_signoff`；本代理不正式合并。