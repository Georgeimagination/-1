# MI455X 正式事务独立复核

复核结论为 `accept`，签字状态为 `ready_for_formal_execution=true`。这表示冻结事务包已经具备进入正式执行的条件，不表示本次复核执行过正式合并；`formal_execution_performed=false`，正式写入次数为 0。

复核者为 `m2_mi455x_formal_transaction_independent_review`，日期为 2026-08-13。所有演练都在本目录的临时镜像中顺序运行，准备包、MI455X staging 和正式库只读。

## 签字绑定

SHA-256 是用于核对文件内容是否逐字节一致的 256 位摘要。独立签字只授权下面这组固定输入，任何一项改变都要重新复核。

| 绑定项 | 固定值 |
|---|---|
| 授权范围 | `formal_execution_of_exact_245_row_signoff_via_transaction_v1` |
| 准备包 manifest | `c679151bd00a1bc02ffe05c4a011c2e0532df095648eab2e40b9336c5aac783f` |
| 准备包成员聚合值 | `dc0a476e4d155df21bcb9a2f9c3bbec43060f027da129ef235b58150b802b23c` |
| 正式事务执行器 | `3db530494b17ca9180326f9bdd5e7a76102007ff46eb4d0da7135f221ddb6da0` |
| 提交前检查（preflight）脚本 | `05fb0d6ca1e60d0a7c23a9af920169ae6b351784bc108125eea2a6d8e18d9917` |
| 镜像演练脚本 | `7b4e27973c648fd6ff5afbbcc76c0670f004ac932ab7b8581985bd9528e37eb3` |
| 六项负向门脚本 | `b0f352a8eadcc145fbc8d01dfc84a549791b2d6659c3e32ed0cf4d969e13a879` |
| MI455X 245 行上游签字 | `196863031761221d825309387c563553187b0ee23630002aeaeed57bbbd6084c` |
| rebase 冻结清单 | `d3156e2f08b72eca66c3146a423e60264f10aa6eaa3229b684789c4b87640802` |
| 正式 32 表基线聚合值 | `f1520907d54e406e7d82f591e3799f07f569218aff0c321ac3b836b6c649da10` |
| 授权令牌摘要 | `4c71a63a07d31759982691644cb2ae2029c0ad07778f1e2ac441a7e09cb2eb91` |
| 本次独立签字文件 | `f4b27f7c64dde754431872720f270dbccbc975ee39db9487f3403e8c229049ea` |

准备包清单有 67 个成员，逐文件哈希、字节数和成员聚合值均复算一致，不匹配数为 0。上游签字共 245 行，其中 242 行是结构化主键写入，另有 3 个文件动作。受控写入覆盖 19 张表；3 个只读保护项（guard）分布在 2 张既有表；3 个新文件目标在事务开始前必须不存在。

## 独立复跑结果

静态合同在新的临时根中重新生成，通过 6,540 项检查；生成的 9 个合同文件与冻结包逐字节一致。随后以重新生成的合同对正式基线运行 preflight，通过 9,379 项检查。

三个事务场景按顺序运行，没有并行：

| 场景 | 结果 | 事务日志与回滚 |
|---|---|---|
| 正向事务 | `PASS` | 46 行事务日志（journal）；19 张表和 3 个新文件全部提交完成；候选与提交后校验均为 111,040 项 |
| 第 3 张表后注入故障 | `FAIL_ROLLED_BACK` | 14 行日志；3 次表提交后按相反顺序恢复 3 张表；没有新文件提交 |
| 第 2 个新文件后注入故障 | `FAIL_ROLLED_BACK` | 86 行日志；19 张表按相反顺序恢复，2 个新文件按相反顺序删除 |

正向候选的 32 表聚合值为 `0d94c60e136fab58941ad8d1f6b5b55a4d2013a370c9a1dc938bd8005fa57bca`。两个故障场景完成回滚后都恢复为正式基线 `f1520907d54e406e7d82f591e3799f07f569218aff0c321ac3b836b6c649da10`。事务日志中的提交顺序和逆序回滚已逐行核对。

冻结脚本定义的六个负向门也全部通过：默认拒绝、错误令牌、未完成独立签字、错误镜像标记、目标文件已存在、错误的合并前哈希。每个门都以退出码 1 拒绝请求，并命中预期错误文本；正式写入仍为 0。由此确认，正式根模式默认硬拒绝，只有完整授权链和精确基线同时满足时才会继续。

## 正式库保持不变

复核前后的 32 表清单逐项一致，清单文件的 SHA-256 都是 `f598cd6b79644918af81fe0d9b009cecee5596e49dadd211f651c293e5b89a64`。正式校验器在复核前后都通过 106,160 项检查，模式为 `gate`，两份日志的 SHA-256 都是 `54976a45522f446b1dafc74f01f83e9922cd80fb8e21f2b13c1f8bb79f9b0b75`。

三个正式文件目标仍然不存在。复核临时镜像、事务临时文件、锁和短盘符映射已经清理，残留数均为 0。本次没有改动准备包、staging 或正式库。

## 执行记录中的失败

`evidence/operator-error-log.csv` 记录了 11 次已关闭事件：10 次为模型或命令构造错误，1 次为 Windows PowerShell 处理长路径时的运行时失败。它们发生在临时镜像搭建、派生审计、只读回读和临时目录清理阶段；没有一次触发正式执行。原始场景日志、结果文件和正式基线没有因此改变，最终派生审计已从原始证据重建并通过。

本次没有用户拒绝、自动审批拒绝、审批连接失败、沙箱拒绝或远程服务错误。

## 正式执行前仍要满足的条件

正式执行只能使用本目录的 `formal-execution-accept-signoff.csv`，并核对其 SHA-256 为 `f4b27f7c64dde754431872720f270dbccbc975ee39db9487f3403e8c229049ea`。执行前还要重新确认准备包未变、正式聚合值仍为 `f1520907d54e406e7d82f591e3799f07f569218aff0c321ac3b836b6c649da10`，三个新文件目标仍不存在。

专用 `BackupRoot` 必须位于正式根之外。它要按冻结模板保存 23 个唯一条目，并生成内容完全匹配的备份 manifest；其中包括 19 张待改表、2 张 guard 表、1 个新文件不存在状态清单和 1 个候选后哈希清单。`MI455X-FORMAL-TRANSACTION-AUTHORIZATION.txt` 的六段内容也必须与冻结合同一致，结果目录必须位于 `BackupRoot` 内，故障注入环境变量必须为空。

本目录只给出独立复核证据和授权签字，没有执行正式事务。