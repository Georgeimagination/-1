# MI455X 正式事务独立复核交接

状态：`completed`

独立复核结论为 `accept`，`ready_for_formal_execution=true`。本次只操作独立复核目录和临时镜像，没有执行正式事务；准备包、MI455X staging 和正式库的写入次数均为 0。

## 输入边界

复核对象是冻结目录 `审计/子代理交接/m2_mi455x_formal_transaction_prep/`。包 manifest 的 SHA-256 为 `c679151bd00a1bc02ffe05c4a011c2e0532df095648eab2e40b9336c5aac783f`，67 个成员的聚合值为 `dc0a476e4d155df21bcb9a2f9c3bbec43060f027da129ef235b58150b802b23c`，成员不匹配数为 0。上游签字是 245 行，拆分为 242 行结构化主键写入和 3 个文件动作。

## 完成结果

静态合同独立重建通过 6,540 项检查，9 个重新生成的合同文件与冻结件完全一致；严格提交前检查通过 9,379 项。正向事务、第三张表后故障、第二个新文件后故障三个场景依次通过。正向候选通过 111,040 项检查，聚合值为 `0d94c60e136fab58941ad8d1f6b5b55a4d2013a370c9a1dc938bd8005fa57bca`；两个故障场景均按相反顺序完成回滚并恢复 `f1520907d54e406e7d82f591e3799f07f569218aff0c321ac3b836b6c649da10`。

六个冻结负向门全部通过。正式基线前后 32 个文件逐项一致，正式校验器前后都通过 106,160 项检查，三个正式文件目标仍不存在。临时镜像、事务临时文件、锁和短盘符映射已经清理，残留数为 0。

精确签字写入 `formal-execution-accept-signoff.csv`，文件 SHA-256 为 `f4b27f7c64dde754431872720f270dbccbc975ee39db9487f3403e8c229049ea`。签字绑定固定授权范围、三份事务脚本、包 manifest 与聚合值、MI455X 上游签字、rebase 冻结清单、正式基线和授权令牌摘要。

## 事件与后续

`evidence/operator-error-log.csv` 有 11 条已关闭记录，其中 10 条属于模型或命令构造错误，1 条属于 Windows PowerShell 长路径运行时失败。没有用户拒绝、自动审批拒绝、审批连接失败、沙箱拒绝或远程服务错误。所有事件都局限在复核目录；原始证据和正式库未受影响。

下一步由总控决定是否按 `EXECUTION-ROLLBACK.md` 正式执行。执行前必须再次核对本次签字哈希、23 个唯一备份条目、六段授权文件、正式基线和三个目标不存在状态。本交接不授权手工改表，也不替代正式执行前的即时检查。

主要产物为 `README.md`、`formal-execution-accept-signoff.csv`、`evidence/independent-review-summary.json`、三组场景证据、六项负向门证据、正式基线前后清单、校验日志和事件记录。最终 `manifest.csv` 枚举本目录全部持久文件。