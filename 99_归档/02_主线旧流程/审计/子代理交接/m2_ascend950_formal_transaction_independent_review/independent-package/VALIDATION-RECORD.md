# 验证记录

## 结论

事务准备包达到 `ready_for_independent_review`。这表示写集、备份合同、回滚合同和隔离演练已经固定，不表示正式合并获得授权。全部验证前后，正式 32 表聚合哈希（aggregate）都是 `f1520907d54e406e7d82f591e3799f07f569218aff0c321ac3b836b6c649da10`，8 个正式新文件目标均不存在，正式写入次数为 0。

## 静态合同与预检

静态合同通过 14,742 项检查：553 行上游签字被规范为 545 行结构化写入和 8 个新文件；结构化写集覆盖 20 张表；5 个只读保护项（guard）保护 3 个既有对象和 2 条既有关系；备份和回滚清单（inventory）有 30 行。严格预检（preflight）通过 21,385 项检查，同时复核 f152 聚合哈希、最终复核报告、签字 SHA-256、53 行暂存区清单（staging manifest）及聚合、逐表合并前（premerge）哈希、行级旧值、目标不存在状态和只读保护项。当前正式库在硬门（`gate`）模式通过 106,160 项检查。

候选结果基准（candidate oracle）连续两次通过。生命周期授权后的候选在硬门（`gate`）模式通过 115,509 项检查，32 表聚合哈希为 `bcb16e932019c1625e9b5a5309a37f569c0bb230b7c921a589dd3f7175632b8c`。20 张表后哈希清单 SHA-256 为 `b84ce89dbdf96accb7861e61cedbfb33618ed8793d33188030f7ef51f15efe73`。

签字中的 115,461 项对应 staging 仍为 `draft` 的临时合并。正式事务把 41 条事实及其断言提升到 `reviewed` 后，校验器多执行 41 项证据门；来源最小集选择运行（selection run）提升为 `reviewed` 后，运行本身增加 1 项、三个成员各增加 2 项，共 7 项。两者合计增加 48 项，得到 115,509。该差值只来自生命周期触发的门，不是事实或来源内容变化。

## 三场镜像事务

正向场景提交 20 张表和 8 个文件，候选与提交后校验均为 115,509 项，最终聚合哈希为 `bcb16e…`。事务日志（journal）记录 20 次表提交和 8 次文件提交，没有回滚记录。

`rollback_after_3_tables` 精确命中 `INJECTED_TEST_FAILURE_AFTER_3_REPLACEMENTS`。事务日志记录 3 次表提交，随后按提交顺序的严格逆序恢复 3 张表；镜像回到 f152。

`rollback_after_2_payloads` 精确命中 `INJECTED_TEST_FAILURE_AFTER_2_PAYLOADS`。事务日志记录 20 次表提交和 2 次文件提交；回滚先按逆序删除 2 个文件，再按逆序恢复 20 张表；镜像回到 f152。三场结束时，镜像临时文件和锁均为 0，正式根保持 f152，正式新文件目标均不存在。保存的结果目录包含原始事务日志、候选校验、提交后校验和事务结果；体积较大的镜像目录已安全删除。

## 拒绝门与异常记录

六个负向门均按预期以非零退出：默认正式根拒绝、错误令牌、`pending / false` 独立签字、错误镜像标记、预存新文件目标和错误合并前哈希。负向套件结束后，正式聚合哈希仍为 f152，正式目标仍不存在。

`operator-error-log.csv` 记录 12 个已识别问题，包括 PowerShell 命令构造、UTF-8 BOM 兼容、旧校验计数假设和正向场景回滚断言。它们分别标为 `model_operator_mistake` 或 `implementation_error`；本任务没有用户拒绝、自动审批拒绝、审批连接失败、沙箱拒绝或远端服务错误。所有问题均有后续通过证据，未降低冻结结果的验证范围。

根 `README.md` 和 `AGENTS.md` 已只读检查。准备包没有改变项目范围、正式状态、运行方式或全局约定，因此不修改这两份根文档。
