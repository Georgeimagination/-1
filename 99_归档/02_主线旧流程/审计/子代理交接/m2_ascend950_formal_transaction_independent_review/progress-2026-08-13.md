# Ascend 950 正式事务独立复核进度

## 状态

状态为 `completed / accept`，`ready_for_formal_execution=true`。本次独立复核只写本目录，没有修改事务准备包、Ascend 950 暂存区、正式 32 表、资料卡或网页快照。

## 复核结果

准备包 `manifest.csv` 的 SHA-256 为 `91af02c00a0e75854473503628ba49c4c3b062ffaca25199486cdb01fcb7c316`。66 个成员均存在，逐文件 SHA-256 和字节数一致，成员聚合为 `a83dc6f86c06dc44daa70a76525f2ea2dc3e4047eca1e3b15bea09245f42d418`。

上游签字有 553 行，其中 545 行为结构化生命周期写入，另外 8 行绑定 3 张资料卡和 5 份官方 HTML 快照。写集覆盖 20 张表；5 个只读保护项覆盖 3 个既有对象和 2 条既有关系。严格预检通过 21,385 项检查，正向候选和提交后镜像均通过 115,509 项检查，候选聚合为 `bcb16e932019c1625e9b5a5309a37f569c0bb230b7c921a589dd3f7175632b8c`。

两个故障注入场景都完成严格逆序回滚，六个负向门全部通过。复核结束时，正式 32 表聚合仍为 `f1520907d54e406e7d82f591e3799f07f569218aff0c321ac3b836b6c649da10`，8 个正式新文件目标均不存在，临时文件和事务锁均为 0。

## 交接

正式执行签字保存在 `formal-execution-signoff.csv`，SHA-256 为 `b40f064127a7ac2c5e8de9b4728501381974b661373e9cf5c2dd46954dde2394`。总控执行前仍须建立并核验正式根之外的 24 项备份和授权标记；任何绑定值变化都会使本签字失效。
