# 第三波正式合并后的文档同步计划

状态：已完成只读审计，等待总控按顺序执行修正与验收。

审计日期：2026 年 8 月 13 日。

审计范围包括 `README.md`、`AGENTS.md`、`研究计划.md`、`资料卡/型号索引.md`、四份进度记录、三个工作包的最终复核与精确签字、组合事务准备和独立签字、外置事务结果，以及正式 32 表和新增资料文件。本次只写本交接文件，没有改动正式数据、资料卡、根文档或进度文件。

## 当前裁决与更新顺序

第三波组合正式事务本身已经成功。事务从 32 表聚合值 `f1520907d54e406e7d82f591e3799f07f569218aff0c321ac3b836b6c649da10` 提交到 `5f62191bfc737535802cd6e0524b1fdf7a742ca36300db11b5a9233d4540052a`，向 20 张正式 CSV 写入 1,057 个已签字主键，11 项不写守卫全部成立，15 个载荷全部落到冻结目标。事务前预检通过 9,656 项，候选和提交后正式校验均通过 125,619 项，`transaction-outcome.json` 为 `PASS`，没有使用回滚。

正式数据的完整性可以接收，但整个第三波工作包暂时不能标为 `closed`。目前有两项收尾问题：五张正式资料卡仍带有“草稿、待复核、尚未合并”等旧阶段文字；两份已正式复制的 AMD 产品 PDF 尚未登记到资料池清单，导致 `Test-SourcePool.ps1` 新鲜运行出现 4 项失败。总控应先修正五张卡，再同步 PDF 与网页清单、汇总统计、资料总览和校验器基线；随后由合并后独立复核者确认这些问题关闭，最后生成总控验收文件并更新根文档和进度记录。

## 正式库的新基线

| 项目 | 合并前 | 合并后 | 变化 |
|---|---:|---:|---:|
| 对象 | 78 | 78 | 0 |
| 对象关系 | 27 | 27 | 0 |
| 规范事实 | 678 | 798 | +120 |
| 逐来源断言 | 680 | 832 | +152 |
| 字段要求 | 863 | 1,059 | +196 |
| 来源家族 | 83 | 92 | +9 |
| 内容版本 | 84 | 93 | +9 |
| 访问入口 | 138 | 153 | +15 |
| 最小来源选择运行 | 8 | 11 | +3 |
| 选择成员 | 96 | 106 | +10 |
| 已验收资料卡 | 40 | 45 | +5 |

11 次选择运行中，10 次为 `reviewed`、1 次为 `superseded`；106 个成员均为 `reviewed`。当前来源筛选状态为 79 个 `selected`、10 个 `redundant_covered`、2 个 `pending`、3 个 `out_of_scope`、2 个 `rejected_unreliable`、2 个 `inaccessible` 和 3 个 `lead_only`。注册表仍有 324 个列定义和 488 个枚举值。

三个工作包的内容增量应分别写清：MI455X 模组包为 39 条事实、53 条断言、44 条字段要求、1 张卡和 4 个最小来源成员；MI350P PCIe 卡包为 40 条事实、49 条断言、46 条字段要求、1 张卡和 3 个最小来源成员；Ascend 950 物理包为 41 条事实、50 条断言、106 条字段要求、3 张卡和 3 个最小来源成员。

## 15 个载荷的准确类型

冻结的 `new-file-targets.csv` 与事务日志表明，15 个载荷由 5 张资料卡、8 份 HTML 网页快照和 2 份固定官方 PDF 组成。分包情况如下：

| 工作包 | 资料卡 | HTML 快照 | PDF | 合计 |
|---|---:|---:|---:|---:|
| MI455X | 1 | 1 | 1 | 3 |
| MI350P | 1 | 2 | 1 | 4 |
| Ascend 950 | 3 | 5 | 0 | 8 |
| 合计 | 5 | 8 | 2 | 15 |

用户授权时把 10 个来源文件统称为“10 份官方网页快照”。这是一处类型表述误差。被冻结的 15 目标、三个数据签字、组合事务签字、正式 endpoint 路径和外置备份从执行前就一致指向 8 份 HTML 与 2 份 PDF，事务没有临时增加目标或改变路径。因此验收文件应如实记录“5 卡 + 8 HTML + 2 PDF”，并说明授权中的“10 份网页快照”应理解为 10 个已冻结来源文件的概括性误称。后续根文档不得继续写成“5 卡 + 10 网页快照”。

## 资料池和网页清单同步

两份新增 PDF 已独立复算哈希、大小和页数：

| 文件 | 字节数 | 页数 | SHA-256 |
|---|---:|---:|---|
| `论文/AMD_Instinct/02_厂商产品资料/2026_AMD_Instinct_MI455X_GPU_Brochure.pdf` | 659,374 | 2 | `6555fef1399a35f472e325dc61936d293e129735ec1e191d5fd0f3b6fa0c4208` |
| `论文/AMD_Instinct/02_厂商产品资料/2026_AMD_Instinct_MI350P_PCIe_Card_Brochure.pdf` | 782,486 | 2 | `a4e93edffc6c2a03668eef72b84d85e1d2586afb331c2228595d1549609afb29` |

实盘资料池因此为 113 份 PDF、315,363,681 字节和 2,214 页；AMD Instinct 从 4 份增至 6 份。旧清单中的 111 行、313,921,821 字节和 2,210 页仍逐项匹配，当前失败来自两份新文件未列清单，不能解释为旧文件损坏。

总控应把两行追加到 `清单/论文PDF清单.csv`，把 `清单/汇总统计.json` 更新为 113 个唯一 PDF、114 次来源文件出现、1 个精确重复、AMD 6 份，并新增或更新“AMD Instinct / 厂商产品资料”2 份这一分类。历史纸面清单的 112 条记录和 11 条待补资料口径没有因这两份产品简报而改变，不能把它们误算成历史论文清单下载完成项。

第三波正式表登记了 10 个带远端 URL 的来源入口，其中华为 H-2 keynote 与 H-12 Atlas 350 上市页已经存在于 `清单/网页与在线资料.csv`，其余 8 个入口不存在。只追加这 8 个缺项后，网页清单应由 914 行变为 922 行；区分大小写的 URL 数由 893 变为 901，忽略大小写时由 892 变为 900。不要重复追加已经存在的两个华为入口。`汇总统计.json` 的 `combined_online_urls` 应改为 901，并建议增加一个明确的项目执行期新增入口计数，避免继续把历史两个目录的 URL 数和执行期新增入口混在同一来源字段里。

`参考资料总览.md` 的启动快照 102 份仍是历史事实，应保留。只更新“增量维护状态”和“文件说明”：当前值改为 113 份、315,363,681 字节、2,214 页，AMD 改为 6 份，总增量改为 +11；网页清单改为 922 行、901/900 个入口，并补充第三波 8 份 HTML 快照和 2 份产品简报。

完成四个清单文件的精确编辑后，更新 `scripts/validation/Test-SourcePool.ps1` 中四份清单的 SHA-256 绑定，以及 PDF 113、网页 922、总字节 315,363,681、总页数 2,214、URL 901/900、汇总 113/901 等预期常量。新鲜校验应达到 113/113 文件路径、大小、哈希和页数全部匹配，只保留既有的一条 Google TPU PDF 解析警告。

## 五张资料卡要删除的旧阶段文字

结构化事实和卡片关键技术语义没有发现新的阻断项。需要修正的是卡片正文中的生命周期描述：

| 卡片 | 当前旧文字位置 | 建议修改 |
|---|---|---|
| `资料卡/AMD/封装/AMD_Instinct_MI455X_模组资料卡.md` | 第 4、151、207、221 行附近仍写草稿、待独立复核、`needs_review` 或不构成正式合并 | 状态改为已验收；虚拟化条目改为本包未结构化、以后按明确字段需求另建；证据完整度改为 `reviewed`；末段记录已完成最终复核和正式合并 |
| `资料卡/AMD/产品/AMD_Instinct_MI350P_PCIe_卡资料卡.md` | 第 4、190、205 行附近仍写待最终复核、`needs_review` 和 staging 草稿 | 状态改为已验收；证据完整度改为 `reviewed`；末段记录正式入库 |
| `资料卡/华为昇腾/封装/华为_Ascend950_共享裸片资料卡.md` | 第 4、73 行附近仍写草稿、待最终复核和正式库未合并 | 改为已验收，并保留共享裸片主体归一化使用 `inferred` 的证据边界 |
| `资料卡/华为昇腾/封装/华为_Ascend950PR_封装资料卡.md` | 第 4、74 行附近仍写草稿、待最终复核和正式库未合并 | 改为已验收；继续说明 `available` 只限嵌入已上市 Atlas 350 的条件 |
| `资料卡/华为昇腾/封装/华为_Ascend950DT_封装资料卡.md` | 第 4、72 行附近仍写草稿、待最终复核和正式库未合并 | 改为已验收、观察状态；保持 `announced` 与可用日期 `pending_verification` |

这些修改只能清理阶段状态，不得改变事实值、单位、来源定位、推断模式、缺口状态或对象层级。修改后重跑正式数据校验，并对五张卡做链接、公式分隔符、旧状态短语和自然化检查。

## 根文档和进度记录的精确落点

### `资料卡/型号索引.md`

开头改为 29 张架构卡、3 张 M1 试填卡和 13 张实现或系统卡，共 45 张。厂商汇总中，华为昇腾由 1 张增至 4 张，AMD 由 6 张增至 8 张，合计由 40 改为 45。华为部分加入共享裸片、950PR 和 950DT 三行；AMD 部分加入 MI455X 模组与 MI350P PCIe 卡两行。卡片状态须使用验收后的状态，不能保留“待复核”。

### `README.md`

“当前阶段”补充第三波正式事务与合并后验收；“已有资料”同步 113 份 PDF、AMD 6 份、8 份第三波 HTML 快照和 2 份产品简报；正式库摘要改为 78/27/798/832/1,059、45 张卡、11 次选择运行和 106 个成员，校验值改为 125,619，32 表聚合值改为 `5f62191bfc737535802cd6e0524b1fdf7a742ca36300db11b5a9233d4540052a`。阅读顺序加入第三波正式合并验收文件。生命周期段写清 1,057 个签字主键、20 表、15 载荷、11 守卫和未使用回滚。

### `AGENTS.md`

“当前状态”“已完成内容”“已知问题”“目录与主要文件职责”同步第三波与资料池新基线。已完成内容应分别给出三个包的事实、断言和要求数量，并采用 5 卡、8 HTML、2 PDF 的准确说法。已知问题保留 MI455X 实际可用日期和功耗等公开缺口、MI350P 发布或首次可用日期缺口、Ascend 950DT 可用日期待验证以及 PR 状态的嵌入条件，删除“第三波尚待正式合并”一类旧状态。

### `研究计划.md`

首页当前状态和末尾执行摘要同步第三波、45 张卡和新正式库计数，资料截止日及 2.2 节当前主样本上限对齐到 2026 年 8 月 13 日。6.3 节和阶段 3 的检索步骤加入下文 `DEC-030` 的停止条件。计划中的历史 M1、第一波或 AWS 节点计数都有明确时间锚点，不得为了统一当前数字而覆盖这些历史验收值。

### `进度/当前状态.md`

删除“正式库尚未写入”“用户尚未明确授权”“当前唯一停点”等已失效文字。当前结论改为 45 张卡；基线同步正式库和资料池新计数；“已正式合并的工作包”加入三个第三波包；“下一停点”改为第三波已关闭后的真实队列。若合并后复核尚未签署 `closed`，则应明确写“正式数据事务已接收，卡片与资料池收尾待关闭”，不能继续沿用授权阻断。

### `进度/任务台账.csv`

五张卡和资料池收尾通过后，把三个 `M2-W3-*` 行改为 `completed`，完成日期为 2026-08-13，清空审批阻断与旧 next step；验证列分别写包内计数、组合事务 PASS、125,619 项和最终验收路径。建议新增 `M2-W3-BUNDLE-FORMAL-MERGE`，单独保存 f152 到 5f6219 的原子事务、1,057 个主键、20 表、15 载荷、11 守卫、外置 24 项备份和未回滚结果；再新增 `SOURCE-POOL-W3-SYNC`，保存 113/113 资料池与 922 行网页清单的同步验证。这样后续恢复时不会把产品包内容验收、正式事务和资料池维护混成一个状态。

### `进度/里程碑.md`

在 M2 阶段节点新增一条已勾选记录，写明三个工作包、5 张卡、120 条事实、152 条断言、196 条字段要求、10 个最小来源成员、事务聚合值与 125,619 项校验。另加资料池同步节点，写 113 份、315,363,681 字节、2,214 页及网页清单 922/901/900。第一波“111 份 PDF 校验通过”和 AWS 节点的旧计数是当时基线，应继续保留。

### 新增范围收缩决定

用户进一步把后续主线收缩到训练或推理芯片本体。建议在 `进度/决策记录.md` 追加 `DEC-031：主线仅覆盖 die/package 与芯片内部事实`，状态为生效，并同步修改 `研究计划.md` 的项目目标、产品范围、对象层级、完成标准、工作队列和检索边界。

新的主线只把 `die`、`package` 及芯片内部的计算、数值路径、存储、数据搬运和片内或封装内互联计入资料卡完成度。`architecture_generation` 继续作为解释芯片内部机制和复用事实的支持证据，但不再单独计入主线完成度。`card`、`module`、`cloud_accelerator`、`cloud_instance`、`server`、`rack` 和 `pod` 退出后续主线；这些对象的既有正式记录作为历史已验收资料保留，不删除、不反向改写，也不继续为其缺口补资料。只有在辨认芯片身份、封装边界或解释芯片内部事实所必需时，才把系统或部署材料作为辅助来源读取，并且不得把卡、实例、服务器或机架聚合值写回芯片。

这项范围收缩会实质改变当前项目目标和 M2 完成条件，不能只写一条进度备注。总控还应审计 `审计/M2-*-实现对象待办.csv`、三份批次计划、Google 云端器件队列和当前任务台账，把退出主线的待办标为 `out_of_scope_by_DEC-031` 或等价的正式状态，避免以后恢复上下文时重新启动。已完成的 MI455X module、MI350P card、AWS 实现卡、Trainium2 实例卡和 Helios rack 卡不因新决定撤销验收；它们只不再作为后续主线覆盖率的分母。第三波 Ascend 950 die/package 仍属于主线。
### `进度/决策记录.md`

追加 `DEC-030：官方一手来源覆盖后停止媒体扩搜`，状态为生效。决定写成：官方论文、固定规格书、精确产品页或开发文档已经覆盖对象身份和当前工作包目标字段时，停止媒体报道的广泛检索与重复抽取；只有正式字段要求留下明确缺口，而且媒体提供独立实测、直接采访或官方资料没有的部署、供货状态时，才围绕该缺口定向使用。媒体若只是复述官方数字，保留线索或标为 `redundant_covered`，不新增规范事实、独立证据计数或最小来源成员。

这项决定同时写入 `研究计划.md` 6.3 节和阶段 3：工作包在转向媒体前要先列出具体 `requirement_id`、缺口状态和所需证据类型；没有明确缺口时停止扩搜。这样既保留第三方独立测量的入口，也避免在官方证据已经闭合后继续消耗时间制造重复断言。

## 总控验收文件

建议创建 `审计/M2_第三波_MI455X_MI350P_Ascend950_正式合并验收.md`。文件至少记录：

1. 三个内容终审、三个单包事务签字、组合事务独立签字和用户明确授权；
2. 授权文字中的类型误称及冻结目标为何仍然精确一致；
3. f152 到 5f6219 的正式事务、1,057 个主键、20 表、15 载荷、11 守卫、外置 24 项备份和 `rollback_used=false`；
4. 三个包的内容计数、五张卡和十个选择成员；
5. 五张卡旧阶段文字的修正与复核；
6. 两份 PDF、八份 HTML、PDF/网页清单和 `Test-SourcePool` 的最终通过结果；
7. 正式数据校验 125,619 项、卡片链接、Markdown 公式分隔符、自然化检查和根文档同步；
8. 合并后独立复核的最终 `accept / closed` 裁决与仍保留的事实缺口。

只有这些项目全部关闭后，三个工作包和组合事务才能在台账中写为 `completed`。

## 最后验证

建议按以下顺序执行，避免根文档先于事实状态更新：

1. 修改五张资料卡并检查旧阶段短语；
2. 追加两条 PDF 和八条缺失 URL，更新汇总统计、资料总览和资料池校验器基线；
3. 重跑 `Validate-ResearchData.ps1`，预期 125,619 项通过；
4. 重跑 `Test-SourcePool.ps1`，预期 113/113 通过并只保留既有解析警告；
5. 完成合并后独立复核，取得 `accept / closed`；
6. 更新型号索引、README、AGENTS、研究计划和四份进度记录；
7. 生成总控验收文件，复核所有链接、数字、哈希、公式分隔符和旧状态残留；
8. 对新增或改写的中文文档依次运行 `report-humanizer` 和 `shuorenhua` 保真回读。

审计中有一次把 statement-form `foreach` 直接接到管道的 PowerShell 解析错误，以及一次假定事务结果位于错误目录的读取失败。两者都是模型或操作者的命令构造错误，均发生在只读复算中；修正后已取得完整结果，没有写入正式库，也没有降低本计划中的计数或裁决。没有用户拒绝、自动审批拒绝、审批连接失败、沙箱拒绝或远端服务错误影响本次文档审计。