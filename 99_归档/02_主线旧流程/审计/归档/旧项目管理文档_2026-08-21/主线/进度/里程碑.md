# 里程碑

## M0：项目基础

状态：已通过

进入条件：用户确认研究计划并要求开始执行。

退出条件：

- [x] 根目录 README、AGENTS 和研究计划存在；
- [x] 项目目录和长任务记录结构存在；
- [x] 字段字典和资料卡模板有可审查初稿；
- [x] 数据模型通过总控审查：统一对象层、唯一事实层、逐来源断言和七目标外键已经确认；
- [x] 正式空表和校验脚本建立；M0 的 32 张表、323 个注册列、118 个字段和 71 组枚举通过空库与故障注入检查；M2 字段合同迁移后注册列为 324 个、字段为 120 个；
- [x] 资料池基线审计完成，并由总控复算数量、总字节、总页数、URL 去重口径和基线哈希；
- [x] 七条产品线候选型号普查完成，64 个官方入口和动态状态经过总控抽查；
- [x] 型号普查已拆成 173 个一行一对象的候选记录，临时标识、层级、范围状态、来源键和关系提示经过总控复算；
- [x] 三张试填卡的对象确认并导入正式对象表：H100 SXM5 80 GB、Trainium2 芯片/实例/UltraServer、MLU590 芯片与 H8/M9 板卡候选。

## M1：三类试填

状态：已通过

验收日期：2026 年 8 月 12 日。验收记录见 `审计/M1_试填合并验收.md`。

进入条件：M0 通过。

退出条件：

- [x] H100 SXM5 80 GB 资料卡完成并核对固定版官方资料；
- [x] Trainium2 资料卡完成，芯片、实例和 UltraServer 层级分开；
- [x] MLU590 资料卡完成，未公开、未找到和第三方线索分开；
- [x] 三张卡的关键事实全部可追溯；
- [x] 来源筛选规则能处理官方资料充分、云端对象和低披露对象；
- [x] 模板、字段和表结构根据试填结果定稿；
- [x] 试填卡经过独立复核，阻断项修正后再次验收；
- [x] 反向移除运行、成员理由和覆盖关系进入正式表；
- [x] 正式数据与资料池校验通过。

M1 验收摘要（2026 年 8 月 12 日当时基线）：正式库为 14 个对象、13 条关系、253 条事实、239 条断言和 265 条字段要求；三张卡各有九域完整度。全局校验通过 39,519 项检查，资料池 105 份 PDF 基线通过。2026 年 8 月 13 日的 Trainium2 三实例复核又发现 S10 源文扩写和网络字段语义错误；这些受影响事实已进入独立修复包，在再次验收前不能用于结论。M2 当前对象与校验数见本文件下一节和 `进度/当前状态.md`。

## M2：七厂商批量建卡

状态：旧范围停止扩展；已完成内容保留

进入条件：M1 通过。

阶段节点：

- [x] 七厂商批次计划完成；
- [x] 56 行高风险对象候选完成身份核对、独立复核和总控验收；
- [x] 正式对象 ID 冻结到 71 个，20 条关系通过 40,237 项校验；
- [x] `M2-GHC-ARCH` 通过独立复核并正式合并：4 张架构卡、35 条事实、7 个最小来源成员和 46 条实现对象待办进入正式项目；
- [x] `M2-GA-ARCH` 与 `M2-NA-ARCH` 关闭语义和字段阻断，分别取得独立复核 `accept` 并正式合并；
- [x] 第一波三个架构工作包全部完成；正式数据 91,190 项检查通过，111 份 PDF 的资料池校验通过；
- [x] 第二波物理实现、云配置和系统工作包完成排队、准备度审计与独立红队；`DEC-023` 已按复核结果修订生效；
- [x] AWS 四个单器件对象通过独立身份门并按 `package / needs_resolution` 正式预留，四条架构关系同步进入正式表；该节点验收时为 75 个对象、24 条关系，91,286 项检查通过；
- [x] Google 三层对象模型通过独立复核并形成 `DEC-024`；
- [x] 1,924 行生命周期迁移显式清单经独立签字后正式执行，语义列与其他列零变化；正式校验 91,827 项检查通过；
- [x] `M2-W2-AMD-HELIOS-SYSTEM` 通过修正、最终独立复核和总控验收：1 张系统观察卡、18 条事实、22 条断言、17 条字段要求及 3 个最小来源成员进入正式项目；
- [x] Helios 包 97 行生命周期迁移与 5 行未决链边界通过独立清单验收；该节点验收时正式库为 632 条事实、622 条断言和 803 条字段要求，93,799 项检查通过；
- [x] AWS 四代物理对象事实包完成迁移后重新签字、事务脚本独立复核、外置备份、正式提交和合并后独立复核：正式新增 57 条事实、69 条断言、71 条字段要求、4 张封装实现卡、A01 至 A08 八源最小集和 11 份官方网页快照；该节点验收时正式库为 678 条事实、680 条断言、863 条字段要求和 40 张卡，验证器通过 106,160 项检查，裁决为 `accept / closed`；
- [x] 全库字段主体合同完成修正、独立签字、正式迁移和迁移后复核：132 行收口为 12 行扩合同、12 行迁主体、108 行保留；4 条 Trainium2 路径完成迁移，事实/要求合同及同目标事实闭环已成为正式硬门，三种模式均通过 97,920 项检查；
- [x] Trainium2 纠错包删除 10 条无证据 S10 事实和错误的 UltraServer 3.2 Tbps 主体链，改正 3XL 来源与网络字段，经独立复核和 17 行生命周期签字后正式合并；后置清单又完成 2 次选择运行和 23 个成员的状态迁移；该节点验收时正式库为 621 条事实、611 条断言和 792 条字段要求，93,179 项检查通过；
- [x] Google v5e、v5p、v6e 云端器件范围通过独立复核，三个 `cloud_accelerator / needs_resolution` 对象和三条架构关系正式预留；该节点验收时为 78 个对象、27 条关系，93,251 项检查通过；
- [x] Google 云端器件来源修正版通过独立复核并有限接收：正式新增 1 个 family、1 个 source、6 个 endpoint 和五份 renderer 提取；G15 原始响应体、正式 selection、facts 与 cards 继续单独跟踪。
- [x] 第三波组合事务完成正式写入：20 张表、1,057 个签字主键和 15 个载荷全部匹配，未回滚；合并后校验通过 125,619 项，32 表聚合值为 `5f62191bfc737535802cd6e0524b1fdf7a742ca36300db11b5a9233d4540052a`。

2026 年 8 月 14 日用户把主线收紧为训练与推理芯片本体。下面的旧退出条件不再作为继续扩展模组、卡、云配置或系统对象的依据；这些对象保留历史数据，但退出新主线。

退出条件：

- [x] 每条产品线的主样本、观察项、历史锚点和排除项确定；
- [ ] 所有主样本有资料卡或明确阻塞；
- [ ] 原子事实、来源映射、冲突和缺口同步写入；
- [ ] 厂商内一致性检查通过；
- [ ] 高风险卡经过独立复核。

## R1：芯片主线重置

状态：已完成（2026-08-17）

进入条件：DEC-030、DEC-031 生效，第三波正式事务状态已核清。

退出条件：

- [x] 七个厂商的 49 个训练与推理芯片对象正式冻结，唯一入口为 `清单/训练与推理芯片名单.md`；
- [x] 早期 173 行候选中的 22 个 `package` 候选全部复核，遗漏芯片得到补裁；
- [x] 每个入选芯片有稳定标识、对象层级、时间边界、纳入依据和官方身份来源；
- [x] 共享裸片、封装变体和产品命名的计数口径明确；
- [x] 不同代理完成独立复核并签字，77 行修订稿裁决为 `accept_for_user_confirmation`，随后取得用户确认；
- [x] 逐芯片工作按唯一名单顺序建立，综合卡路径使用 `资料卡/<厂商>/芯片/<芯片名>.md`。

当前 10 个 `chip_primary`、29 个 `architecture_evidence` 和 39 个 `out_of_scope_nonchip` 只是正式库存分类，不是 R1 的退出结果。R1 的候选全集、计数验收和独立复核已移入 `审计/归档/芯片名单冻结_2026-08-17/`。

R1 补充口径门（2026 年 8 月 20 日）：

- [x] 三组前置调研统一按“发现候选因素”使用，不直接生成芯片事实；
- [x] 芯片侧候选因素与负载变量分层，MoE All-to-All 通信量、KV Cache 迁移量等不登记为芯片属性；
- [x] 资料卡和字段字典升为 0.3，正式合同更新为 141 个事实字段、346 个列定义和 76 组枚举；
- [x] 22 个新增条件列只限定实测，负载和工作负载派生结果不设芯片完整度领域；
- [ ] 下一次 Windows 正式写入前补跑完整 PowerShell 硬门。

## M3：逐芯片数据工程与最小参考资料库

状态：可开始；恢复正式写入前仍有 Windows PowerShell 硬门待补跑

进入条件：已满足。R1 的 49 个芯片对象通过独立复核并由用户确认。

退出条件：

- [ ] 每份入选来源承担至少一项事实或必要证据角色；
- [ ] 每份被覆盖来源写明覆盖者；
- [ ] 来源家族、版本、镜像和动态页面关系清楚；
- [ ] 冲突证据和独立验证没有因最小化被删除；
- [ ] 每张资料卡只列核心证据和必要验证。
- [ ] 每个冻结芯片的字段要求、事实、断言、缺口、冲突和派生指标完成；
- [ ] 每个冻结芯片有独立、可复现的最小来源选择运行。

## M4：全量验收

状态：未开始

进入条件：M3 通过。

退出条件：

- [ ] 关键数字逐项核对原文条件；
- [ ] 派生指标全部可复算；
- [ ] 对象、精度、稀疏、带宽方向和单位检查通过；
- [ ] 所有本地文件、网页状态、访问日期和哈希可复核；
- [ ] README、AGENTS、当前状态、任务台账和决策记录同步；
- [ ] 最终交付没有提前加入训练与推理架构差异分析。
- [ ] 每个冻结芯片有一份可独立阅读的综合资料卡，关键数字绑定正式事实。

