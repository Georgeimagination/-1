# 最新芯片范围修订独立复核（2026-08-17）

复核角色：未参与本轮 CSV 修订的独立复核者  
裁决：`accept_with_fixes`

## 裁决

49 个计数对象的范围结论成立，不需要把本轮提升的 8 个对象降回观察或待决。当前 CSV 的总数、分层、对象类型和共享硅设计分组均能复算通过：38 个主样本加 11 个历史锚点，共 49 个计数对象，对应 45 个 `silicon_design_group`；其余为 4 个路线图观察对象、5 个边界待决对象和 19 个排除对象，合计 77 行。

当前文件仍有两处 CSV 行级错误和五组验收稿表述错误。它们不改变 49 个计数对象，也不改变 45 个设计组，但会把 Feynman 的正式命名状态、Rubin CPX 的物理层级和 MI455X 的 package、EAM、compute tray 关系写错。因此，本次不能给出无条件 `accept`。修正下列精确位置并重新核对后即可接收，不需要重做七厂商范围研究。

## 计数和结构复算

| 检查项 | 独立复算结果 | 判断 |
|---|---:|---|
| CSV 数据行 | 77 | 通过 |
| 主样本 | 38 | 通过 |
| 历史锚点 | 11 | 通过 |
| 计数对象 | 49 | 通过 |
| 计数对象的共享硅设计组 | 45 | 通过 |
| 路线图观察对象 | 4 | 通过 |
| 边界待决对象 | 5 | 通过 |
| 排除对象 | 19 | 通过 |

七家厂商的计数分别为 NVIDIA 11、Google 9、AWS 6、Groq 1、寒武纪 4、华为 11、AMD 7，与验收稿的完整候选名单一致。所有计数行都满足 `object_layer` 为 `die` 或 `package`；`counts_toward_chip_completion=true` 只出现在 `include_main` 和 `include_historical_anchor`，没有模组、卡、实例或系统混入分母。

12 个本轮新增或改动的行也能闭合：Rubin CPX、TPU 8t、TPU 8i、Trainium4、Ascend 960、Ascend 970、MI455X GPU package 和 MI430X GPU package 共 8 个计数行；Rubin Ultra、LP35、Feynman 世代未命名 GPU 和 LP40 共 4 个观察行。Rubin CPX 在 CSV 中已经正确落为 `die`，MI430X 已正确落为 `package`。

## 一手来源抽查

一手来源抽查没有发现需要撤销的提升对象。详细定位见[最新芯片范围修订官方证据抽查](最新芯片范围修订官方证据抽查_2026-08-17.md)。本轮与范围裁决直接相关的判断如下。

| 对象 | 复核判断 | 必须保留的限定 |
|---|---|---|
| Rubin CPX | 纳入，`die` | NVIDIA 明确写明 `monolithic die design`，并公开 NVFP4、GDDR7 和长上下文推理信息。[NVIDIA Newsroom](https://nvidianews.nvidia.com/news/nvidia-unveils-rubin-cpx-a-new-class-of-gpu-designed-for-massive-context-inference) |
| TPU 8t | 纳入，`package` | 架构和规格足够；`package` 是厂商未公开封装细节时采用的保守单器件容器。[Google Cloud](https://cloud.google.com/blog/products/compute/tpu-8t-and-tpu-8i-technical-deep-dive) |
| TPU 8i | 纳入，`package` | 官方确认每颗芯片含两个 core dies 和一个 chiplet die；不能把它简写成“两个 Tensor Core 裸片和一个 CAE chiplet”。[Google Cloud](https://cloud.google.com/blog/products/compute/tpu-8t-and-tpu-8i-technical-deep-dive) |
| Trainium4 | 纳入，`package` | 只公开相对代际目标，后续事实必须标为设计期目标。[Amazon](https://www.aboutamazon.com/news/aws/trainium-3-ultraserver-faster-ai-training-lower-cost) |
| Ascend 960 | 纳入，`package` | `package` 是保守容器；未来数值按规划目标登记。[Huawei](https://www.huawei.com/en/news/2025/9/hc-xu-keynote-speech) |
| Ascend 970 | 纳入，`package` | 官方明确部分规格仍在制定，所有定值都需保留规划条件。[Huawei](https://www.huawei.com/en/news/2025/9/hc-xu-keynote-speech) |
| MI430X | 纳入，`package` | package 身份成立；产品页性能数值来自工程预测，后续不能当成量产实测。[AMD](https://www.amd.com/en/products/accelerators/instinct/mi400/mi430x.html) |
| MI455X 内层 GPU package | 纳入，`package` | 内层 CoWoS-L 多芯片 package 有一手资料支持；每个 EAM 内含几个 package 尚未由公开资料直接给出。[AMD CDNA](https://www.amd.com/en/technologies/cdna.html) |

Rubin Ultra、LP35 和 LP40 保留观察状态合适。GTC 2026 只确认 Feynman 世代会有一颗新 GPU，没有发布该 GPU 的正式 SKU 名，因此第四个观察行应明确写成工作标签，不能声称 `NVIDIA Feynman GPU` 已被正式命名。

## 需要修正的精确位置

### CSV

1. `审计/训练推理芯片名单冻结.csv` 第 69 行，`FREEZE-AMD-016`：
   - `counting_method` 仍写着“GPU package 待用户裁决”，与 `FREEZE-AMD-015` 已转为计数的当前状态冲突。
   - `official_source_locator` 写着“EAM 为承载四个 GPU packages 的外层形态”。AMD brochure 的图示表达的是四颗 MI455X GPU 组成 compute tray，不能据此断言一个 EAM 承载四个 package。这里应把 package、EAM 和 compute tray 分成三层，并把每个 EAM 内的 package 数量记为未公开。

2. `审计/训练推理芯片名单冻结.csv` 第 77 行，`FREEZE-NV-020`：
   - `canonical_chip_name`、`time_boundary` 和 `official_source_locator` 把 `NVIDIA Feynman GPU` 写成正式命名对象。官方只正式命名了 Feynman 世代，并说明该世代会有一颗新 GPU。
   - 建议将名称改为 `NVIDIA Feynman-generation unnamed GPU (working label)` 或等价的明确工作标签；定位应改为“官方确认 Feynman 世代将有新 GPU，GPU SKU 尚未命名”。`include_observation / false / unresolved` 可以维持不变。

### 验收稿

1. `审计/训练推理芯片名单冻结验收.md` 第 8、10、16 和 54 行：把 4 个观察对象统称为“已命名对象”，并把 Feynman GPU 列为正式命名项。应改为“路线图观察对象”，同时注明 Feynman 只是世代名，GPU SKU 尚未命名。

2. 同文件第 41 行：完整候选名单把 Rubin CPX 写成 `GPU package`。应改为 `Rubin CPX monolithic die`，与 CSV 第 10 行和 NVIDIA 一手资料一致。

3. 同文件第 60 行：删除“MI455X EAM 是承载四个 GPU packages 的外层模组”。可改为“公开资料区分内层多芯片 GPU package、EAM 模组和由四颗 MI455X GPU 组成的 compute tray；每个 EAM 内的 package 数量未公开”。

4. 同文件第 64 行仍沿用旧分布“17 行进入计数、2 行观察”。当前 22 个早期 `package` 候选的实际分布是 19 行进入计数、0 行观察、1 行待决、2 行排除。第 68 至 71 行的表格已经是正确数字。

## 唯一性、外键和必填字段

77 个 `freeze_row_id` 全部唯一，`vendor + canonical_chip_name` 组合也全部唯一。50 个非空 `early_candidate_id` 均在 173 行早期候选表中恰好存在一次，且未被重复引用；13 个非空 `existing_formal_object_id` 均在 `数据/objects.csv` 中恰好存在一次。

早期候选中 `object_type=package` 的 22 行全部恰好映射一次，当前分布为 19 个计数、1 个待决、2 个排除。计数对象的 `silicon_design_group` 均非空。三个未确认名称和 MI500 Series 没有设计组，是对象尚未成立或只到系列层级造成的预期空值。核心身份、范围、计数、来源、定位、复核状态和冻结状态字段无缺失；所有官方入口均为 HTTPS，域名属于相应厂商。未发现首尾空白、重复早期 ID 或重复正式对象 ID。

12 个新增或改动行保持 `pending_independent_review`，其余 65 行保留旧复核状态；77 行的 `freeze_state` 都是 `candidate_pending_user_confirmation`。在上述修正得到回读确认前，不应把这 12 行批量改为已通过独立复核。

## 复核绑定和运行记录

本次复核读取的文件哈希如下：

- `审计/训练推理芯片名单冻结.csv`：`d5acc9ffcea2c2feaa2c47b939f7d02691ef6f9d2f522673e5c78beeba60df84`
- `审计/训练推理芯片名单冻结验收.md`：`2950153284083b4c8ef41e0a4340ad1261c077f9c2540279bec5b2a2af3e1d22`
- `审计/子代理交接/最新芯片公开身份复核_2026-08-17.md`：`428e087056d452fadfee74ec0ff8a00c6ba253a4926f19d7fdfeb6fb8edd56a4`

NVIDIA 技能目录的本地查询因环境没有安装 `npx` 而以退出码 127 失败，分类为工具运行时失败，不是用户拒绝、审批失败、沙箱拒绝或远端服务错误。随后使用 NVIDIA 官方页面和可访问的目录入口完成证据核对；该失败没有降低本次范围裁决的证据质量。

## 交接

本次只新增这份独立复核报告，没有修改候选 CSV、验收稿、README、AGENTS、进度文件或正式结构化数据。修复上述行级问题后，应重新计算 77/49/38/11/45/4/5/19、22 个早期 package 映射和文件哈希，再由未参与修复者确认。通过后方可把本轮 12 行的独立复核状态改为可提交用户确认。

## 返修回读

返修后裁决：`accept_for_user_confirmation`

2026 年 8 月 17 日再次只读检查当前 CSV 和验收稿。前述问题均已关闭：`FREEZE-AMD-016` 已把内层 GPU package、EAM 模组和 compute tray 分开，删除了“一个 EAM 承载四个 package”的无依据关系；`FREEZE-NV-020` 已改为 Feynman 世代未命名 GPU 的工作标签，且明确 GPU SKU 尚未公布。该行已经补齐 `freeze_state=candidate_pending_user_confirmation`，全表 77 行均为 24 列。

验收稿已同步使用“路线图观察对象”，Rubin CPX 已写为 `monolithic die`，MI455X 的三层关系和未公开数量边界表述准确。22 个早期 `package` 候选现为 19 个计数、0 个观察、1 个待决、2 个排除。

最终复算结果为 77 行、49 个计数对象、38 个主样本、11 个历史锚点、45 个共享硅设计组、4 个路线图观察对象、5 个边界待决对象和 19 个排除对象。77 个行 ID 与 77 个“厂商加规范名称”组合均唯一；50 个早期候选引用和 13 个正式对象引用均唯一且有效；核心必填字段、计数状态、对象层级、设计组和首尾空白检查均无错误。

返修使 `FREEZE-AMD-016` 也进入待复核状态，因此当前是 13 行 `pending_independent_review`、64 行保留旧复核状态；验收稿已按这组数字更新。77 行的 `freeze_state` 仍全部为 `candidate_pending_user_confirmation`。

本次回读绑定以下返修后内容：

- `审计/训练推理芯片名单冻结.csv`：`63b0cd6ea1c20eaa8f7fac5fe6ed2784db13efe1d325baa9b165820c3a5cd66e`
- `审计/训练推理芯片名单冻结验收.md`：`70141c261fe142981c568acc5a2c199ae8fd1d4e6d21cb2f067b967ec311178d`

当前名单可以提交用户确认。这个裁决只表示修订候选稿通过独立复核，不表示名单已经正式冻结，也不授权在用户确认前启动批量事实抽取或资料卡生产。

## 最终状态回填确认

主代理依据上述 `accept_for_user_confirmation` 裁决完成机械状态回填后，本代理再次只读检查当前 CSV。77 行均为 24 列，77 个 `freeze_row_id` 和 77 个“厂商加规范名称”组合保持唯一；49 个计数对象与 28 个非计数对象的范围状态没有变化。

77 行的 `independent_review_status` 现已全部回填为 `reviewed_accept_for_user_confirmation`，77 行的 `freeze_state` 仍全部为 `candidate_pending_user_confirmation`。没有空状态、列错位或提前正式冻结。验收稿第 89 行与这组状态一致。

最终状态回填绑定哈希为：

- `审计/训练推理芯片名单冻结.csv`：`df171b83b1be7dd747e9c78f5417ce8347c4bfcf70c31cb77f39699f44ef41ea`
- `审计/训练推理芯片名单冻结验收.md`：`9ffb78ea3f501b50a6292fdbcd26afd2748ee5ca5f1fc7fc6ee87bf76ea16083`

最终状态回填确认通过。候选名单当前可以提交用户确认，用户确认前仍不得把 `freeze_state` 改为正式冻结。
