# 训练与推理芯片候选冻结稿最终交叉检查

检查日期：2026-08-14  
复核角色：未参与候选清单编制与合并的最终交叉检查代理  
结论：`accept_for_user_confirmation`

## 复核范围

本次完整读取并以 `AGENTS.md`、`审计/新会话交接_芯片名单冻结.md`、`进度/决策记录.md` 的 DEC-030、DEC-031 为口径，交叉检查了 `审计/训练推理芯片名单冻结.csv` 和 `审计/训练推理芯片名单冻结验收.md`。同时读取早期 173 行候选表和正式对象表，用于核对早期候选 ID 与正式对象 ID。没有联网扩搜，没有开展规格研究，也没有修改名单、验收稿、进度文档、项目说明或正式数据。

## 结论

当前候选稿可以提交用户确认。未发现会阻止提交的硬错误。这里的接收只表示候选名单已通过独立复核，可以进入用户确认门；不表示名单已经正式冻结，也不授权启动规格抽取、事实生产或资料卡生产。

当前 CSV 有 73 行：41 个对象进入候选完成度分母，其中主样本 30 个、历史锚点 11 个；41 个对象对应 38 个共享硅设计组。其余为 7 个观察对象、6 个待决对象和 19 个排除对象，合计关系为 $30 + 11 + 7 + 6 + 19 = 73$。这组数字与验收稿的总表、七厂商完整名单表和边界说明一致。

| 厂商 | 历史锚点 | 主样本 | 计入合计 |
|---|---:|---:|---:|
| NVIDIA | 1 | 9 | 10 |
| Google | 2 | 5 | 7 |
| AWS | 1 | 4 | 5 |
| Groq | 1 | 0 | 1 |
| 寒武纪 | 2 | 2 | 4 |
| 华为 | 3 | 6 | 9 |
| AMD | 1 | 4 | 5 |
| 合计 | 11 | 30 | 41 |

验收稿“当前计入的完整候选名单”逐厂商、逐名称与 CSV 的 41 个计数对象一致。GB203 已纳入 NVIDIA 主样本，GroqChip Processor 已改列历史锚点；NVIDIA L2 和 H20 BFX 的底层芯片身份均保留为待决。RTX PRO 6000D 对 GB202 的身份补证也已写入当前稿。未检出旧的 40、71、4 个待决等过时基数。

## 结构与外键复算

早期候选表共有 173 行，其中 `object_type=package` 的 22 行在冻结 CSV 中全部恰好命中一次：17 行进入当前计数、2 行进入观察、1 行待决、2 行排除。50 个非空 `early_candidate_id` 全部存在于早期候选表；13 个非空 `existing_formal_object_id` 全部存在于 `数据/objects.csv`。行 ID 重复、必填字段缺失、应填排除理由为空、首尾空白均为 0。

所有计数对象的层级均为 `die` 或 `package`，没有模组、卡、云实例、服务器、机架或 Pod 混入完成度分母。所有 `official_source_url` 均为 HTTPS，且域名属于对应厂商的官方域：NVIDIA、Google、AWS/Amazon、Groq、寒武纪、华为/昇腾或 AMD。七个既定厂商无缺失，也没有多余厂商。

73 行的 `independent_review_status` 已统一回填为 `reviewed_accept_for_user_confirmation`，`freeze_state` 仍全部为 `candidate_pending_user_confirmation`。因此，文件既记录了独立复核签字，又没有把候选稿写成正式冻结，符合用户要求的两阶段确认门。

## 重点边界复核

MI455X 的两层对象已经分开：CDNA 5 XCD 计入主样本；内层 MI455X GPU package 为 `pending_user_decision`，当前不计；外层 MI455X EAM 模组为 `exclude_nonchip`，既有正式对象和历史数据继续保留。若用户同意纳入内层 package，总基数由 41 增至 42，共享硅设计组仍为 38。CSV 中相关备注已经使用最新的 41→42 口径。

MI350P 仍是 `accelerator_card / exclude_nonchip / false`，正式对象 ID 保留，没有删除或改写历史数据。Ascend 950 Die、950PR、950DT 均为 `include_main / true`：名单按三个芯片对象计数，同时都归入 `HUAWEI-ASCEND950-SHARED-DIE`，按共享硅设计汇总时只算一个组。这与 DEC-031 和用户的明确边界一致。

GH200 保持多处理器模组排除；B100、B200、B300 保持外层 SXM 模组排除。NVIDIA L2、H20 BFX、MLU580、MI308X、MI440X 以及 MI455X GPU package 构成 6 个待决对象，不进入当前分母。7 个未来观察对象也未被写成已经一般可用。

## 尚待用户裁决

用户仍需决定 MI455X 内层 GPU package 是否成为第 42 个计数对象，并确认其他 AMD OAM/PCIe 市场型号继续只由共享 GCD/XCD 承接，以及观察和待决对象是否维持当前分层。收到用户确认前，名单必须保持候选状态，任务台账继续为 `in_progress`。

## 最终校验指纹

本次签字绑定以下当前文件内容：

- `审计/训练推理芯片名单冻结.csv`：SHA-256 `51A9010C2790B48DA8C221B8954757E0F6338742BB1A95BBF1A34ED4CBE100B3`
- `审计/训练推理芯片名单冻结验收.md`：SHA-256 `386E554C3B91716A2F32F4F0DF9A879827250BA34965C2CECA1803AC67BFDA95`

如果候选名单或验收稿后续发生修改，需要重新复算哈希，并确认 41 个计数对象、73 行分类、22 个早期 package 候选闭环和用户确认门没有意外改变。

## 写入文件

本代理只写入：`审计/子代理交接/chip_freeze_final_crosscheck.md`。
