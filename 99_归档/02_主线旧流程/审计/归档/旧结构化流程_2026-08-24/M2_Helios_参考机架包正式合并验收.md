# M2 Helios 参考机架包正式合并验收

验收日期：2026 年 8 月 13 日  
工作包：`M2-W2-AMD-HELIOS-SYSTEM`  
结论：`accept`，已写入正式资料卡、结构化数据和最小参考资料库。

## 接收范围

本次接收 AMD Helios 72-MI455X 参考机架观察卡。正式对象 `OBJ-AMD-HELIOS-72-MI455X`、MI455X 对象及其关系已经存在，因此没有新增对象或关系。Helios 是 AMD 提供给 OEM（原始设备制造商）和 ODM（原始设计制造商）的机架级参考设计，不是 AMD 直接销售的机架 SKU；来源预计 2026 年下半年规模部署，正式状态继续记为 `announced`。

工作包新增 18 条事实、22 条逐来源断言、17 条字段要求和 9 条九域完整度记录。正式库由 614 条事实、600 条断言和 786 条字段要求增加到 632、622 和 803 条。相关组件、精度路径、存储层级、互联、拓扑与条件集按机架对象入库，没有把 MI455X 单模组值乘以 72，也没有把系统聚合值下放到芯片。

正式资料卡为 `资料卡/AMD/系统/AMD_Helios_72_MI455X_参考机架观察卡.md`。

## 事实边界

卡片保留 AMD 对完整机架直接给出的身份、72 个 MI455X、OCP MXFP4 与 MXFP8 理论峰值、HBM4 容量和带宽、scale-up、scale-out、网络协议、交换结构与液冷信息。3 AI exaflops 的原始厂商标签缺少数据格式和运算计数定义，相关精度路径、条件、事实、断言和字段要求共 5 行继续保持 `needs_resolution`。

“multi-plane network with all-to-all GPU connectivity”只支持多平面交换网络和 GPU 全互达。正式拓扑规范为 `switched_fabric`，不推断完全图物理直连、单跳或无阻塞。1.67 PB/s 保留为精确机架内存带宽，来源同页的 1.7 PB/s 只用于确认 peak theoretical 口径，不替换精确值。260 TB/s 与 43 TB/s 仍缺方向、有效载荷及峰值或持续值定义。

## 来源与最小集

本包新增两个一手来源版本和四个访问入口：

- AMD Helios 产品页固定快照：282,127 字节，SHA-256 `ed1642ec9ec16f9a5db4a8956b367e9859a3b2f47f9b18fcfe441fc56c34c6b7`；
- AMD Instinct MI400 产品页固定快照：318,594 字节，SHA-256 `aa5603704c4d77d1e912a5921e289ccabaa45111bab209c2ce08975aedc58a7e`。

正式快照位于 `最小参考资料库/快照/AMD/Helios/2026-08-13/`。已有 AMD CES 2026 新闻稿继续复用，没有建立重复来源版本。

选择运行 `SELRUN-M2W2-AMD-HELIOS-20260813` 经独立反向移除复核后改为 `reviewed`。Helios 页面承担对象边界、72 数量和多数机架规格；MI400 页面保留精确 1.67 PB/s 及其理论峰值定性；CES 新闻稿保留唯一的 3 AI exaflops 原词和 2026-01-05 early-look 日期。移除任一来源都会损失当前事实或日期证据。

## 独立复核与生命周期

首次独立复核提出的拓扑归一、带宽口径、MXFP 脚注、CES 来源角色和条件集预算五项问题均已修正。最终复核报告 `审计/子代理交接/m2_review_helios_system_final.md` 给出 `accept`。

冻结包共有 102 行。DEC-025 行级清单明确授权其中 97 行的 `review_status: draft → reviewed`；3 AI exaflops 未决链的 5 行没有进入清单，状态未变。清单 SHA-256 为 `263e4b176f2eb5cb73890d5d50ee8635d75e19af384ab56dbd1f03100a17b46b`，绑定的冻结包聚合 SHA-256 为 `9f87160dcafd5061abd1b01a57b5df2a63c2c662446bd3a436f2d9da5034fad5`。

合并后逐主键比较得到：102 行新增记录全部存在；97 个获准的 `review_status` 单元格发生变化；选择运行另有 1 个语义 `status` 变化；两个快照 endpoint 的 `local_path` 改为正式路径；所有既有正式行和其他新增字段均没有意外变化。

## 验证与恢复

合并前备份位于 `审计/合并备份/M2-W2-AMD-HELIOS-SYSTEM-20260813/`。40 个恢复载荷的清单 SHA-256 为 `d996fbd6b820bb9de04c574ed1b72106d7a810e91ceb378b1ab1809618bf47d6`。

真实工作目录的正式校验器通过 93,799 项检查，注册表仍为 323 列、488 个枚举值。两个新增本地 endpoint 均存在且哈希匹配，正式表没有 staging 路径。资料卡中的 18 个事实 ID 与本包正式事实全量对应。

备份脚本最初把并不存在的 `最小参考资料库/README.md` 列为必备文件；命令在复制 40 个实际目标后停止。这是操作清单错误，不是沙箱、审批或工具能力限制。主控随后核对已复制文件、生成 40 行备份清单并逐项校验哈希。一次只读路径核验也因 statement-form `foreach` 直接接管道而出现 PowerShell 解析错误；改为先保存结果后通过。两次错误均未改变合并结果。

## 验收结论

Helios 参考机架包已正式合并。观察卡继续保留公开口径缺失和未来部署边界；本次验收不把参考设计解释为已供货产品，也不进入训练与推理架构差异分析。
