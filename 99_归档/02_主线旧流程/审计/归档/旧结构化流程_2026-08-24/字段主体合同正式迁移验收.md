# 字段主体合同正式迁移验收

验收日期：2026 年 8 月 13 日  
总控裁决：`accept`  
正式状态：已完成并闭环

## 验收结论

字段主体合同已经写入正式数据模型。原先一列同时承担事实主体和资料缺口目标的含混做法已拆开：`allowed_subject_kinds` 只约束规范事实，新增的 `allowed_requirement_target_kinds` 只约束字段要求。正式校验器默认使用 `gate` 模式，把主体合同错误作为失败；`audit` 模式只用于迁移前盘点。

迁移内容与独立签字逐项一致。132 行审计结果最终分为 12 行扩合同、12 行迁主体和 108 行保留；4 条 Trainium2 字段要求已经从旧的单核精度路径迁到芯片级精度路径。迁移后的正式库有 324 个注册列、120 个事实字段、792 条字段要求；`fields.csv` 为 13 列。

## 授权与备份

本次迁移以修正版、166 行精确独立签字和只读预检为授权依据：

- 修正版：`审计/子代理交接/field_subject_contract_remediation/`
- 独立签字：`审计/子代理交接/field_subject_contract_remediation_independent_review/formal-migration-accept-signoff.csv`
- 只读预检：`审计/子代理交接/field_contract_migration_preflight/`
- 迁移后复核：`审计/子代理交接/field_subject_contract_postmerge_review.md`

独立签字共 166 行，SHA-256 为 `10a1e1f177f41ed2845ea7a6a1c28bbc375de2deb053a25a2610d6dac3fb0d02`。迁移开始前，四个正式文件和六个项目文档已复制到 `审计/合并备份/FIELD-SUBJECT-CONTRACT-20260813/`；`backup-manifest.csv` 有 10 行，冻结清单有 12 行。备份清单 SHA-256 为 `ed51485459396f69fddc31e9c092e7e0953ec750acf2dc73347ce8d83f9ab34f`，冻结清单 SHA-256 为 `ca9705458a598a48e2efb9478ae94ed88cc0bae336046f3767340acfa108abf7`。

## 正式变化

| 正式文件 | 迁移前 SHA-256 | 迁移后 SHA-256 | 允许变化 |
|---|---|---|---|
| `数据/schema-columns.csv` | `85129e57f6ab927cac43ca820d7b8df6558516f956bb1525ffca615f66654a6b` | `4c756065e0fc3fba175b0abeb41699ec637224aa4e8a5c9e150ff60730ce9375` | 新增 1 个列定义，共 11 个非空单元格 |
| `数据/fields.csv` | `7cf5442800a7e453b34bbda4ac335ccf101ce5cac2dde297be657d4f56a02cae` | `1723cd5ef441ea12c53e785a4d2e2101f81ecdc5ce10872a02152cfb6539e2ee` | 120 个要求目标合同值，加 2 个事实主体合同修正 |
| `数据/field-requirements.csv` | `e1df2c5584bfbaccdf4352ba8f7a7d2783250d9982473a3d15b649e5521bc62a` | `93c27127b6124331368e24ccec97c48620d2e85f08057d14d049d87edb91d347` | 12 行、32 个单元格 |
| `scripts/validation/Validate-ResearchData.ps1` | `66daa54b3273bbdf4f44368a9fb0791816e7e44521e645f6d47ae82f0c3d2f38` | `db0a62b32c6de8920056e1611bfab42dab0c0a78e46c9414f3e35f4e850f230d` | 整文件按已签字候选替换 |

迁移前 32 表聚合值从备份独立重构为 `97ebb17a2518e3a43b18a382dcfc879c1f5e2b08118a6149c8fa984e65017453`。迁移后聚合值为 `d10ad305a820f0fc1db7f2bf7030ca149ea7b556ceacd0a32d593f08a7cc01ff`。13 个禁止写入或只读探针保持原哈希；事实表和枚举表没有变化。

## 校验结果

正式库完成三次新鲜校验：

```text
SUBJECT-CONTRACT AUDIT: 0 issue(s); mode=audit.
PASS: 32-table research data model; 97920 checks executed; subject_contract_mode=audit.
Registry: 324 columns, 488 enum values.

PASS: 32-table research data model; 97920 checks executed; subject_contract_mode=gate.
Registry: 324 columns, 488 enum values.

PASS: 32-table research data model; 97920 checks executed; subject_contract_mode=gate.
Registry: 324 columns, 488 enum values.
```

迁移后独立复核裁决为 `accept`，报告 SHA-256 为 `3b8f560419841a4ad5d5882833a754e9a80b951855b1bdfdfcae7090e3e10166`。复核重新检查了 10 行备份清单、166 行签字、13 个禁止写入探针和三种校验模式，没有发现未授权变化。

## 执行记录

执行中出现过三类命令或模板构造错误：首次生成冻结清单时把反斜杠误当成无效正则，验收稿首次生成时误用了会解释 Markdown 反引号的 PowerShell 双引号模板，另有一次只读字符串替换选错重载。它们都属于总控操作错误，不是沙箱拒绝、审批失败或工具能力限制。异常文件在验收前已重建，控制字符和未展开模板的复查结果均为 0；正式四文件的签字哈希、13 个禁止写入探针和三次校验结果不受影响。

## 后续边界

字段合同迁移改变了正式基线。AWS 四代物理对象事实包此前的签字和演练因此不能直接执行，必须在当前 13 列 `fields.csv` 和新校验器上重新签字、重新演练。旧 AWS 签字中对 `FIELD-PHY-CLOCK.allowed_subject_kinds` 的写入已经由本次迁移满足，后续只能把它作为“当前值必须保持不变”的守卫，不能重复写入或用旧字段表覆盖。
