# M2 第一波收尾一致性复核

复核日期：2026 年 8 月 13 日

## 结论

第一波三个架构包的事实范围和来源选择结论没有改变。收尾复核修正了正式元数据里的旧阶段说明，建立统一卡片入口，并重新跑通结构化数据与资料池校验。第二波可以在此基线上排队。

正式库仍有 71 个对象、20 条关系、614 条事实、600 条逐来源断言和 786 条字段要求。资料池仍为 111 份内容去重的 PDF（Portable Document Format，便携式文档格式），共 313,921,821 字节、2,210 页。本次没有删除来源，也没有改写架构事实、断言原文、来源成员或冲突值。

## 修正内容

`数据/objects.csv` 中 20 个第一波架构对象还写着“工作包预留”或“事实尚待抽取”。这些对象已经验收，旧说明会误导第二波代理，因此只更新 `notes`；对象标识、类型、厂商和 `review_status` 均未变化。修改前文件保存在 `审计/合并备份/OBJECT-NOTES-20260813/`，原文件 SHA-256（256 位安全散列算法）为 `31b295e227ac0d4e565984f2a7988048c4c2d8b650734ca7256cce1a5e45717c`。

`最小参考资料库/selection-runs.csv` 的 Google/AWS 与 NVIDIA/AMD 两次运行仍带有“待总控合并”的旧说明。两行 `notes` 已改为正式接收后的状态；运行标识、成员、算法、截止日和复核状态均未变化。修改前文件保存在 `审计/合并备份/SOURCE-SELECTION-NOTES-20260813/`，原文件 SHA-256 为 `4d0820be0d67149f944c6369b8eedbd436fbf7d050385d98f88b5a063aaef8d7`。

新建的 `资料卡/型号索引.md` 汇总 29 张架构卡与 3 张 M1 试填卡。32 个卡片链接逐一检查，均指向现有文件；观察对象、历史锚点和 MLU590 供货待核状态没有因建立索引而改变。

## 验证

结构化数据校验通过 91,190 项检查，字段和枚举注册表仍为 323 个列定义、488 个枚举值。资料池校验核对 111/111 个 PDF 的路径、大小、哈希和页数并通过，保留一条既有 Google TPU（Tensor Processing Unit，张量处理单元）文档交叉引用警告。

`资料卡/型号索引.md` 通过 report-humanizer 机器扫描，并按 shuorenhua 的保真要求回读对象标识、数字、单位、路径和责任归属。根 README、AGENTS、研究计划、当前状态、决策记录与参考资料总览也已同步；扫描器对历史项目名和绝对路径的提示属于必须保留的项目标识，不作删除或改名。

## 过程异常

本次没有用户拒绝、自动审批拒绝、沙箱拒绝或远端服务错误。三次校验脚本构造问题属于操作失误：一次把“未搜索到旧文本”的正常退出码当成失败，一次因 PowerShell 双引号转义暂时丢失 Markdown 反引号，一次按标量读取单行 CSV（Comma-Separated Values，逗号分隔值）时误判 `Count`。三处都在写入后立即回读并修正，正式文件和验证结果未受影响。