# 正式库四表生命周期迁移验收

验收日期：2026 年 8 月 13 日  
状态：已通过并执行  
变更范围：1,924 行 `review_status: draft → reviewed`

## 执行依据

迁移清单位于 `审计/子代理交接/data_review_lifecycle_migration/migration.csv`，共 1,924 行，SHA-256 为 `E2D7F62795DE4DBAB1985BE057F3B2EBFDC750F775AFD3A4773FB7A278CD2A54`。清单按正式主键列出表名、批次、旧状态、新状态、必须保留的语义状态和验收依据。

`审计/子代理交接/data_review_lifecycle_migration_independent_review.md` 由未参与清单生成的代理完成。复核者独立重建四批次白名单，检查 16 个“批次 × 表”单元格和 48 行分层样本，裁决为 `accept`，明确授权总控执行迁移。四张正式表在执行前的哈希与独立复核绑定值完全一致。

## 实际变更

| 正式表 | 行数 | `review_status` 变化 | 迁移后状态分布 |
|---|---:|---:|---|
| `最小参考资料库/fact-assertions.csv` | 600 | 598 | `reviewed=600` |
| `数据/facts.csv` | 614 | 554 | `reviewed=556`，`needs_resolution=58` |
| `数据/field-requirements.csv` | 786 | 698 | `reviewed=701`，`draft=14`，`needs_resolution=71` |
| `最小参考资料库/source-screening.csv` | 81 | 74 | `reviewed=77`，`needs_resolution=4` |

总计恰好 1,924 个单元格发生变化。逐表把迁移后文件与备份按行、主键和列重读比较，除白名单内 `review_status` 外没有其他差异。`facts.resolution_state`、断言 `extraction_status`、字段要求 `requirement_status`、来源筛选 `screening_status` 的变化数均为 0；行数、主键集合和顺序也没有变化。

这次迁移只表示每行已按当前证据和项目口径复核，不批准事实本身的争议或缺口。58 条 `needs_resolution` 事实、71 条 `needs_resolution` 字段要求、14 条 `pending_verification` 草稿要求和 4 条未决来源筛选记录保持原样；断言仍使用原有 `source_checked` 等提取状态，没有批量改成 `independently_reviewed`。

## 备份和哈希

执行前备份保存在 `审计/合并备份/DATA-REVIEW-MIGRATION-20260813/`，包含四张原表、原哈希清单、迁移清单和独立复核报告。迁移后文件哈希为：

| 正式表 | 迁移后 SHA-256 |
|---|---|
| `最小参考资料库/fact-assertions.csv` | `173BB2406374C25767AAFF6404BC2E9854E5EF133A154668AB167E9BE367C0BF` |
| `数据/facts.csv` | `634ABA9D93229219DDD4DA9952612E97841C1937C9D032B45FA4C3FF42F3DEA2` |
| `数据/field-requirements.csv` | `429DD029364820A8F5E428F62EDAF4C80AE692230AF13A7B7090EB1B32A4BC19` |
| `最小参考资料库/source-screening.csv` | `C06EA015AA97627F7324F518DBF58080F227521E81960F275C2CFCA2FD49F968` |

备份恢复只能用于撤销本次行级状态迁移，不能覆盖之后可能发生的语义修订。若以后需要改变事实值、断言定位、字段要求结论或来源筛选结论，应另开工作包。

## 验证结果

每完成一张表都运行正式校验器。最终结果为：

```text
PASS: 32-table research data model; 91827 checks executed.
Registry: 323 columns, 488 enum values.
```

第一张表写入后的首次内存差异检查把修改前对象与待写对象复用了同一引用，错误报告 `diff count 0`。这是总控的操作构造错误，不是用户拒绝、自动审批拒绝、审批连接失败、沙箱拒绝或工具运行失败。磁盘备份没有改变；总控随即以备份文件重新做逐行、逐列比较，确认 598 个目标状态变化、语义变化 0、其他变化 0，再继续后三张表。最终四表联合差异检查为 1,924/0/0，并由正式校验器通过。

根 README、AGENTS、研究计划、当前状态、任务台账、里程碑和决策记录已随正式状态变化同步更新。后续包可以把 `reviewed` 理解为行级复核完成，但仍须读取语义状态，不能把 `provisional`、`needs_resolution` 或 `pending_verification` 当成已定案。